# Superseded planning documents

Files here are not built on and not referenced by any generator, page, or
script (confirmed by grep across `.py`/`.html`/`.md`/`.js` before each move).
Kept for history, not for reuse.

## 6S_Success_Home_Quest_Master_Plan_and_Card_Appendix.html

Moved 2026-09-17 per GitHub issue #7. Proposed 212 micro zones against the
Field Manual's 114, and its 2,844 rows reduce to a few hundred unique
strings once eight boilerplate sentences per zone are collapsed, the exact
duplication the v3 card architecture removed. The games review recommended
not building on it; its own canon defect ("Set in Order") was already fixed
before this move, confirmed at 0 occurrences.
