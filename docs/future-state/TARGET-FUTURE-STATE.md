# Home Quest: Target Future State

Owner: operator (Prompt 9, `super prompts/6S-SUCCESS-HOME-QUEST-MOBILE-CLAUDE-CODE.md`).
First written 2026-09-02. Review whenever this prompt is run again, and after
any release or incident.

This is a living standard, built from what this sandbox can actually verify.
Most metrics below have no instrumentation reachable from here: no Stripe
credential, no analytics credential, no app store account, no device. Where a
baseline is unavailable it says so rather than guessing, per CLAUDE.md
section 0.4 ("unknown is not unused") and this prompt's own step 2 rule
("never invent a baseline").

The mobile app has never been opened by anyone other than the operators who
wrote it, on a simulator or bundler, never on a real phone, and it has zero
installs, zero households, and zero real users of any kind. Every "desired
range" below is therefore a hypothesis to validate once the on-device pass
(`mobile/quest-app/ON-DEVICE-TEST.md`) and a real release exist, not a target
being missed today.

| # | Dimension | Metric | Baseline | Desired range | Rationale | Guardrail | Source | Confidence | Owner | Review date |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | User outcome | Zones verified holding per active household after 30 days | Unknown, zero households exist | >=1 zone still holding at day 30 for a majority of households that finish one | The product's own promise is a house that stays organized, not one reset once | None held after 30 days would mean the product does not do the one thing it claims | Would need account/sync layer (5B.8, blocked) or a manual survey | None (0 households) | operator | after first 10 real installs |
| 2 | Method fidelity | S-order correctness across all zones | 114 of 114 zones correct (sort, straighten, shine, safety, standardize, sustain), verified by parsing `quest-corpus.json` as JSON, not sampling | Stay at 114 of 114 | Safety as the fourth S is D-014, a settled decision; any drift is a trust defect, not a style choice | Any zone out of order blocks release | `docs/audit/CURRENT-STATE-AUDIT.md`, re-verified here 2026-09-02 | Observed fact | operator | every corpus change |
| 3 | Experience | Crash-free session rate; offline pass rate | Unknown, never run on a device or in production | No hard target yet; first target is "any session completes at all" | Calm and resilient offline is R1's core promise, not a nice-to-have | A single confirmed crash on the core loop blocks any store submission | None yet; `ON-DEVICE-TEST.md` check 9 is the first real test | Unknown | operator, then Phil on-device | after `ON-DEVICE-TEST.md` runs once |
| 4 | Household value | Households with >1 active member sharing progress | 0 (no account layer exists; 5B.8 blocked) | Not applicable until 5B.8 | R1.1 scope, explicitly gated behind R1 usability validation in the source prompt | None yet | Backlog 5B.8 | N/A, feature does not exist | operator | when 5B.8 is unblocked |
| 5 | Trust | Photos leaving the device; network calls from app code | 0 confirmed by source and by an automated test (`EXPO_OFFLINE=1 npx expo export` and the offline-first design itself; no `fetch`, `XMLHttpRequest`, or analytics SDK anywhere in `mobile/quest-app/`) | Stay at 0 | "No network calls at all" is written into `App.js`'s own header comment as a non-negotiable | Any PR adding a network call to this app needs an explicit decision, not a silent merge | Source read directly 2026-09-02: grep for `fetch(`, `XMLHttpRequest`, `axios` across `mobile/quest-app/` returns nothing outside `node_modules` | Observed fact | operator | every dependency added |
| 6 | Product quality | Unit tests passing; preflight gates covering the app | 24 of 24 tests pass across 3 files (`importProgress.test.js` 10, `pickCard.test.js` 7, `eventLog.test.js` 7); 3 dedicated preflight gates (`gate_mobile_js_tests`, `gate_mobile_npm_test_complete`, `gate_mobile_finish_actions_distinct`) | Every new `lib/*.js` file ships with a `*.test.js` sibling before merge | Two real defects (dead "Not now" button, identical finish-screen actions) shipped and sat undetected until a cold read found them; tests exist now specifically because that class of bug is otherwise invisible from source alone | A new `lib/` file with no test file should not merge | Ran `npm test` directly 2026-09-02, all pass; `python ops/preflight.py`, clean | Observed fact | operator | every mobile PR |
| 7 | Business health | Acquisition, conversion, AI cost, support load for the app | 0 across the board, no store listing exists | Not applicable pre-launch | Same honest floor as the web business: ROADMAP-2026-2029.md's own numbers, no invented target | None yet | ROADMAP-2026-2029.md | Observed fact (zero, not unmeasured) | operator | at first store submission |
| 8 | Platform parity | Function-level parity between web Quest and mobile MVP | Mobile has done/skip loop, house progress, import; missing mode selection, timer, photo capture, recommendation engine, streak, progress map, analytics (7 named gaps) | Recommendation engine is the next parity gap worth closing, per the PRD's own ordering, and only after 5B.4 closes | A native app that does less than the installable web page is not worth the download | Do not build ahead of 5B.4/5B.9 closing, per the PRD's explicit recommendation | `docs/product/PRD.md`, `docs/audit/CURRENT-STATE-AUDIT.md` | Observed fact | operator | after 5B.4 and 5B.9 close |
| 9 | Content operations | Corpus version match between web and mobile | Both read from the same `quest-data.js`-derived source; `ops/build_mobile_corpus.py` regenerates `mobile/quest-app/assets/quest-corpus.json` from it | Corpus must never be hand-edited on the mobile side | Two divergent copies of 684 cards would silently drift, the exact "generator ownership" defect class this repo already gates elsewhere | `gate_mobile_corpus_current` in `ops/preflight.py` | Observed fact | operator | every corpus change |
| 10 | Learning velocity | Real, confirmed defects found and fixed per app-focused cycle | 3 confirmed defects in 3 consecutive cycles (2026-08-31 dead "Not now" button, 2026-09-02 identical finish actions, 2026-09-02 missing on-device check for the first fix), each with a regression test or script check added | Keep finding and closing at least one real gap per app-focused cycle until 5B.4/5B.5/5B.9's on-device halves close | The pattern that has worked here is cold-reading source for the "control whose promise and handler have drifted apart" shape, not assuming a green build means the app works | Stop only once the on-device pass actually runs and reports back | `ops/NIGHTLY-LOG.md`, BACKLOG-2026-H2.md's 5B section | Observed fact | operator | every app-focused cycle |
