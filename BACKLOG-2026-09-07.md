# Backlog, reprioritised 7 September 2026

Supersedes the ordering in `BACKLOG-2026-H2.md`. That file's process rules still
hold; its priorities predate this review.

Reprioritised on Phil's direction: expand the micro zone concepts and the decks,
improve the web app around micro zones, keep detailed information on all six 6S
activities for every micro zone, and keep pushing image and video generation.

Everything below was measured this week, not assumed. Where something could not
be checked it says so.

---

## 0. The one number that decides the order

**A three-week decline, not a rise.** 76 visitors and 190 visits in the last 30 days (read
directly from the analytics database 2026-09-21 14:05 UTC, superseding the 78/200 figure
this section carried from 2026-09-17). The last 7 days fell for the third week running: 18,
then 14, then 10 visitors. One sale ever, $19, and that buyer was a personal referral.

So the test applied to every item is the one in `GOALS.md`: does this help
somebody arrive, or does it improve something nobody is looking at yet. Both
kinds of work appear below, and the second kind is marked HOLD rather than
dropped, because building ahead of the constraint is how a compounding asset
gets abandoned in month four.

---

## 1. Done this week, for the record

| Item | Evidence |
|---|---|
| **C8 (`REVIEW-COMMERCE-2026-09-07.md` section 7): the virtual consult's own text link turned into a real, origin-tracked button on all 163 organic-entry pages, 2026-09-22 operator.** | Every unblocked row above was again done or Phil-gated; C8 was the one real, unactioned "correct on its merits" exception the review's own suggested order carves out from the traffic-constraint hold ("Everything marked Below waits behind O1, with the exception of C8, which is half a day and correct on its merits"). Verified live before building: `site/zones/entryway-the-landing-spot.html` still carried the consult as a plain `<a href="../consulting.html">` inside a `font-size:14.5px;opacity:.85` sentence, no button, no origin, matching the review's 2026-09-07 finding exactly, seven weeks later. The arithmetic that makes this matter: contribution per order is $242.45 on the consult against $18.15 on the print pack, a 13.4:1 ratio, next to a button styled to look like an afterthought. **Built.** `ops/build_zone_pages.py`'s `offer()`/`room_offer()` (114 zone pages, 20 room pages) and `ops/build_articles.py`'s `offer()` (the 2 generic pillar articles) now render a real `<a class="btn btn-ghost btn-sm" data-sku="CN-VIRTUAL">` button beside the existing explanatory sentence, carrying `?from=zone:<slug>` / `?from=room:<slug>` / `?from=article:<slug>`. The remaining 27 hand-authored articles (confirmed: no generator owns `site/articles/`, per `ops/specific_articles.py`'s own docstring) needed a different approach: new `ops/wire_consult_cta.py`, idempotent, inserts the same button into each article's existing print-pack CTA row; correctly skips `index.html`, the 2 B2B articles (`what-a-5s-engagement-costs.html`, `why-5s-decays-after-six-months.html`, a different offer for a different buyer) and `why-you-cant-see-your-own-clutter.html`, which already links straight to the Stripe payment link with `data-sku="CN-VIRTUAL"` set, a stronger version of the same fix, not a gap. `site/assets/js/measure.js` gained a `service-cta` click handler (parallel to the existing `buy-click`/`quote-click` handlers): parses the `from=` query string into a page type and an origin slug, reads `data-sku`, and fires one event, so a consult click can finally be told apart from a pack click in the data. **Coverage confirmed exactly matches the review's own "163 pages" figure**: 114 zones + 20 rooms + 29 articles (31 minus the 2 correctly-excluded B2B ones), with the zones/articles index pages correctly excluded from the count. New `gate_consult_cta_current` in `preflight.py` (pure logic in `check_consult_cta_current`) re-reads the real shipped `site/zones/`, `site/rooms/` and `site/articles/` files (skipping the site-wide `_`-prefixed scratch-probe convention, found live during this cycle's own test run rather than assumed) and fails if any page's button is missing, downgraded back to a plain text link, or has its origin query string stripped; `ops/tests/test_gate_consult_cta_current.py` (5 cases, including the real committed site) fail-then-pass proved directly. Idempotency proved directly: `ops/wire_consult_cta.py` and a full `build_zone_pages.py`/`build_articles.py` regeneration both produce a byte-identical tree on a second run. Full `preflight.py` (every gate passed, 24 warnings, all previously diagnosed sandbox limits), `check_urls.py` (189/189), `audit_pages.py` (193/0), `affiliate.py --check` (164 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans across zones/rooms/articles) all clean after. No price or product touched, no new page (163 existing pages gained one button each); IndexNow not applicable (no new URL). |
| **Paid book content leaking into the free social-post pool, found and fixed; two other silent `corpus_posts.py` extraction gaps closed in the same pass, 2026-09-22 operator.** | Continued the standing cold-read fallback the last PM check-in named: `ops/corpus_index.py` and `ops/generated_products.py`. `corpus_index.py`'s classifier matched kind "video-script" on the bare substring `script`, also a literal substring of `manuscript` and `description`; every `chapter_NN_manuscript.md` (the paid $9.99 book's own chapter text) and every `*-description*.md` file were silently classified ready to post free (182 of 821 pooled "video-script" units traced to a manuscript, confirmed live, not assumed). Separately, `corpus_posts.split_posts()` required a `\n---+\n` divider between numbered `## ` sections and returned nothing, silently, for any file with none, which was most of the corpus (119 of 153 ready `facebook-post` files, 17 of 51 `linkedin-post` files). Fixed: anchored the video-script pattern to real filenames; reclassified `quote-card` as non-publishable (a designer brief, byte-verbatim duplicate of the `quote` kind); rewrote `split_posts` to split on the heading itself via the already-trusted `_h2_sections`; added a `split_posts_or_whole` fallback for single-post files (`facebook-longform-post.md`, `facebook-group-discussion-post.md`). Net: `facebook-post` 155 to 259 usable posts, `linkedin-post` 311 to 476, `video-script` 816 with 0 manuscript leakage (was 182). Fixed the stale `social_units` citation this changed in `RISKS.md`/`STATUS.md`. Two new `preflight.py` gates (`gate_corpus_posts_no_manuscript_leak`, `gate_corpus_posts_extraction_yield`), each proved to fail by name on the reverted code via `run_gate` and pass on the fix; new `ops/tests/test_gate_corpus_posts.py` (6 cases) and `ops/tests/test_corpus_posts.py` extended 18 to 25, both fail-then-pass proved. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), all 235 other test files, `check_urls.py` (189/189), `audit_pages.py` (0 findings), `affiliate.py --check` (164 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed (this content has never been posted anywhere); IndexNow not applicable. |
| **Two silent generator bugs closed: a fingerprint-chain gate gap and a dead retry comparison, both found in a cold-read of low-mention `ops/*.py` files, 2026-09-21 operator.** | Continued the standing cold-read lane (a concurrent PM check-in had just named `render_all_zone_videos.py` next). (1) `ops/wire_measure.py` writes its script tag as `src="{pre}assets/js/measure.js"`, an f-string placeholder, not a literal `href=`; `gate_generator_chains_fingerprint` only scanned `build_*.py` and only matched `href=`, so it was invisible on both counts. Reproduced live in a scratch copy: a standalone `python ops/wire_measure.py` stripped the `?v=` cache-busting hash off all 190 pages' measurement tag, confirmed by diff. Widened the gate's scan to `wire_*.py`/`canonical_links.py` and its regex to match `src=` and the placeholder shape; proved the widened gate fails against the unfixed file (`git stash`); fixed by chaining `fingerprint_assets.main(False)` at the end of `wire_measure.py`'s own `main()`, the established remedy for this exact shape. (2) `ops/render_all_zone_videos.py`'s resource-exhaustion retry compared `p.returncode` to `3221225794`, the unsigned value of `0xC0000142`; CPython reports a Windows crash code as signed (`-1073741502`), so the retry could never fire. Not reproducible in this Linux sandbox; fixed from CPython's documented signed-DWORD behaviour, both forms accepted. `test_gate_generator_chains_fingerprint.py` widened 9 to 12 cases, `test_render_all_zone_videos.py` widened 4 to 5, both fail-then-pass proved directly. Full `preflight.py` clean after (every gate passed, 22 warnings, all previously diagnosed sandbox limits). No price or product touched, no page changed; IndexNow not applicable. |
| **Handed-off `preflight.py --deep` run closed clean; a stale "needs Phil's machine" claim in `MARKETPLACE-LISTINGS.md` found and corrected, 2026-09-21 operator.** | The 10:1x PM check-in handed "run `preflight.py --deep` today" to this slot by name; ran it, every gate passed, 24 warnings, all previously diagnosed sandbox limits. Beyond that, `MARKETPLACE-LISTINGS.md` twice claimed `build/listings/build_etsy_assets.py` "cannot be run... from this sandbox," blocking the documented `print_fix.css` simplification on Phil's own machine. False since 2026-09-13 (`find_browser()` already checks this sandbox's own `/opt/pw-browsers/chromium`, per `gate_etsy_pdfs_current`'s own docstring), never reread since. Verified directly in an isolated `/tmp` directory, never touching the committed `build/listings/etsy/` tree: both `print_fix.css` variants render 76 pages, 0 blank, identical text, identical card size (180 x 244.5pt); only the page margin differs. Corrected both stale claims with dated notes; left the actual `print_fix.css` removal undone, since it still needs a human eye on the rendered page and Etsy is not live yet (owner gate 4). Full `preflight.py` (every gate passed, 22 warnings, identical baseline), `check_urls.py` (187/187), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed; IndexNow not applicable. |
| **D9's own repeated reopening closed with a recorded decision (`DECISIONS.md` D-020), 2026-09-21 scheduled operator.** D9 (route link equity into zone pages, `REVIEW-DISCOVERY-2026-09-07.md`) had been independently reopened four times on 2026-09-21 alone, each cycle re-confirming the same structural ceiling rather than moving it: small rooms cannot reach a median zone in-degree of 15 from room-sibling links alone, and two exhaustive reads of all 114 zones' own `purpose`/`done_looks_like` text found every genuine same-job pair the content honestly supports without fabricating a connection (`CLAUDE.md` section 6). The fourth status block already reasoned through the correct call, a GREEN-tier judgement to treat the floor (met, live `ops/link_graph_report.py`: min 9, max 21, avg 12.4, 0 orphans) as D9's real bar and downgrade "median above 15" to aspirational, but never recorded it anywhere a future cold read would find it before re-attempting the same search, `CLAUDE.md` section 21's "do not repeatedly reopen settled decisions" applied to a decision instead of a build artifact. Added `DECISIONS.md` D-020 (decision, rationale, evidence, rejected alternatives, revisit condition, matching D-019's own format), added its row to section 43's own index table, verified both directions with `check_decisions_index()` directly (0 problems), and pointed D9's own section in `REVIEW-DISCOVERY-2026-09-07.md` at it so the acceptance line no longer reads as open work. Confirmed no `preflight.py` gate enforces the median clause as a hard requirement, so this is a decision-record fix, not a gate change. Full `preflight.py` (every gate passed, 22 warnings, identical set to the pre-change baseline, `gate_decisions_index_current` specifically confirmed), `check_urls.py` (187/187), `fix_dashes.py --check` (0/0) all clean after. Also re-tested this sandbox's own network egress directly rather than citing prior cycles (`curl` to `6s-success.com`/`api.stripe.com`, both a 403 policy denial from the agent proxy, confirmed live): still none, consistent with every prior finding. No price, product or site page touched; IndexNow not applicable. |
| **A real, live defect in `ops/ship.py` itself, the one tool this repository is told to ship through: a commit whose only change was a brand-new file silently never shipped, while every step still reported "ok"; fixed and gated, 2026-09-21 operator.** | Every unblocked backlog/review row again done or Phil-gated (8 GitHub issues unchanged, decision/blocked-on-art; no mail credential; preflight clean, 22 warnings, all previously diagnosed), so continued the standing low-mention `ops/*.py` cold-read fallback the last three PM check-ins named by name: `ship.py` first. `dirty()` excluded every untracked ("??") `git status` line before deciding whether there was anything to commit, a helper copied from `ops/sync_push.py`, where the exclusion is correct (that tool only decides whether to refuse a rebase, so a stray untracked scratch file should not block it). `ops/ship.py` reuses the same helper for a different decision: whether to run `git add -A` and commit at all. Checked directly rather than assumed whether the scratch files the exclusion was meant to shrug off could even reach `git status`: they cannot, `site/**/_*.html` is gitignored, so the exclusion protected against nothing there while silently breaking the case where a commit's only content is one or more new files. Proved live in an isolated sandbox repo (a real bare origin and clone, driving the actual shipped `ops/ship.py`, not a re-description of it): a brand-new untracked file with no tracked file modified made `changes` come back empty, took the "nothing to commit" branch, skipped `git add -A` entirely, and reported "commit ok / push ok / verified ok" while the file never reached `origin/main`, exactly the "green check, nothing actually shipped" shape `ops/verify_deploy.py`'s own `CRITICAL_PAGES` fix (same day, this section) closed for a different surface. Fixed by removing the `??` exclusion from `ops/ship.py`'s own `dirty()` (left `ops/sync_push.py`'s copy untouched, since its exclusion is correct for its own, different decision). New `ops/tests/test_ship_new_file_committed.py` (2 cases, a real bare-origin sandbox in the same style as `test_ship_conflict_safety.py`) fail-then-pass proved directly: temporarily restored the old exclusion, watched the new-file-only case fail by name with the exact "reported success, file never reached origin" text, restored the fix byte for byte (`diff` confirmed identical), reran clean; the existing `test_ship_conflict_safety.py` (3 cases) still passes unchanged, so the fix does not disturb the conflict-resolution path it already protects. `gate_tests()` already globs every `ops/tests/test_*.py` file, so no new `preflight.py` wiring was needed; a hand-written gate would only re-describe what the sandboxed test already proves end to end. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits, identical set to the pre-fix baseline), `fix_dashes.py --check` (0 em dashes, 0 en dashes) clean after. No price, product or site page touched (an internal ops tool and its test gained a fix and coverage); IndexNow not applicable. |
| **ops/verify_deploy.py's smoke-test page list was missing every real buy path, 2026-09-21 operator.** Every unblocked row in `BACKLOG-2026-09-07.md` and `REVIEW-DISCOVERY-2026-09-07.md` was again done or Phil-gated (8 GitHub issues unchanged, all `decision`/`blocked-on-art`; no mail credential), so this pass continued the standing low-mention `ops/*.py` cold-read fallback, starting with the two lowest-mention files (`build_social_pins.py`, `verify_deploy.py`, both 18 mentions). Live-verified `build_social_pins.py` by actually rendering a real card (`--zone "Landing Zone"`) and reading the PNG: correct dimensions, correct fit, no defect. `verify_deploy.py` read cold, then checked its own `PAGES` list (what "prove a deployment actually works" actually tests) against the real page list in `site/`: it covered 13 marketing pages (method, shop, book, about, contact, resources, invest, the four legal pages, consulting) but never `quest.html` (the Home Quest app, `GOALS.md` O5, the actual product), `deck.html` (the free lead magnet most outbound links point at), `corporate.html` (the B2B enquiry funnel, the other real revenue channel named in `GOALS.md` section 2), `thanks.html` (what a paying customer sees immediately after checkout) or `how-we-make-money.html` (the affiliate disclosure every product page promises is "above them", `CLAUDE.md` 5e). A deploy that 404'd any of those would have scored 10 of 10, the exact "green check, broken customer path" shape `ops/deploy_freshness.py`'s own docstring already documents for a different dimension (current vs. working), just never closed for this one. Added all five to `PAGES`. New `CRITICAL_PAGES` in `verify_deploy.py` names the subset that must never silently disappear again; new `gate_verify_deploy_pages_current` in `preflight.py` re-derives it on every run and fails if a future edit drops one from `PAGES` or if the page it names stops existing in `site/`. `ops/tests/test_gate_verify_deploy_pages_current.py` (3 cases) fail-then-pass proved directly: a page removed from `PAGES` fails by name, a page named in `CRITICAL_PAGES` but absent from `site/` fails by name, the real committed state passes clean. `ops/tests/test_verify_deploy.py` still passes (11/11, now correctly reporting "all 18 pages" instead of 13). Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), `fix_dashes.py --check` (0/0) clean after. No price or product touched, no new site page (an internal ops tool and its test gained coverage); IndexNow not applicable. |
| **D9 follow-on (further honest partial, evidence of a structural ceiling), REVIEW-DISCOVERY-2026-09-07.md, 2026-09-21 operator (scheduled cycle).** Checkout arrived shallow and detached; unshallowed and fast-forwarded onto `origin/main` (964-commit fast-forward, no reset or force). `python ops/preflight.py` clean on the first run (every gate passed, 22 warnings, all previously diagnosed sandbox limits). Read the four newest `ops/NIGHTLY-LOG.md` entries: the standing handoff is D9 (route more link equity into zone pages, or find a structural lever other than D10, since D10 was confirmed not to move D9's own number). Before hunting more `ZONE_RELATIONS` pairs, checked whether an already-shipped mechanism was under-used: `zone_page()`'s own "The rest of the {room}, in working order" table of contents already links every zone to every other zone in its own room, confirmed live on all 114 pages, contributing 2 to 6 links per zone depending on room size. This explains the low end of the distribution structurally: Stair Landing, both bathrooms, Pantry, Dining Room, Hall Closet and Entryway all have 5 or fewer zones, so room-sibling links alone cannot reach 15 there, whatever `ZONE_RELATIONS` adds. Then read all 114 zones' real `purpose` and `done_looks_like` text cold a second time, looking specifically for genuine same-job matches the first pass missed rather than assuming the corpus was exhausted. Found 7: `Toilet Area` (Primary and Guest Bathroom, identical job, both "keep the smallest, dirtiest patch of the room clean and stocked"), `Shower or Tub` (same two rooms, both "keep only the products in use within reach"), `Media Center`/`Primary Media Zone` (Living Room and Family Room, both about labelled cables and a findable fault), `Toy and Play Zone`/`Toy Storage Zone` (Family Room and Kids Bedroom, both "so a child can put them away without an adult"), `Landing Zone`/`School and Activity Launch Zone` (Entryway and Kids Bedroom, both about staging what leaves the house), `Crib and Sleep Zone`/`Bed and Sleep Zone` (Nursery and Kids Bedroom, both "and nothing else"), `Power Tool and Battery Zone`/`Power Tool Storage` (Garage and Workshop, both "guarded" and "ready to run"). Added all 7 as new two-member `ZONE_RELATIONS` groups, each grounded in the zone's own quoted text, nothing invented. Also found two zones belonging in an existing group by job, not by name: Home Office's `Supply Cabinet` ("hold the consumables this office burns through in a year... backstock directly behind it") added as the `backstock` group's fifth member; Patio or Deck's `Surface, Rail, and Safety Zone` ("the structure you stand on and lean against, kept sound") added as the `floor-path` group's fourth, that group's own "why" text broadened from "the floor" to "the floor or walking surface" to stay true of every member. `_validate_zone_relations` passed (every new pair a real content.json zone); the existing `gate_zone_relations_rendered` re-derives from `ZONE_RELATIONS` itself and needed no code change, confirmed passing against the regenerated site, `ops/tests/test_gate_zone_relations_rendered.py` (6 cases) green. **Honest result, a real but small move, not a close.** `ops/link_graph_report.py` before/after: min unchanged at 9 (floor still met, 0 zones below 8), max 20 to 21, average 12.2 to 12.36, median 12.0. The count of zones clearing 15 did not move (17, unchanged), because most of the 16 zones this pass touched were already above the floor and gained exactly the +1 a two-member group contributes, not enough to cross the 15 line from where they sat. Recorded in `REVIEW-DISCOVERY-2026-09-07.md`'s own D9 section as a fourth status block, with the explicit finding that the room-sibling mechanism plus a second exhaustive cold read together point to a structural ceiling rather than an unexploited gap: getting the median past 15 from here needs either a mechanism this content set does not honestly support without `CLAUDE.md` section 6's fabrication, or a decision to treat the floor requirement (met) as D9's real bar and "median above 15" as aspirational. Recommending the latter as a GREEN-tier product-content judgement call (reversible, no price/product/customer surface touched) rather than re-deriving the same ceiling every future cycle. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), `check_urls.py` (187/187), `audit_pages.py` (191/0, 0 duplicate titles/descriptions), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no new page (16 existing zone pages gained one link each in an existing section); IndexNow will pick up the 16 changed pages on its next successful run. |
| **D12 (done for its own unblocked scope, honest partial on the rest), REVIEW-DISCOVERY-2026-09-07.md, 2026-09-21 operator (second cycle).** Checkout arrived shallow and detached; unshallowed and fast-forwarded onto `origin/main`, no reset or force. `python ops/preflight.py` clean on the first run (every gate passed, 22 warnings, all previously diagnosed sandbox limits). Read the last four `ops/NIGHTLY-LOG.md` entries; the most recent named D12 (rewrite and re-link the six specific articles) as "the next unblocked substantive row in `REVIEW-DISCOVERY-2026-09-07.md`." D12's own line: "Blocked on. Nothing for the writing and linking. Images are blocked on D2." Checked all four of its own acceptance criteria live before writing anything, not assumed from the row's own age. **Direct answer above the fold: real, live gap, fixed on all six.** Each of `how-to-organize-a-junk-drawer`, `why-mail-piles-up-by-the-door`, `why-you-always-lose-your-keys`, `why-the-medicine-cabinet-never-gets-cleared-out`, `why-you-cant-find-the-right-charger` and `why-you-have-to-dig-for-what-you-need` opened with a true, specific narrative hook (60-95 words) and only stated the actual answer in a separate `<p class="notice">` block below it, the identical first-60-words gap D1 already closed on all 114 zone pages, confirmed live before touching anything rather than assumed from the review's own framing. New `ops/specific_articles.py` (`DIRECT_ANSWERS`, one entry per slug, the same real answer the old notice block already stated, tightened to 60-75 words and moved to lead the page instead of follow it; nothing newly claimed, only reordered) is now the single source of truth for these six hand-maintained pages; the former hook paragraph moved to the second paragraph unchanged, and the now-redundant notice block was removed rather than left duplicating the lede. New `gate_specific_article_direct_answer` in `preflight.py` (pure logic in `check_specific_article_direct_answer`), re-deriving the expected opening paragraph from `ops/specific_articles.py` and diffing it byte for byte against the shipped HTML, the same pattern `gate_zone_direct_answer_current` already uses for zone pages; `ops/tests/test_gate_specific_article_direct_answer.py` (7 cases, including all six real committed articles) fail-then-pass proved directly: planted a stale answer on the real committed `why-you-always-lose-your-keys.html`, watched the gate fail by name citing the file and the mismatched text, restored byte for byte, reran clean. **10+ contextual inbound links: already honestly at its ceiling, not newly worked this cycle.** Re-measured with `ops/link_graph_report.py --detail articles` rather than trusted from A8's own prior figures (this file, 2026-09-15): charger 11 (over the 10-link target), mail 5, keys 4, dig-for-what 4, junk drawer 3, medicine cabinet 3. A8 already established most homes have only one real zone genuinely about keys, mail, the junk drawer or the medicine cabinet, so forcing a fifth or sixth link on those four would fabricate a connection `CLAUDE.md` section 6 forbids; nothing found this cycle changes that, so this criterion is recorded as already met where it honestly can be, not re-worked to force a number. **3+ images and a `Person` author: correctly still blocked**, on D2 (a real photograph) and D21 (a real bio and verifiable profile URLs) respectively; neither is fabricable, so D12 closes for its own unblocked scope rather than claims those two. Regenerated `ops/build_seo.py` (6 sitemap URLs' content hashes moved, nothing else) and `site/build-id.txt` after the content change, the same "source corrected, artifact never re-derived" drift class three of the last four log entries already caught, caught this time before committing rather than after. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), `check_urls.py` (187/187), `audit_pages.py` (191/0, 0 duplicate titles/descriptions), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans), `ops/audit_visual.py --all --mobile` (0 findings across 194 pages, the one already-documented non-blocking `kitchen-deck.html` font-load race) all clean after. No price or product touched, no new page (six existing articles restructured, zero added); IndexNow will pick up the six changed articles on its next successful run. |
| **D10 (done, against its own acceptance line), REVIEW-DISCOVERY-2026-09-07.md, 2026-09-21 operator.** Checkout arrived shallow and detached; unshallowed and fast-forwarded onto `origin/main`. `python ops/preflight.py` FAILED on the first run of this cycle: `publish-image-current` named real content (D9's own 69 changed zone pages, this section's own row above) sitting unpublished behind a failed `publish-image.yml` attempt, because `site/sitemap.xml`/`ops/sitemap-content-hashes.json` had not been regenerated after D9's own merge commit, the same drift class as D8's own sitemap gap two rows up. Fixed at the generator's own output (`ops/build_seo.py`), pushed and confirmed the fix unblocked the workflow before picking new work, per this session's own "if it fails, fixing that IS this run's work." With preflight clean, picked D10 (strengthen the room hubs), the next unblocked row this section's own D9 entry names by number. Verified this section's own claim live before building anything: every room page's H1 was still the bare room name ("Entryway"), and the manual's own "Where to start" tip (already the FAQ's answer to "where should you start") sat inside "For this room", three sections below the zone list it should answer for. Built `ROOM_JOB` in `ops/build_zone_pages.py`: one short clause per room, paraphrased from that room's own authored `intro` in content.json (the same sentence the lede paragraph already renders in full a few lines down), not a new claim about the room; every room name checked against the real content.json list before a page builds (`_validate_room_job`), so a renamed or added room fails loudly rather than shipping with no job in its own heading. H1 is now "{Room}: {job}" on all 20 pages. Moved the "which zone first" answer to a notice block directly above the zone list, beside the existing "how long does this room take" total; removed from "For this room" below so the same tip is not said twice on one page. Room-page median word count, re-measured live rather than carried forward from this section's own 1,057-word figure, was already 1,742 before this change (the room-map figure, room kit and other work since 2026-09-07 had already moved it) and 1,752 after, above the 1,600 target in guidance rather than link-list growth. **All three of this section's own acceptance lines are met:** H1 states the room and the job; both "how long" and "which zone first" render above the zone list; median word count above 1,600 in guidance. New `gate_room_hub_current` in `preflight.py` (pure logic in `check_room_hub_current`), re-deriving the expected H1 from `ROOM_JOB` and the expected "which zone first" text from content.json's own tips, diffing both byte for byte against the shipped HTML; `ops/tests/test_gate_room_hub_current.py` (7 cases, including the real committed site) fail-then-pass proved directly: an H1 reverted to the bare room name, a missing or post-list "which zone first" answer, and a duplicated "Where to start" line each fail by name; the real site passes clean. Also found and fixed while verifying: `site/build-id.txt` was stale against this commit's own content change; regenerated with `ops/build_id.py`. `preflight.py --own` could not complete in this sandbox (hangs on egress it does not have, a known limit); substituted a direct idempotency proof instead, regenerating `build_zone_pages.py`/`build_seo.py` a second time and confirming a byte-identical tree against what was committed. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (187/187), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans), `ops/audit_visual.py --all --mobile` (0 findings across 194 pages, one already-documented non-blocking font-load race on `kitchen-deck.html`) all clean after. No price or product touched, no new page (20 existing room pages restructured, 0 added); IndexNow will pick up the 20 changed pages on its next successful run. A concurrent PM check-in session pushed a STATUS.md fix mid-cycle; merged cleanly (`site/build-id.txt` the only conflict, resolved by regenerating it fresh rather than hand-picking either side, same hash before and after). |
| **D9 (partial, honest result), REVIEW-DISCOVERY-2026-09-07.md, 2026-09-21 operator.** Checkout arrived shallow and detached; unshallowed and fast-forwarded onto `origin/main`. `python ops/preflight.py` FAILED on the first run of this cycle: `publish-image-current` named real content (D8's own 22 changed zone pages) sitting unpublished behind a failed `publish-image.yml` run on commit `80168ae2`, because `site/sitemap.xml`/`ops/sitemap-content-hashes.json` had not been regenerated after D8's content change (`publish-image.yml` runs `preflight.py --own`, which checks the sitemap generator; this session's own plain `preflight.py` runs do not, the actual reason the drift shipped unseen). Reproduced the fix (`ops/build_seo.py`) independently, then found a concurrent session had pushed the identical fix (`58a4dca2`) while this cycle was diagnosing it; fetched and fast-forwarded onto it rather than duplicate the commit, confirmed CI re-triggered and `preflight.py` clean after. Per this session's own instructions, "if it fails, fixing that IS this run's work"; recorded fully in `ops/NIGHTLY-LOG.md` rather than repeated here. With preflight clean, picked D9 (route link equity into the zone pages), named next by the prior PM check-in and by D8's own handoff line above. D9's own examples (entryway shoes vs mudroom shoe storage; kitchen sink vs under-sink; nightstand vs nightstand) are not caught by the existing `_sibling_index()` exact-noun match, confirmed directly (`searchable()` returns different strings for each pair), so this needed a second index of genuine same-job groups across different zone names. Read all 114 real zone names cold and built `ZONE_RELATIONS` in `ops/build_zone_pages.py`: 22 hand-curated groups (workbench, hand tools, cleaning chemicals, landing surfaces, shoes, coats, sinks, nightstands, dressers, closets, desks, books, linens, charging, seating, dining, cooking, garden tools, seasonal outdoor gear, paper storage, backstock, floor paths, display), each a real shared job checked against the zone's own name, not invented to hit a number; every (room, zone) pair validated against the live content.json before a page can build (`_validate_zone_relations`), so a typo fails loud rather than silently dropping a link. Deliberately not exhaustive: about a third of the 114 zones (Kitchen's Upper Cabinet Zone, the Nursery's Changing Station, and similar) have no genuine same-job counterpart elsewhere and get no group, because inventing one is exactly what `CLAUDE.md` section 6 forbids. Rendered as a new "Zones that do the same job, elsewhere in the house" section, positioned after the existing sibling block and deduplicated against it so nothing lists twice on one page. **Honest result, not full compliance:** measured with `ops/link_graph_report.py` before and after. Floor requirement met: min in-degree 7 to 9, 0 zones below 8 (was 1, `stair-landing-the-wall-and-display-zone`). Median requirement not met: median in-degree 10 to 12, avg 10.7 to 12.2 (target: median above 15); 17 of 114 zones now clear 15 (was 6). 69 of 114 zones gained at least one new link; the other 45 have no genuine cross-room same-job counterpart in this content set. Recorded honestly rather than claimed done, matching D8's own precedent immediately above. New `gate_zone_relations_rendered` in `preflight.py` (pure logic in `check_zone_relations_rendered`), re-deriving the expected link set from `ZONE_RELATIONS` and `_relation_index()` and diffing byte for byte against the shipped HTML; `ops/tests/test_gate_zone_relations_rendered.py` (6 cases, including the real committed site) fail-then-pass proved directly (planted a stale link on the real committed `workshop-the-main-workbench.html`, watched the gate fail by name citing the file and the mismatched text, restored byte for byte, reran clean). Full `preflight.py` (every gate passed, 24 warnings, all previously diagnosed sandbox limits), `check_urls.py` (187/187), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans, 0 thin pages) all clean after. No price or product touched, no new page (69 existing zone pages gained one section each); IndexNow will pick up the 69 changed pages on its next successful run. D10 (room hub depth) is the next unblocked row in this section, a separate ~3 operator-day piece of work; extending `ZONE_RELATIONS` further without fabricating connections is the honest way to close more of D9's own remaining gap, correctly left for its own cycle. |
| **D8 (partial, honest result), REVIEW-DISCOVERY-2026-09-07.md, 2026-09-21 operator.** Confirmed D1's CI green first (`checks.yml` #1222, `publish-image.yml` #359, both `success` on `44e8598e`), then D6/D7 already fixed (0 duplicate H1s, all 114 titles grammatical, confirmed live), so per the last PM check-in's own handoff this pass picked D8: the five `ZONE_SPECIFIC_READING` articles (keys, mail, charger, medicine cabinet, junk drawer) were each reachable from only the one zone they were originally written for, well under D8's "at least 10 contextual links" target. Read every zone's own real published text (passes, hazards, judgement call) for a genuine, specific mention of the same friction rather than link zones to hit a number. Found real, grounded matches on 12 more zones (a stray charger on 10 zones total: dining room sideboard, both bedroom nightstands, kids' study desk, primary bathroom toilet area, home office desk, kitchen sink zone, both living room surfaces; a second junk drawer on the entryway console; mail on the dining room sideboard and stair landing; keys on the primary bedroom dresser top and stair landing; expired medicine on the nursery backstock shelf) and added one `ZONE_SPECIFIC_READING` entry each, title and description grounded in the zone's own quoted words, nothing invented. **Honest result, not full compliance:** the charger article now has 10 real inbound links, meeting D8's target; keys (3), mail (4), junk drawer (2) and medicine cabinet (2) do not, because most homes only have one real zone genuinely about each of those, and no further zone's own text supported a fifth without fabricating a connection, which `CLAUDE.md` section 6 and this repo's own standing practice both forbid. Recorded here rather than silently claimed done. No new gate needed: `gate_general_reading_differentiated` (non-diagnosed zones) and `gate_diagnosis_rendered` (the two diagnosed zones touched, `entryway-the-bench-or-console` and `kitchen-the-sink-and-dishwashing-zone`) both already re-derive `ZONE_SPECIFIC_READING` fresh from the module on every run, so the new entries are already gate-protected against drifting; both confirmed passing directly (`python ops/tests/test_gate_general_reading.py`, `test_gate_diagnosis_rendered.py`, both green, "real site clean if built"). Regenerating touched 22 zone pages total (12 for the new links plus 10 more from the deterministic general-reading rebalance the added specific entries free up capacity for elsewhere, the same documented cascading mechanism the two original non-diagnosed `ZONE_SPECIFIC_READING` entries already exercise). **One real transient caught and re-verified, not shipped on:** a first full `preflight.py` run (started concurrently with `audit_visual.py --all --mobile`, repeating the exact mistake the D5 cycle's own "changing next cycle" note warned against) reported `test_gate_page_ownership_registry.py` failing 1 of 6 cases; rerun standalone immediately, 6 of 6 passed, and a second full `preflight.py` run by itself (not concurrent with anything) came back clean, confirming the first result was a concurrency artifact of this sandbox, not a real defect, before anything was committed. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (187/187), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans), `ops/audit_visual.py --all --mobile` (0 findings across 195 pages, one already-documented non-blocking font-load race on `kitchen-deck.html`'s hidden print sheet) all clean after. No price or product touched, no new page (22 existing zone pages gained or lost one related-reading link each); IndexNow will pick up the 22 changed pages on its next successful run. D9 (zone-to-zone routing, median in-degree above 15) is the next unblocked row in this section; zone in-degree is unchanged by this pass (still min 7, max 17, avg 10.7, confirmed via `ops/link_graph_report.py` before and after), a separate ~1 operator-day piece of work, correctly left for its own cycle. |
| **D4 (household variants), REVIEW-DISCOVERY-2026-09-07.md, shipped for the 12-zone pilot cohort, 2026-09-20 operator.** Checked epics 1 through 4 before falling back to another narrative cold-read: preflight green, GitHub's 8 open issues all decision/blocked-on-art, and BACKLOG epics A-C above all done or Phil-gated, but the discovery review's own D3/D4 ("blocked on nothing") were still measurably open (0 occurrences of "apartment"/"inches" sitewide, confirmed fresh, not carried forward). Authored 2 real household conditions per pilot zone (no console and cannot drill, shared by several people, small kitchen, mobility limits, and similar, grounded in each zone's own purpose text, nothing invented) with distinct guidance under each; new `variants` field on `content.json` (synced to `mcp/content.json`), rendered by `variants_html()` in `ops/build_zone_pages.py` as "If your `<thing>` is not like this", after the zone's own standard and before the safety notice. New `gate_variants_rendered` in `preflight.py` re-derives the block from the corpus and diffs it against the shipped HTML byte for byte; `ops/tests/test_gate_variants_rendered.py` (7 cases, including the real committed site) fail-then-pass proved directly (planted a stale sentence and a dropped block, watched the gate fail by name, restored, reran clean). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no new page (12 existing zone pages gained one section each); IndexNow will pick up the 12 changed pages on its next successful run (no egress from this sandbox to submit directly). D3 (capacity/sizing guidance) shipped later the same day, row below. |
| **D3 (capacity and sizing guidance), REVIEW-DISCOVERY-2026-09-07.md, shipped for the same 12-zone pilot cohort, 2026-09-20 operator.** Named unblocked and unpicked across three PM check-ins (18:40, 19:20, 19:40) on 2026-09-20, then handed to this operator by name at 20:10 ("NEXT FOR THE OPERATOR: D3"). Checked before starting: preflight green, GitHub's 8 issues unchanged (decision/blocked-on-art), BACKLOG epics A-C above all done or Phil-gated, D3's own acceptance ("Blocked on. Nothing.") still open (0 occurrences of "inches" and "how much space" sitewide, reconfirmed fresh). Authored a concrete capacity rule and a "does not fit" sentence per pilot zone (grounded in each zone's own already-published `done_looks_like`/`passes` text, e.g. "two pairs per person" for the shoe rack, "no more than six tools" for the cooking crock, nothing invented); new `capacity` field on `content.json` (synced to `mcp/content.json`), rendered by the new `capacity_html()` in `ops/build_zone_pages.py` as "How much this `<thing>` can actually hold", placed after the zone's own standard and before the D4 variants block. The "does not fit" sentence links a new `id="honest-count"` anchor added to `site/articles/zone-too-small-for-what-it-holds.html` (hand-authored, confirmed no generator owns it), reusing KC-007's own "inadequate capacity" root cause rather than inventing a new one. New `gate_capacity_rendered` in `preflight.py` re-derives the block from the corpus and diffs it against the shipped HTML byte for byte, and separately confirms the target anchor actually exists on the article (not just that the href string looks right); `ops/tests/test_gate_capacity_rendered.py` (8 cases, including the real committed site) fail-then-pass proved directly (planted a stale rule sentence on the real committed `entryway-the-landing-spot.html`, watched the gate fail by name citing the file, restored byte for byte via `diff`, reran clean; also proved the missing-link and missing-anchor cases synthetically). Regeneration proved idempotent: reran `ops/build_zone_pages.py` and `ops/build_seo.py` a second time against the already-changed tree and confirmed zero further diff, so the committed output really is what the generators produce, not a hand-touched approximation of it. `ops/build_seo.py` run by hand after the content change (D4's own follow-on entry below documents why `build_zone_pages.py` does not chain it automatically); `site/sitemap.xml`'s 12 affected URLs were already dated 2026-09-20 from the D4 work earlier the same day, so only the content-hash file needed the new hash recorded, confirmed by reading the diff rather than assumed. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits, matching the pre-change baseline), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans) all clean after. No price or product touched, no new page (12 existing zone pages gained one section each, one existing article gained an id attribute); IndexNow will pick up the 12 changed zone pages and the 1 changed article on its next successful run (no egress from this sandbox to submit directly). |
| **D4 follow-on, found 2026-09-20, this operator, then converged onto a concurrent, more complete fix.** `python ops/preflight.py` FAILED on this cycle's own first run: `publish-image-current` reported the D4 commit unpublished. Confirmed against the real GitHub Actions log (run 353, the attempt for this exact commit): it failed on `gate_generator_ownership`, naming `site/sitemap.xml`/`ops/sitemap-content-hashes.json` as stale. `ops/build_zone_pages.py`'s own `main()` does not chain `ops/build_seo.py` (confirmed by grep, unlike `build_resources.py`/`build_corporate.py`, which do), so D4's real content change to 12 zone pages shipped without its sitemap dating catching up. Reran `ops/build_seo.py` locally and confirmed the same 12-URL fix a concurrent session had already pushed (`197c3a68`), which also added `.githooks/pre-commit` and `ops/check_sitemap_current.py` so a stale sitemap is refused at commit time rather than caught by CI later; fetched before pushing and rebased onto it rather than duplicate the fix. No new gate needed: `gate_generator_ownership` already caught this correctly. Full account in `ops/NIGHTLY-LOG.md`, same date. |
|---|---|
| The paid $19 EPUB's own manuscript source carried 17 live British `organise`/`organising`/`organisation` spellings across 9 of its 50 chapters, inconsistent with the same book's own American spelling everywhere else and with the site-wide D11 fix that never reached this directory; fixed at the source, rebuilt, and gated, 2026-09-20 operator | D11 (2026-09-14) normalized `organise`/`organize` inconsistency across `site/**/*.html`, including the free sample chapters under `site/downloads/`, but `gate_us_spelling_consistency` only ever globbed `site/**/*.html`; it never reached `content/book/*hapter*/chapter_*_final.html`, the 50 files `ops/build_epub.py` actually reads to build the one product this business has sold a copy of, a different directory the D11 sweep never visited. Verified directly with the gate's own exact regex against all 50 real committed chapters before writing anything: 17 instances across 9 chapters (4, 6, 12, 13, 14, 15, 16, 34, 37), including inside `<img alt="">` text a screen reader would read aloud, several sitting right next to a sibling figcaption or comment that already used the correct spelling. Fixed by substituting the matched suffix only (`organise`→`organize`, `organising`→`organizing`, `organisation`→`organization`), preserving every surrounding word; rebuilt `build/6S-Success-Home-Edition.epub` via `ops/build_epub.py` (32/32 of its own checks passed) and confirmed by unzipping the built file and re-scanning every `.xhtml`: 0 British spellings, was 17. `gate_us_spelling_consistency` extended to also scan `book_chapter_files()`; `ops/tests/test_gate_book_chapters_house_style.py` extended (13 cases now, was 7) with a new planted-defect case, fail-then-pass proved both synthetically and directly against the real pre-fix committed corpus (`git stash` on the 9 chapter files alone reproduced the exact 17-instance failure by name, restored, reran clean). Full `preflight.py` (every gate passed, 22 warnings, identical baseline to the pre-change run), `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (163 documents) all clean after. No price or product touched, no site page changed (the fix is inside the book manuscript source and its compiled EPUB, not a rendered page); IndexNow not applicable. The separate, already-documented `sample-pdf-spelling` warning (page 243's SegoeUI-Semibold font subset, needs the source manuscript or better font tooling) is a different artifact and unaffected by this fix. |
|---|---|
| The standing interactive `shop.html`/`kit.html` QA handoff, named twice in `ops/NIGHTLY-LOG.md` PM check-ins ("clicking every filter and buy link, confirming disclosure order") and never actually run, closed: genuinely exercised for the first time, no live defect found, 2026-09-20 operator | Every existing shop check (`gate_prerender_shop_current`, `gate_shop_buy_claim_honest`, `gate_price_matches_its_own_link`) reads static HTML text against the catalogue; none had ever actually clicked a filter button or followed a rendered action link, the literal gap the handoff named. `kit.html` has no JS grid to drive (static retailer-search links); read it cold instead and checked all 8 Target search-term URLs decode to sensible, correctly-encoded terms (no defect, matches the already-gated disclosure-above-links and zone-count checks). Built a new headless-Chromium probe for `shop.html`, `ops/tests/test_shop_interactive.py`, driven the same iframe/dump-dom way `test_quest_flow.py` already drives the Quest: clicks all 8 real filter buttons (All + 7 categories), and for each checks the rendered tile count against a fresh, independent parse of `data.js`'s own `window.CATALOG` (not the browser's copy), that exactly one button reads `aria-pressed=true`, that every action link has a non-empty href, that every Stripe buy link is well-formed and carries the `data-sku` `measure.js` needs, that the SKU resolves to a real catalogue entry whose own `buy` field matches the rendered href, and, across all 155 buy links seen, that no two different SKUs ever share one Stripe payment link (a real, serious class of bug: Stripe payment-link line items are immutable per link, so two SKUs sharing one would either misattribute the sale or sell the wrong product). Ran clean on the real site: 155 distinct buy links, all 8 categories render the exact count the catalogue itself has for that category. Proved the check can actually fail before trusting the clean result: planted two real SKUs (`PACK-HOUSE`, `BK-EB`) sharing one Stripe URL in a copy of `data.js`, reran, watched it fail by name identifying both SKUs and the shared link, restored the file byte for byte (confirmed by `git diff` showing no change), reran clean. `gate_tests()` already globs every `ops/tests/test_*.py` file, so no separate `preflight.py` wiring was needed. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits, 219 test files), `check_urls.py` (188/188), `affiliate.py --check` (163 documents) all clean after. No price, product or page touched; this is new test coverage, not a content or code change. IndexNow not applicable. |
| `ROADMAP-2026-2029.md` section 3c's own catalogue-size headline ("The catalogue is 164 items, not 10") did not match its own listed breakdown; fixed and gated, 2026-09-20 operator | The exclusion-claim and cold-read lanes were independently reconfirmed exhausted by the preceding dozen-plus cycles (see `ops/NIGHTLY-LOG.md`), so this pass used a subagent to find a genuinely untried angle: summing a strategy document's own numbers against its own headline. 114 zone packs + 20 room packs + 15 situation kits + 6 area bundles is 155, not 164, an error present since 2026-08-26; all four components verified fresh against the live `ops/build_catalog.py` catalogue and found correct, so only the summed headline was wrong. Fixed the sentence; new `gate_roadmap_catalogue_count_current` in `preflight.py` (re-derives the four counts from the live generator and re-sums the sentence's own numbers on every run), `ops/tests/test_gate_roadmap_catalogue_count.py` (6 cases) fail-then-pass proved directly against the real pre-fix committed file. Full `preflight.py` clean before and after (every gate passed, 22 warnings). No price, product or site page touched; IndexNow not applicable. Full account in `ops/NIGHTLY-LOG.md`. |
| The paid $19 book's own manuscript source, `content/book/*hapter*/chapter_*_final.html` (the 50 files `ops/build_epub.py` reads to write the shipped EPUB), had zero automated protection against an em/en dash, the retired "Set in Order" name for step 2, a fabricated statistic, or an uncited appeal to research, unlike every other customer-facing surface on the site; closed, 2026-09-19 operator | Preflight clean before (every gate passed, 22 warnings, the standing sandbox set), 8 GitHub issues unchanged (`decision`/`blocked-on-art`), no mail credential, `BACKLOG-2026-09-07.md` sections 2-6 again all done or Phil-gated, and the 114-zone narrative cold-read manifest (`ops/zone-narrative-read.json`) confirmed complete (114 of 114), closing that lane for good. Looked for a genuinely new angle rather than defaulting to an already-exhausted one, per the last several PM check-ins' own instruction. Found it in `gate_no_stray_dashes`'s own docstring, which explains it deliberately excludes `content/**/*.html` and `content/**/*.md` as "pre-existing source archives, drafts and review notes... never going to ship." True for almost all of `content/`, but checked directly and found false for one specific subtree: `ops/build_epub.py`'s own glob (`BOOK.glob("*hapter*/chapter_*_final.html")`) reads exactly those 50 files to build `build/6S-Success-Home-Edition.epub`, the one product this business has sold a copy of. `gate_unsourced_stats` has the identical gap from the other side: it only scans `all_pages()` (`site/**/*.html`), which excludes `downloads/` and never reaches `content/book/` either. So the actual manuscript of the paid product had never been checked for any of these three defect classes, ever. Ran the real detection logic (STAT/STAT_BIG/CLAIMY/AUTHORITY patterns, the em/en dash count, a "set in order" scan) against all 50 real committed chapters before writing anything: 0 fabricated statistics, 0 uncited authority claims, 0 em/en dashes, and exactly one "Set in Order" occurrence (chapter 2), read in full and found to be a legitimate, transparent aside naming it as one of three translations OTHER 6S/5S books use for the step, immediately followed by the book's own figure mapping the term to "Straighten" (the name used 5 more times in that same chapter), not the book adopting the retired name as its own. So this closes a latent gap, not a live defect, the same posture as `gate_hero_fallback_current` and `gate_prerender_shop_current`. Fixed with a new `book_chapter_files()` helper in `preflight.py`; wired into `gate_no_stray_dashes` (em/en dash scan) and `gate_unsourced_stats` (stat/authority scan), and a new `gate_book_no_retired_terminology` (checks "Set in Order" against a name-keyed whitelist covering exactly the one reviewed chapter, so a *new*, unreviewed occurrence anywhere else still fails). New `ops/tests/test_gate_book_chapters_house_style.py` (10 cases) fail-then-pass proved directly, using synthetic chapter files in a temp directory with `book_chapter_files()` monkeypatched (so the real, clean, committed chapters are never at risk): a planted em dash, en dash, unreviewed "Set in Order," and unsourced statistic each fail their respective check by name; a clean synthetic chapter and the real committed chapter 2 (matched by exact path, proving the whitelist is real and not a blanket skip) both pass; the real 50-chapter corpus passes all three checks clean. Full `preflight.py` run to completion in the background (foreground exceeds this sandbox's timeout): every gate passed, 22 warnings, identical set and count to the pre-change baseline. `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (control layer clean) all clean after. No price or product touched, no site page changed (the manuscript itself needed no edit, only new coverage), no new page; IndexNow not applicable. |
| A real, live O1 (traffic) defect: `sitemap.xml`'s lastmod, the crawl-priority hint search engines use to decide what to re-fetch, had stopped moving for 24 of 188 URLs, including the home page and quest.html (the single most-engaged page on the site); fixed at the source and gated, 2026-09-19 operator | GitHub (8 issues, all decision/blocked-on-art) and BACKLOG-2026-H2.md's own epic 2-6 rows again all done or Phil-gated, no mail credential, preflight clean twice (before and after); checked whether this sandbox now had real network egress via the agent proxy first (a genuinely new angle, since prior cycles only ever asserted "no egress"), confirmed it does not (403 on every non-allowlisted host, including 6s-success.com and api.stripe.com), so that closes as a non-finding rather than reopening the "verify against production" question. `ops/build_seo.py`'s `build_sitemap()` kept an existing URL's recorded lastmod unless the row was deleted by hand, a decision documented in its own comment after an earlier attempt to derive it from git self-referentially failed; nobody had deleted a row since 2026-08-24, so the "bump it deliberately" mechanism was never actually exercised. Verified directly rather than assumed: for each of the 188 sitemap URLs, diffed the real committed file against its state at the commit immediately before the oldest real commit after its recorded lastmod. 24 URLs carry a genuine content change in that window (not just a shared-asset fingerprint bump, confirmed by checking `corporate.html`, whose only post-lastmod commit turned out to be fingerprint-only and correctly stays unflagged); `deck/entryway-print-and-play.html` was 15 days stale against a real "81 of 88 illustrated" count edit, `quest.html` 15 days stale against five real behavioural fixes. Fixed with `_content_hash()` in `build_seo.py`, hashing each page with the `ops/fingerprint_assets.py` `?v=` cache-bust query string stripped (so a shared CSS/JS edit does not read as all 191 pages changing, the exact false positive the earlier, reverted attempt hit), persisted per URL in the new `ops/sitemap-content-hashes.json`; `build_sitemap()` now moves lastmod only when a URL's hash has moved since the file last recorded it. Proved both directions directly against the real files, not assumed: mutated a real page's content with the hash file NOT regenerated and confirmed the next build would move its lastmod; mutated only the fingerprint query string across three real pages and confirmed no lastmod moved; confirmed two consecutive real runs of `ops/build_seo.py` are byte-identical (no self-referential churn). New `gate_sitemap_lastmod_current` in `preflight.py`, re-deriving each page's hash from the real committed file independent of `build_seo.py`'s own write path; `ops/tests/test_gate_sitemap_lastmod_current.py` (4 cases) fail-then-pass proved directly against the real committed `site/index.html` (mutated, watched the gate fail naming the URL, restored byte for byte, reran clean). `build_seo.py` added to `GENERATOR_PROTECTED_ELSEWHERE`'s existing entry. Regenerating produced exactly the 24-row lastmod change and the new hash file, nothing else; `check_urls.py` (188/188), `audit_pages.py` (191/0), `fix_dashes.py --check` (0/0), and a full `preflight.py` fast run (every gate passed, 22 warnings, matching the pre-change baseline) all clean after; mobile `npm test` unaffected (unchanged files). No price or product touched, no new page; IndexNow not applicable (existing pages, not new URLs). |
| `ops/import_chapter_svgs.py`'s six wired book-chapter figures had no gate protecting them against going stale if their own chapter source were hand-edited after the import ran; closed, 2026-09-19 operator | Every unblocked row in this backlog was again done or Phil-gated, 8 GitHub issues unchanged (`decision`/`blocked-on-art`, confirmed live via the GitHub API), 0 open PRs, CI green on the last 3 runs of both workflows, no mail credential, so this pass continued the standing low-mention `ops/*.py` cold-read lane the prior cycle named by name: `import_chapter_svgs.py` (18 mentions, the lowest of the three named candidates), `linkedin_posts.py` (25) and `social_drafts.py` (20). The latter two read clean: `linkedin_posts.py`'s docstring claim that "every post is built on a `the_call` entry from content.json" checked directly against the real corpus rather than assumed, true (posts 3, 4, 7 and 9 all trace to real, specific `the_call` text: the entryway's six sheets of paper, the coat-rail sunk cost, the doormat test, the under-sink P-trap); `social_drafts.py` and its `corpus_posts.py` rotation/word-bound/character-cap logic all traced through and found correct, including the 2026-09-12 "remaining count" fix still holding. `import_chapter_svgs.py` wires six hand-authored SVG diagrams out of book chapters 31-39 onto their zone pages, once each, by design: `wire()` skips a figure the moment its id already exists on the page, so rerunning the importer is safe but also means nothing would ever notice if a chapter's own SVG were hand-edited afterward, the exact "source corrected, artifact never re-derived" shape this repository's own gates keep closing elsewhere (hero-fallback, the Standards Pack, the pre-rendered shop grid, the KDP cover). Checked directly before writing anything: re-extracted and re-transformed all six figures fresh from the real, committed chapter HTML and diffed them against what each real, committed zone page actually carries, byte for byte identical on all six, so this closes a latent gap, not a live one, the same posture as `gate_hero_fallback_current`. Refactored the shared transform out of `wire()` into a new `render_figure()` so the importer and the gate cannot silently disagree about what "current" means, confirmed behaviour-preserving (rerun produces zero site diff). New `gate_chapter_svgs_current` in `preflight.py`, registered in `GENERATOR_PROTECTED_ELSEWHERE`; `ops/tests/test_gate_chapter_svgs_current.py` (5 cases) fail-then-pass proved directly against the real committed file (planted a stale figure block on a real zone page, watched the gate fail by name citing the figure id, restored byte for byte, reran clean). Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits, identical count and set to the pre-change baseline), `check_urls.py` (188/188), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after; the full `ops/tests/test_*.py` suite already reran clean inside preflight's own `gate_tests`. No price or product touched, no site page changed; IndexNow not applicable. |
| A second real, live "copy vs control" defect on the Home Quest, found continuing the same interactive-QA method in the same cycle: "Work a room" and "One S across a room" silently ran a whole-house draw instead of refusing when no room was chosen, despite both captioned "Uses the room chosen above"; fixed and gated, 2026-09-19 operator | `begin()` used the same unguarded room fallback for modes "room"/"spass" as mode "draw" uses on purpose; confirmed live, tapping "Start the pass" with no room chosen opened a card reading "1 of 114" (the whole house), not a single room's. Fixed with one guard in `begin()` before `build()` runs. New `ops/tests/test_quest_mode_requires_room.py`, fail-then-pass proved. Full account in `ops/NIGHTLY-LOG.md`. |
| A real, live broken Back button on the Home Quest's map and Keep screens for any first-time visitor, found by the first genuinely untried method (driving the live app interactively in a headless browser) after several cycles' text-based cold-reads found nothing; fixed and gated, 2026-09-19 operator | `renderStart()`, the handler wired to `#m-back`/`#k-back`/`#f-again`, returned early from `applyFirstRunGate()` before ever calling `show("start")` whenever the visitor was still "first run" (0 cards done, 0 zones held); a visitor reaching the map or Keep screen via "or pick a different room" (which reveals those buttons without changing either count) and then tapping Back saw nothing happen, confirmed live by reading the real `hidden` attributes on `#view-start`/`#view-map` before and after the tap. Fixed by moving `show("start")` before the gate's early return. New `ops/tests/test_quest_back_button_first_run.py`, fail-then-pass proved against the real committed file; all 6 existing Quest browser tests rerun clean. No new `preflight.py` gate needed (`gate_tests()` already runs every test file). Full account in `ops/NIGHTLY-LOG.md`. |
|---|---|
| The handed-off narrative/tone cold-read of the three remaining generated commerce pages (`kitchen-deck.html`, `deck-gallery.html`, `kit.html`) closed, no new defect, 2026-09-19 operator | Named across the last three PM check-ins as the one genuinely untried lane once every other named lane (zone pages 114/114, room pages, articles, hand-authored trust pages, `.github/workflows/*.yml`, `ops/*.py` by mention count) was confirmed exhausted; `shop.html` was already read for tone 2026-09-14, these three were not. Read all three end to end: mechanically 0 hits for the corpus-wide forbidden-phrase list and 0 em/en dashes across all three files; `kit.html`'s eight zone-count fractions and its "these links pay us nothing, no affiliate programme has approved us" claim checked against `ops/affiliate-accounts.json` and GOALS.md O4, true; `deck-gallery.html`'s every per-family drawn count verified by counting the actual card ids listed under each heading against the stated fraction, all matched, both whole-deck totals (89 including the Room divider, 88 the catalogue's own count) summed correctly; `kitchen-deck.html` read in full including all twelve root-cause cards and all six events, the deck's own arithmetic checked (1+7+21+12+18+7+6=72, and KA-015's "makes the other seventy one hold" against a 72-card deck), every "Draw one of these"/"Next:" cross-reference resolved to a real card title, and the two chemical/food-safety claims (bleach plus an acidic or ammonia cleaner releasing gas; raw meat drip contaminating ready-to-eat food; the 5C/41F and -18C/0F fridge guidance) all checked as real, correctly stated facts. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits) clean before and after; no file changed by this pass. No price or product touched, no page changed; IndexNow not applicable. Full account in `ops/NIGHTLY-LOG.md`. |
| A real, live object-URL leak in the Home Quest's Keep screen (the before/after photo record), found and fixed, 2026-09-19 operator | Every unblocked backlog row again done or Phil-gated, narrative cold-read lane closed 114/114 last cycle and its own handoff named the standing fallback lanes as exhausted; ranked `site/assets/js/*.js` by mention count anyway and cold-read the lowest, `photos.js`. `site/assets/js/quest.js`'s `renderKeep()` has five real call sites; only 3 (reset-zone, upload, delete) called the file's own documented `releaseUrls()` invariant before repainting. The other 2, the `#go-keep` nav button (the ordinary, everyday way anyone opens Keep) and the restore-backup flow, never did, so every normal visit leaked one blob URL per photograph shown, for the tab's life, on the one feature whose value depends on a long session staying usable. Fixed by moving the release inside `renderKeep()` itself so no call site can omit it. Verified two ways: `ops/tests/test_quest_keep_url_leak.py` drives the real `#go-keep` button in a real headless browser (`window.QuestPhotos` stubbed because IndexedDB itself does not complete under this sandbox's headless Chromium against `file://`, confirmed by a standalone probe first; `renderKeep()`/`paintShots()`/`releaseUrls()` and the real button all run unmodified), and a new `gate_quest_keep_releases_urls_first` in `preflight.py` re-derives the same invariant from source with no browser at all. Both fail-then-pass proved directly against the real committed file (`git stash`/git history reproduce the leak by name, restored and clean). `ops/fingerprint_assets.py` rerun for the new hash. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `ops/tests/test_quest_flow.py` (no regression) all clean. No price or product touched, no new page; IndexNow not applicable. Full account in `ops/NIGHTLY-LOG.md`. |
| The manifest-tracked narrative cold-read of `site/zones/*.html` closed, 114 of 114; a real corpus-wide grammar defect found and fixed in the process, 2026-09-19 operator | Every unblocked backlog row again done or Phil-gated (8 GitHub issues unchanged, CI green, no mail credential); a subagent independently re-verified all five unblocked O1 distribution lanes (SEO, internal linking, structured data, page speed, Pinterest/Instagram crops) complete today. Continued the standing handoff, the last 65 of 114 unread zone pages. A JSON-based cross-check script (hazard-list/FAQ/JSON-LD verbatim agreement, no duplicate hazard category) found the 65 mechanically clean, but a corpus-wide scan turned up a real live defect the check wasn't built to catch: 29 of 114 zones (content.json's own Shine-pass `surface` label authored lowercase) shipped a visible FAQ answer starting mid-sentence right after a question mark ("the shelf boards, empty. Take everything...") and the same lowercase JSON-LD HowToStep name a search engine can surface directly. Fixed with a shared `_cap()` helper in `ops/build_zone_pages.py` (one of the three readers of this field already capitalised it correctly; now all three share the logic); regenerated 33 pages, capitalisation only. New `gate_zone_shine_step_capitalised` in `preflight.py`, `ops/tests/test_gate_zone_shine_step_capitalised.py` (6 cases) fail-then-pass proved directly against the real committed file. Also did full narrative reads (not just the mechanical check) on the batch's highest-stakes safety pages (both Workshop zones, two Garage chemical/power-tool zones, two Patio/Deck rail-and-electrical zones, the Nursery diaper-storage zone): all hazard copy specific and plausible, no fabricated claims. `ops/zone-narrative-read.json` now shows 114 of 114 read, closing the lane several prior cycles flagged as approaching completion. Full `preflight.py` clean (every gate passed, 23 warnings, all previously diagnosed sandbox limits; two gates failed transiently mid-cycle on a concurrent-run `_visual_probe.html` artifact, re-verified clean independently and on a full fresh rerun), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans) all clean after. No price or product touched, no new page (33 existing pages corrected); IndexNow has all 188 sitemap URLs already submitted (no new URLs, existing pages edited). Full account in `ops/NIGHTLY-LOG.md`. |
| Narrative cold-read of `site/zones/*.html` continued, 15 more pages (one zone each from dining room, entryway, family room, garage, guest bathroom, guest bedroom, hall closet, home office, kids bedroom, kitchen, laundry room, living room, mudroom, nursery, pantry) still outside the manifest's tracked coverage, 2026-09-19 operator | Re-checked before continuing this lane rather than defaulting to it: unshallowed and fast-forwarded cleanly onto `origin/main`, read `GOALS.md`, `BACKLOG-2026-09-07.md`, `BACKLOG-2026-H2.md`'s live process rules and `CLAUDE.md`. `preflight.py` clean first (every gate passed, 22 warnings, all previously diagnosed sandbox limits). Checked GitHub live via the API (a subagent call, since this session's own CI/issue tools were still loading): 8 open issues, all `decision`/`blocked-on-art`, none pickable; CI green on both `checks.yml` and `publish-image.yml`'s last two runs. `PYTHONIOENCODING=utf-8 python ops/inbox_agent.py --apply`: no mail credential, UNCHECKED as every prior cycle. Nothing in epics 1-5 (measurement, broken/dishonest, traffic, conversion, product) was open and unblocked, so continued the standing epic-6 handoff: 95 of 114 zone pages still outside `ops/zone-narrative-read.json`'s tracked coverage. Picked one still-unread zone from each of 15 rooms (most rooms already had a different zone read in an earlier pass; this batch picks the next unread page within them, not a fresh room), read each cold: full JSON-LD (HowTo + FAQPage), visible copy, the "Check these before you start" hazard block, the kit disclosure sentence, and the "Questions people ask" FAQ block, cross-checked against each other rather than trusted individually. Also ran a fresh mechanical scan of the batch for the corpus-wide forbidden-phrase list ("set in order", "guarantee", "clinically", "testimonial", "as seen on", "studies show", "proven to", "scientifically", star-rating language): 0 hits. **Found no defect.** All 15 pages: hazard-list sentences match their FAQ and JSON-LD entries verbatim, no two hazards share a category, the kit-disclosure grammar fix (`gate_zone_kit_disclosure_grammar`, 2026-09-19 earlier cycle) holds on every plural noun in the batch ("cleaning supplies", "baking supplies", "desk drawers"), pricing bands ($4/zone, $19/pack, $250/consult) consistent, safety copy specific and non-generic (bleach/ammonia gas, lead glaze, button batteries, allergen cross-contact), cross-room "same zone in another room" links resolve correctly where present (hall closet, guest bedroom, mudroom). Updated `ops/zone-narrative-read.json` with real page-level entries and a note per page (34 of 114 now individually tracked, up from 19). Full `preflight.py` re-run in the background after (foreground hit this session's own timeout, the same transient shape prior cycles document); every gate passed, matching the pre-change baseline. No price, product or page touched (only the tracking manifest and the command deck); IndexNow not applicable. Marginal yield of this lane continues to fall (five consecutive batches now clean), worth a fresh look if the next pass also finds nothing. Full account in `ops/NIGHTLY-LOG.md`. |
| Narrative cold-read of `site/zones/*.html` continued at page granularity (not room granularity), 15 pages across 15 previously-untracked rooms, plus an independent mechanical scan of all 114 pages, 2026-09-19 operator | Before continuing this lane, re-checked whether GOALS.md's own "distribution beats production" rule pointed somewhere else first: ran `ops/link_graph_report.py` fresh (0 orphans across 114 zone, 20 room and 29 article pages), confirmed `OWNER-ACTIONS.md`'s three top actions and the built Pinterest/Instagram assets are all genuinely Phil-gated, and confirmed the 8 open GitHub issues are unchanged (`decision`/`blocked-on-art`). Nothing in epics 1-5 was open and unblocked, so continued this lane. Read 15 zone pages cold, one per room across every room not yet tracked in `ops/zone-narrative-read.json` (dining room, entryway, family room, garage, guest bedroom, hall closet, home office, kids bedroom, kitchen, laundry room, living room, mudroom, pantry, patio or deck, workshop), full JSON-LD, visible copy, hazard list and kit disclosure each. Also ran a fresh mechanical scan across all 114 committed pages, independent of any existing gate: a forbidden-phrase check ("set in order", "guarantee", "clinically", "testimonial", "as seen on", "studies show", "proven to", "scientifically", star-rating language) and a duplicate/truncated FAQPage-answer check re-derived from the real shipped JSON-LD. No defect found either way: tone consistent, hazard sentences match their visible list and FAQ entry on all 15, no two hazards share a category, pricing consistent, 0 forbidden phrases and 0 duplicate/truncated FAQ entries corpus-wide. Updated the manifest with real page-level entries for these 15 (19 of 114 now individually tracked, up from 4); left its own honest refusal to backfill the pre-manifest room-level estimate below untouched, having checked the log's newest-first ordering confirms that estimate genuinely predates the manifest rather than being a same-day gap. Full `preflight.py` clean before and after (every gate passed, 22 warnings, all previously diagnosed sandbox limits). No price or product touched, no page changed; IndexNow not applicable. Full account in `ops/NIGHTLY-LOG.md`. |
| A real, live, corpus-wide grammar defect (subject-verb disagreement in the optional-kit disclosure, e.g. "Only if your towels has one") found by the narrative cold-read on its first page this cycle; fixed and gated across all 114 zone pages, plus a coverage manifest for the cold-read lane itself, 2026-09-19 operator | `ops/zone_supplies.py`'s `render()`/`render_storage()` built "Only if your {noun} has one" / "Not every {noun} needs these" from the zone's own search-term noun with no check that the noun is grammatically singular; many real nouns are not ("towels", "bed and linens", "dresser drawers", "cleaning supplies", "shoes and boots", "coats and outerwear", "baby clothes"). Verified live on `guest-bathroom-the-guest-linen-zone` before fixing, then scanned all 114 committed pages: every zone with a plural optional-kit noun carried it. Fixed by rewording so the noun is never the subject of a verb ("Only if it applies to the {noun}" / "Not every home needs these for the {noun}"), correct for any noun regardless of number; regenerated all 114 zone pages. New `ops/tests/test_zone_supplies_grammar.py` and `gate_zone_kit_disclosure_grammar` in `preflight.py`, both fail-then-pass proved directly against the real committed file; the gate's own first-draft regex false-positived on the fixed wording itself ("Not every home needs these" contains "needs these"), caught before shipping and kept as a documented case in `ops/tests/test_gate_zone_kit_disclosure_grammar.py`. Also built `ops/zone-narrative-read.json`, a 114-page manifest for the standing cold-read lane, because at least two prior log entries recorded not knowing which specific pages had been read, only which rooms were touched in passing; seeded honestly with no page marked read unless read under the manifest. Read and cleared 4 pages this cycle (guest bathroom x2, nursery, primary bathroom), all clean besides the grammar fix. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no new page; IndexNow attempted and correctly deferred to the hourly workflow (no egress from this sandbox). Full account in `ops/NIGHTLY-LOG.md`. |
| Narrative cold-read of `site/zones/*.html` continued, 10 more pages across 10 previously-unsampled rooms, 2026-09-19 operator | Dining room, family room, garage, guest bedroom, hall closet, home office, kids bedroom, laundry room, living room, mudroom, read in full (JSON-LD, visible copy, hazards, FAQ, kit list, pricing band) against tone and contradicted-promise defects, the same lane the row below closed one instance of. No defect found: tone consistent, no "Set in Order," no fabricated claims, hazard sentences match across all three surfaces, no two hazards share a category, pricing consistent. Roughly half of the 114 zone pages (49 of 114) have now had this read; the rest are the next unattempted lane. Full account in `ops/NIGHTLY-LOG.md`. |
| A real, live duplicate-content defect on 6 of 114 zone pages, found by the first genuine narrative cold-read of the pages (not a mechanical check); fixed and gated, 2026-09-19 operator | Every unblocked backlog row was again done or Phil-gated, 8 GitHub issues unchanged, no mail credential, so this pass followed the standing handoff from the last several cycles: a narrative-level read of `site/zones/*.html` for tone and contradicted promises, not the mechanical checks already run repeatedly. A 39-page sample across all 20 rooms found no tone or logic defects, but did find a real structural bug: `ops/hazard_icons.py` deliberately collapses 251+ hazard entries onto five drawn icon categories (`Burn or fire`, `Fall, cut, or crush`, etc.), and `ops/build_zone_pages.py` used that same category string, unmodified, as both the bold heading in the visible hazard list and the literal FAQ question text. On 6 zones (kitchen cooking, kitchen lower cabinets, primary bedroom dresser top, guest bathroom toilet area, both patio/deck zones) two genuinely distinct hazards share a category, e.g. a kitchen's unattended-oil fire risk and its gas-burner fire risk are both "Burn or fire": verified live, this rendered the identical bold heading twice in the hazard list, the identical question asked twice with two different answers in the visible FAQ, and the identical `"name"` twice in the FAQPage JSON-LD, distinguishable only by reading the fine print underneath. Safety is the fourth S (D-014) and this sits in the one block that exists specifically to be read before starting. Fixed with a new `_grouped_watch_for()` in `build_zone_pages.py`, merging same-category entries within one zone into a single item with both sentences joined, used by both the hazard-list render and `zone_faq()` (which already fed both the visible FAQ and the JSON-LD from one function, so one fix closes all three surfaces). Regenerating touched exactly the 6 affected pages, byte-identical everywhere else, confirmed by a full-corpus scan for repeated `<dt>` FAQ questions (0 of 114 after, was 6). New `gate_no_duplicate_hazard_labels` in `preflight.py`, re-deriving the defect independently from the shipped HTML; fail-then-pass proved directly against the real pre-fix committed file (planted it live, watched the gate fail by name citing the file and the repeated label, restored). Also found and fixed in the process: the merge correctly drops 6 rendered icons (252 raw `watch_for` entries, 246 now-distinct-per-zone categories), which `gate_hazard_icons_current`'s own 1:1 entry count assumed could never happen; corrected that gate to count distinct categories per zone instead of raw entries, so it does not now falsely fail on this correct merge. `ops/tests/test_gate_no_duplicate_hazard_labels.py` (6 cases, including a live check against all 114 real committed pages) fail-then-pass proved. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no new page; IndexNow submitted for the 6 changed pages. Mobile `npm test` not run: nothing under `mobile/` was touched.|
|---|---|
| The Home Quest's own installed-app shortcut ("Draw a card", wired in `site/manifest.webmanifest`) left a visitor who had finished the whole 684-card deck on the page's raw, un-rendered markup instead of the real start screen; fixed and proved fail-then-pass, 2026-09-18 operator | Cold-read `site/assets/js/quest.js` per the standing handoff (36 mentions, 1857 lines, next in the hand-authored JS lane after `site.js`/`measure.js`/`shop.js`/`photos.js`). The `?zone=`/`?room=` deep links already guard against `begin()` finding nothing left to draw (`if (run) { return; }`, falling through to `releaseHero()`/`renderStart()`), but `?go=draw`, the one deep link `manifest.webmanifest`'s "Draw a card" shortcut actually opens, returned unconditionally regardless of success. A returning, engaged visitor who installed the app and finished every zone in the house, then tapped their own shortcut, would land with the hero still suppressed and `#first-run` (visible by markup default, for the no-JS case) telling them to "start at the door", while `#mode-list`/`#start-head` (hidden by markup default) never appeared. Fixed to the same fall-through shape as `zone=`/`room=`. New `ops/tests/test_quest_draw_shortcut_full_completion.py` drives a real headless Chromium, seeds all 684 real card ids done (via the live `window.QUEST`, not synthetic data), reloads at `?go=draw`, and checks the real start screen renders; fail-then-pass proved directly against the real committed file (`git stash` on the source alone reproduces the exact pre-fix symptom by name, restored and reran clean). `ops/tests/test_quest_flow.py` (the existing full functional flow test) reran clean after. No generator owns `quest.js` (confirmed by grep); `gate_tests` already globs every `ops/tests/test_*.py` file, so no new `preflight.py` gate registration was needed. `ops/fingerprint_assets.py` rerun for the changed file's new hash (only `site/quest.html` and `site/sw.js` reference it). Full `preflight.py` clean after (every gate passed, 23 warnings, all previously diagnosed sandbox limits). No price or product touched, existing page's script corrected not added, IndexNow not applicable. |
| The free Micro Zone Map (`site/downloads/6S-Micro-Zone-Map.html`, shipped this morning) scrolled sideways on a phone, 758px of content against a 390px viewport; fixed and verified, 2026-09-18 operator | Found by actually running `preflight.py --deep`, which most cycles skip for cost, rather than trusting the fast gate. `ops/build_zone_map_pack.py`'s `.sheet{width:7.9in}` had no responsive override, the identical shape `ops/build_standards.py` already fixed once for the Standards Pack (its own comment documents that earlier incident); the new generator reintroduced it. Fixed with the same `@media screen and (max-width:800px)` override, print layout untouched. No new gate needed: `gate_mobile_touch_targets` already exists and caught it the moment it ran with a working browser; also installed `pypdf`/`cffi` (missing here) so `check_pack_pages.py` could confirm all three printables (Standards Pack, Kitchen deck, Micro Zone Map) still paginate correctly, 20/12/20 pages, 0 blank. `audit_visual.py --all --mobile` now reads 0 pages scrolling sideways, was 1. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, existing page corrected not added, IndexNow not applicable. Full account in `ops/NIGHTLY-LOG.md`. |
| `ops/accept_image.py`'s own negative-clause extractor could produce a checklist item naming no object at all (a bare pronoun or comparative), which the accept test scores as a HARD FAIL when unanswered; fixed and gated, 2026-09-18 operator | Full account in `ops/NIGHTLY-LOG.md`, same date. Ran `_negative_clauses()` against all 228 real `done_looks_like`/`leave_behind.standard` strings in `content.json`: six zones' own wording ("a seat with nothing on it at all", "nothing beside it", "smelling of nothing at all", a console holding "nothing that was there yesterday", "no higher") produced a `contradicts` item asking a vision model "Is on it at all visible in this image?" or equivalent, a question with no coherent answer sitting in a hero photo's accept/reject path, one step past the sibling bug `gate_image_prompt_negations_handled` already caught in `ops/image_local.py` earlier the same day. Fixed with `_FILLER_EDGE`/`_NO_OBJECT_LEFT`, preserving real content that filler was obscuring ("nothing at all sitting on either lid" now correctly keeps "sitting on either lid" instead of losing it). 6 new cases in `ops/tests/test_accept_image.py` (13 total) and a new `gate_accept_image_contradicts_name_an_object` in `preflight.py`, both fail-then-pass proved directly against the real committed file. `check_all()` (89 card + 114 zone checklists, 0 errors) and `self_test()` (4/4) both clean after. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price or product touched, no site page changed (internal image-acceptance tooling, not a rendered page); IndexNow not applicable. |
| `ops/sync_page_links.py` could misreport a genuinely live payment link as an unresolvable dead orphan, and `ops/image_local.py`'s negation splitter missed a real, shipped mid-sentence "no", the exact failure class its own docstring names, on card EP-001; both fixed and gated, 2026-09-18 operator | Continued the standing tied-mention `ops/*.py` cold-read lane the prior PM check-in named (`image_local.py`, `send_brief.py`, `sync_page_links.py`); `send_brief.py` read clean. `sync_page_links.py`'s `active` set only gained a url inside the `if s:` (sku) branch, so a genuinely active payment link with no `sku` metadata would be classified `dead` and printed as an unresolvable orphan; `--apply` never touches an orphan so no live link was ever rewritten, but the `--check` report could raise a false alarm on a working link. Extracted the classification into a testable `links_from_stripe()` with the branches independent, 5 new cases in `ops/tests/test_sync_page_links.py`, fail-then-pass proved (the pre-fix file has no such function, `AttributeError` before, clean after). Then ran `image_local.split_negations()` against every real subject the two local generators would send to the model today (114 zone `done_looks_like` subjects, 89 card overrides/callouts), not synthetic cases: `ops/card-subjects.json`'s own shipped EP-001 override, "an empty console table with no keys on it, a jacket thrown over a chair", puts "no" mid-clause, not at the clause's start, so the matcher (anchored `^(no\|without)\b`) left "no keys" in the positive prompt, the identical failure the file's own docstring already paid to learn twice (a nursery crib, a kitchen counter), both at a clause's start; a diffusion model draws toward "keys" regardless of "no" in front of it, which draws the opposite of a PROBLEM card about nobody giving keys a home. Fixed by also matching a mid-clause `with no X`, stripping a trailing pronoun tail (`on it`/`on them`); verified clean against the full real corpus after (0 of 203 real subjects carry a bare "no"/"without" post-split, was 1). 7 new cases in `ops/tests/test_image_local.py`, fail-then-pass proved directly against the real committed file. New `gate_image_prompt_negations_handled` in `preflight.py`, re-running `split_negations()` against every real subject on every future cycle (no GPU needed, both generator modules import clean without torch), so a future hand-authored override with the same shape cannot ship silently again; `ops/tests/test_gate_image_prompt_negations_handled.py` (4 cases) fail-then-pass proved, including against the real live corpus pre-fix. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits, 194 test files), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed (both fixes are inside GPU-only local generators, neither image has been regenerated by this pass), IndexNow not applicable. |
|---|---|
| `ops/send_questions.py`'s BLOCKING list, the owner-facing email that claims "This is only the list that cannot" be done without Phil, had gone stale against OWNER-ACTIONS.md's own ranking; fixed and gated, 2026-09-18 operator | 8 GitHub issues unchanged (all decision/blocked-on-art), no mail credential, so this pass continued the standing tied-15-mention `ops/*.py` cold-read lane the last two log entries named (`image_local.py`, `send_brief.py`, `send_questions.py`, `sync_page_links.py`, `video_srt.py`, `wire_nav.py`). `send_brief.py`, `video_srt.py`, `wire_nav.py` and `sync_page_links.py` all read and ran clean (`wire_nav.py`'s standalone run correctly stripped `aria-current` from 5 pages since nothing chains `wire_aria_current.py` after it, but the change was never committed, and `gate_nav_current` already catches exactly that shape if it ever ships uncorrected, so no gate was needed there). `send_questions.py` is the real find: OWNER-ACTIONS.md's own "Start here: 20 minutes, in this order" table ranks Google Search Console, authorising YouTube uploads and pasting Stripe's business description as the three highest-value single actions on the whole page, and separately calls YouTube "the biggest single lever on the business right now." `send_questions.py`'s BLOCKING list carried Search Console but had never been told about the other two, the same "source corrected, artifact never re-derived" class this backlog's own closing section names as dominant, this time in a real email to Phil rather than a page. Fixed by adding both, in OWNER-ACTIONS.md's own current words (14 videos publish now, 88 more re-render in the background, no further action needed once that finishes); also fixed the subject line, which hardcoded "2 things... 3 decisions" instead of counting `len(BLOCKING)`/`len(DECISIONS)`, the identical staleness risk one level up. New `gate_send_questions_covers_top_owner_actions` in `preflight.py`, re-deriving the ranked "Do" column fresh from the real committed `OWNER-ACTIONS.md` on every run and checking a capitalised keyword from each row (e.g. "YouTube", "Stripe") against the real `BLOCKING` list text, so a future re-ranking cannot go silently unmentioned again; `ops/tests/test_gate_send_questions_covers_top_owner_actions.py` (5 cases) and two new cases in the existing `ops/tests/test_send_questions.py` (10 total) both fail-then-pass proved directly against the real pre-fix committed file (`git stash` reproduced both omissions by name, restored and reran clean). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable: this is a manually-sent owner email, not a published page. |
|---|---|
| `ops/stripe_setup.py`'s own `--plan`/`--apply` output told an operator the book and the Micro Zone Manual were still blocked on front matter, issue #3, months after that issue closed and both started selling live; fixed and gated, 2026-09-18 operator | 8 GitHub issues unchanged (decision/blocked-on-art), no mail credential, so this pass continued the prior PM check-in's own named handoff, the next ungated low-mention `ops/*.py` cold-read tier (`import_room_images.py`, `mailer.py`, `stripe_setup.py`, `wire_breadcrumbs.py`, 12 mentions each). `mailer.py` and `wire_breadcrumbs.py` read clean. `stripe_setup.py`'s "Not created, deliberately" block unconditionally printed "Book and manual        blocked on front matter, issue #3" on every run. Checked directly rather than trusted: issue #3 closed 2026-08-25 (read the issue itself), `ops/front-matter.json` carries zero unanswered fields today, and both products have sold live via Stripe Payment Links since 2026-08-21, created through a separate catalogue-driven script, `ops/stripe_catalog.py`, which already re-derives deliverability from the real front-matter file on every run rather than a hardcoded string. `STRIPE.md`'s equivalent table was already corrected for this exact drift on 2026-09-12; this script's own stdout was the one place nobody had told, the same "source corrected, artifact never re-derived" shape this backlog's own closing section names as dominant. Also corrected the file's opening docstring, which still described the whole catalogue as "41 items; three deliverable today," a stale count from before the catalogue grew to its real 159. New `gate_no_stale_stripe_setup_book_blocker` in `preflight.py`, joining the file's existing `gate_no_stale_*` family (`gate_no_stale_listmonk_blocker`, `gate_no_stale_affiliate_blocker`, `gate_no_stale_stripe_website_blocker`, and others); `ops/tests/test_gate_no_stale_stripe_setup_book_blocker.py` (5 cases) fail-then-pass proved directly against the real committed file (`git stash` on the source alone reproduced the pre-fix failure by name, restored and reran clean). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
|---|---|
| `ops/hero-fallback.json`, the record `build_quest.py` treats as the PUBLISHED set of zone hero photos and the one `wire_zone_heroes.fallback_wire()` restores from in every environment without `build/heroes/zones/` (every CI checkout), had no gate protecting it from going stale again the way it already had once; closed, 2026-09-18 operator | 8 GitHub issues unchanged (decision/blocked-on-art), no mail credential, so this pass cold-read the lowest-mention `ops/*.py` file in `ops/NIGHTLY-LOG.md` (`refresh_hero_fallback.py`, 2 mentions), per step 5d. Its own docstring already names a real 2026-09-17 incident: five newly approved zone heroes went live on their pages while four stayed missing from this hand-maintained file, so the Home Quest app kept showing no picture for zones whose photo was already live on the web page, a real customer-visible inconsistency between two channels showing the same content. `refresh_hero_fallback.py --apply` fixes drift once run, but nothing re-asserted it had been, and its filename does not match `gate_every_generator_has_a_protection_plan`'s own `build_*.py` glob, so it had no gate at all, the exact blind spot `prerender_shop.py` had before it got one. Checked directly first: `python ops/refresh_hero_fallback.py --check` reports 0 added/dropped/changed against the real committed file today, so this closes a latent gap, not a live defect. New `gate_hero_fallback_current` in `preflight.py` (pure logic in `check_hero_fallback_current`, regenerates the record from the real `site/zones/*.html` pages and diffs it against the committed `ops/hero-fallback.json`), registered in `GENERATOR_PROTECTED_ELSEWHERE` for documentation even though the meta-gate's glob would not reach it by name either way, same honest posture `prerender_shop.py`'s entry already established. `ops/tests/test_gate_hero_fallback_current.py` (7 cases) fail-then-pass proved directly against the real committed file: planted the exact 2026-09-17 regression shape (a wired page's entry dropped), watched the gate fail by name citing the file and the fix, restored the file byte for byte, reran clean. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), all 184 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
|---|---|
| `site/kit.html`'s "N of 114 zones ask for this" line, for all eight kit items, was reading a frozen catalogue column that two other files had already stopped trusting for the same reason; fixed at the source and gated, 2026-09-17 operator | Every unblocked backlog row was again done or Phil-gated, 8 GitHub issues unchanged (decision/blocked-on-art), no mail credential, so this pass continued the PM check-in's own hand-authored `site/*.html` handoff (`404.html`, `corporate.html`, both clean) into the next tier, `kit.html`. Its "N of 114 zones ask for this" claim for each of the eight items comes from `ops/affiliate-catalogue.csv`'s own `_zone_count` column, a value frozen at whatever catalog state last hand-edited that CSV. `ops/zone_supplies.py` already computes this figure fresh instead ("that column lives in a file another agent is editing this cycle and a stale count would silently reorder every page") and `ops/build_manual_print.py` already found the same column wrong for ten records after cleaning tools were added later; `ops/build_kit_page.py` was the one reader of it nobody had checked. Checked directly before fixing: all eight counts on the live committed page currently match a fresh recomputation from `content/manual/source/zone_products.json`, so this closes a latent gap, not a live defect. Fixed `build_kit_page.py`'s `kit()` to recompute from the real zone map via `ops/zone_supplies._rarity()`, the same source and method the other two files already use, rather than trust the CSV column; regenerating produced a byte-identical `site/kit.html`. Proved directly, not just by construction: planted a stale `_zone_count=999` in the CSV, watched the pre-fix generator ship "999 of 114 zones ask for this" on the rendered page, confirmed the fixed generator ignores it and renders the real "109", then restored the CSV. New `gate_kit_page_zone_counts_current` in `preflight.py` (pure logic in `check_kit_page_zone_counts`, independent of both `build_kit_page.py` and `zone_supplies.py`, re-deriving rarity straight from `zone_products.json`), `ops/tests/test_gate_kit_page_zone_counts.py` (4 cases, including a live check against the real committed page) fail-then-pass proved. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no new page; IndexNow not applicable (existing page unchanged). |
|---|---|
| `ops/receive_deploy_key.py`, which installs real read-only SSH deploy keys onto this GitHub repository, had zero test coverage and could read a failed `gh api` call as "the repo has no keys yet"; fixed and gated, 2026-09-17 operator | CI run 309 confirmed green first (the prior PM check-in's handoff), then this pass cold-read the tied 16-mention tier per the standing lane. `existing()` parsed `gh api repos/.../keys` stdout without checking `proc.returncode`; a failed call and a repo with zero keys both produce empty stdout, so a broken `gh` session would have been read as "no keys installed yet," meaning `--install` could reinstall every mailbox key as new and `--check` could tell an operator a key was "not yet installed" when the honest answer was unknown, the exact "unknown read as unused" shape `CLAUDE.md` 0.4 names. Fixed: `existing()` now raises `RuntimeError` naming the real stderr (or exit code) on a non-zero returncode; `main()` catches it, prints `UNCHECKED`, and refuses to proceed. New `ops/tests/test_receive_deploy_key.py` (the file's first test, 6 cases: dedupe, a clean parse, a failure with real stderr, a failure with empty stderr naming the exit code, and `main()`'s UNCHECKED refusal), fail-then-pass proved directly (`git stash` on the source alone, `AttributeError` against the pre-fix file, restored and clean). No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file, matching the pattern `test_service_orders.py`/`test_youtube_upload.py` already established for a security-adjacent tool with zero prior coverage. Not a live incident: no evidence `gh api` has actually failed in a real run; this closes the gap before it caused a wrong action. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
|---|---|
| `site/shop.html`'s pre-rendered product grid, the only thing a JS-less crawler or search engine reads of the storefront, had no gate protecting it against going stale against the live catalogue; closed, 2026-09-17 operator | Every unblocked row in this backlog was again done or Phil-gated, 7 GitHub issues unchanged (all decision/blocked-on-art), no mail credential, so this pass cold-read `ops/prerender_shop.py` from the standing low-mention `ops/*.py` lane. It is a real generator (drives a headless browser to write a committed block into `site/shop.html`, the same shape as `build_etsy_assets.py` and `build_kdp_cover.py`, both already protected by a dedicated regenerate-and-diff gate), but it was in neither `GENERATOR_OWNERSHIP_CHAIN` nor `GENERATOR_PROTECTED_ELSEWHERE`, and `gate_every_generator_has_a_protection_plan`'s own glob only ever looks for `build_*.py`, so a file named `prerender_shop.py` was invisible to that meta-gate too, not merely unlisted by it. The existing `gate_shop_prerendered` only checks that a pre-rendered block exists and clears a 100-card floor, so the shop's 159 cards could go silently stale against a changed catalogue (a price, a product added or dropped) while staying above that floor forever, the exact "source corrected, artifact never re-derived" defect class this backlog's own closing section names as dominant. Checked directly before writing anything: ran `python ops/prerender_shop.py` against the real committed tree, twice back to back; both renders were byte-for-byte identical to the committed file and to each other, so this closes a loophole, not a live defect, and the comparison can safely be a raw byte diff rather than the normalized-text comparison the Etsy PDF gate needs (that generator's own metadata makes two renders of identical content byte-different; this one does not). New `gate_prerender_shop_current` in `preflight.py` (dirty-tree refusal, no-browser and timeout handled as UNCHECKED never a false pass, matching `gate_etsy_pdfs_current`'s posture), registered in `GENERATOR_PROTECTED_ELSEWHERE` for documentation even though the meta-gate's `build_*.py` glob would not reach it by name either way (noted honestly in the gate's own docstring rather than silently relied on). `ops/tests/test_gate_prerender_shop_current.py` (5 cases, using a detached git worktree sharing the real object database, the same approach `test_gate_kitchen_deck_current.py` uses, since this generator needs the real `site/shop.html`, `shop.js` and `data.js` to render anything) fail-then-pass proved directly: a genuinely current shop.html passes clean, a hand-planted stale-grid regression is caught by name, the gate restores the working tree either way, and a dirty `shop.html` already ahead of HEAD is refused rather than diffed against a moving target. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page content changed, IndexNow not applicable (no page added or rewritten). Mobile `npm test` not run: nothing under `mobile/` was touched. |
| `ops/product_links.py`'s keyword matcher could fabricate a false plural ("can" matching "canes", "tub" matching "tubes") and certify the wrong retailer product as verified; fixed before it affected a live link, 2026-09-17 operator | Cold-read per the prior PM check-in's handoff (the highest-priority untouched file at the 10-mention floor, real affiliate revenue plumbing). `matches()`'s unconditional "-es" plural rule fabricates a different real word for any keyword not ending in s/x/z/ch/sh; 4 currently-used keywords hit this (can/tub/pan/mat -> canes/tubes/panes/mates), the exact "certifies the wrong product" failure this function's own docstring already warns against. Checked `ops/product-links-evidence.json` before fixing: none of the 120 currently-published links actually depended on the false form, so this closes a loophole, not an active wrong link. Fixed to only add "-es" when English actually requires it. New `test_matcher_plural_does_not_fabricate_a_different_word` in `ops/tests/test_product_links.py` (8 assertions), fail-then-pass proved directly (4 of 8 failed against the old matcher, 0 after the fix). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed), all 168 non-slow test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
| The phone app's store icons had no protection against going stale if the site's own brand palette ever changes; closed before it caused a live defect, 2026-09-17 operator | Every unblocked row in this backlog was again done or Phil-gated, 8 GitHub issues unchanged (all decision/blocked-on-art), no mail credential, so this pass cold-read `ops/build_app_icons.py` (5 mentions in `ops/NIGHTLY-LOG.md`, the lowest of any `ops/*.py` file, continuing the standing cold-read method). `gate_store_art` already existed and checked every store icon's size and PNG colour type, but nothing re-derived the pixels from the generator: `build_app_icons.py` draws every icon from `draw`/`DEEP`/`CREAM`, imported from `ops/build_icons.py`, so a future palette change there would leave every store icon the old colour, correctly sized and typed, gate green throughout, the same "source corrected, artifact never re-derived" shape as `gate_manual_print_fonts_current` and `gate_standards_pack_current` found this week. Installed Pillow locally (absent from this sandbox, matching every prior "kdp-cover-current... Pillow is not installed here" warning) and actually regenerated all five files to check for a live defect first, not assumed: 4 of 5 came back pixel-identical; the fifth, the feature graphic (the only one with rendered text), differed in 6,743 of 512,000 pixels, all inside the text's own bounding box and shaped like anti-aliasing drift between FreeType builds, not a content change, confirmed by inspecting the actual differing pixel values (smooth gradients along glyph edges) rather than assumed from the count alone. No live defect today; the regenerated files were reverted, not committed, since shipping this environment's font rasterisation over the existing committed one would trade one artifact for an equally arbitrary other with no real improvement. Added the same mtime-style staleness check `gate_icons_current` already uses for its own generator (a pixel-diff gate here would be a false-alarm machine across environments, per that gate's own documented reasoning about non-reproducible PNG/font encoding): `gate_store_art` now warns if `ops/build_icons.py` or `ops/build_app_icons.py` was committed after the store art it draws. `ops/tests/test_gate_store_art.py` extended with 4 new cases (clean when the generator predates the art, warns when either file moved after it, silent with no git history at all), fail-then-pass proved directly: reverted the fix and watched the two staleness cases fail by name, restored and reran clean. Full `preflight.py` (every gate passed, 21 warnings, all previously diagnosed sandbox limits, two fewer than the last recorded count only because Pillow is now present in this session), all 167 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (clean) all clean after. No price or product touched, no site page changed, IndexNow not applicable. Mobile `npm test` not run: nothing under `mobile/` was touched. |
| `invest.html`, the investor pitch page, claimed a "41-item catalog and a working cart"; neither true, fixed and gated, 2026-09-16 operator | Every unblocked row in this backlog was again done or Phil-gated, so this pass continued the hand-authored `site/*.html` cold-read handoff at `invest.html`. The real digital catalog (`site/assets/js/data.js`) holds 159 items; the cart was removed entirely 2026-09-08 (A6 above), checkout is per-product Stripe links. Fixed to "a 159-item catalog and one-click Stripe checkout on every product." Checked the page's other hard numbers before stopping: 123 products/7 families/88 (72%) all-room/33 core, all verified directly against `content/manual/source/products.json` and found correct, so only the storefront line needed fixing. New `gate_invest_page_catalog_current` in `preflight.py`, re-derives all five numbers from the real source files on every run; `ops/tests/test_gate_invest_page_catalog_current.py` (6 cases) fail-then-pass proved. Full `preflight.py` (every gate passed, 24 warnings), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (clean), mobile `npm test` (5 suites) all clean after. Full account in `ops/NIGHTLY-LOG.md`. No price or product touched, no new page, IndexNow not applicable. |
| 3 of 4 British spellings in the free sample PDF fixed live, 2026-09-16 operator (later cycle) | `gate_sample_pdf_spelling` (2026-09-16, earlier cycle) found the defect and correctly discarded a same-day fix attempt that risked a wrong-font regression on 2 of 4 instances. Re-checked the font attribution directly rather than trusted the record: only page 243 is `SegoeUI-Semibold` (the genuinely blocked one); pages 228, 253 and 259 are plain `SegoeUI`, the weight already proven fixable. Fixed those three by redacting and re-inserting the whole word with the exact embedded font subset extracted from the file, matching the sampled background colour and the span's own colour/size. A single-character substitution was tried first and rendered pixel-correct, but left the PDF's own text layer with a blank where the letter should read (a pymupdf ToUnicode edge case specific to one-glyph text objects); replacing the whole word instead extracts correctly. Verified: text extraction now reads "organized"/"organization" on all three; a full pixel diff against the pre-fix file at 150dpi shows 0 changed pixels on every one of the other 489 pages and only the redrawn word's own ~1,500-2,000 of 6.31M pixels on each fixed page; page count (492), embedded JPEG validity and page 10's text all unchanged. Page 243 stays open, same reason as before. Gate docstring and its test corrected to match (1 remaining instance, not 4). Full `preflight.py` (every gate passed, 25 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price or product touched, no site page changed, IndexNow not applicable (a binary download, not an HTML page). |
| The print edition of the Micro Zone Manual was embedding font data a real font change had already made stale; regenerated and gated, 2026-09-16 operator | Every unblocked row in this backlog was again done or Phil-gated, so per step 5d this pass cold-read `ops/verify_media_delivery.py`, `ops/mailer.py`, `ops/generated_products.py`, `ops/wire_legal_strip.py`, `ops/corpus_index.py`, `ops/linkedin_posts.py`, `ops/diagnosis.py` and `ops/video.py` (all clean) before reaching `ops/build_manual_print.py` and actually running it rather than only reading it. Commit `7e7d1db7` (2026-09-15, Phil) re-cut Newsreader-400-normal and Inter-400-normal from full variable fonts down to the single weight the CSS declares, roughly halving each file; nothing reran the print builder afterward, so the committed `content/manual/print/6S-Micro-Zone-Manual-PRINT-7x10.html` (last built 2026-08-21) still embedded the old, larger font bytes for both faces as base64. No live customer was affected: MZ-MANUAL, the $29 product actually sold, is fulfilled from `content/manual/micro-zone-manual-publishable.html`, which links Google Fonts rather than embedding, and the print edition itself is not yet submitted anywhere (Amazon KDP account creation is still owner-gated, `OWNER-ACTIONS.md`/backlog section 6 item 4). Regenerated the file (`ops/build_manual_print.py`, 1,601,422 to 1,469,306 bytes, only the two `@font-face` blocks differ; two back-to-back runs in this environment are byte-identical). New `gate_manual_print_fonts_current` in `ops/preflight.py`, same regenerate-and-diff posture as `gate_kdp_cover_current`/`gate_etsy_pdfs_current` (this generator lives outside `gate_generator_ownership`'s own diff set, and the one existing check already mapped to it, `gate_front_matter_filled`, only checks the copyright page's bracketed fields, never the fonts); `ops/tests/test_gate_manual_print_fonts_current.py` (5 cases, a minimal fixture standing in for the real font-embedding contract so the real, large woff2 files and the real content.json/products.json pipeline are never touched) fail-then-pass proved, plus a direct check against the real pre-fix committed file (gate correctly refused with "already differs from HEAD" while the fix sat uncommitted, then passed clean once committed). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed), all 159 non-slow `ops/tests/test_*.py` files (the 160th, `test_generator_ownership.py`, is the known-slow worktree test unrelated to this change), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0) all clean after. No price or product touched, no site page changed, IndexNow not applicable (a print-production file, not a published page). Mobile `npm test` not run: nothing under `mobile/` was touched. |
|---|---|
| The Standards Pack a visitor actually downloads (`site/downloads/6S-Standards-Pack.html`) had no gate protecting it against going stale against `build/6S-Standards-Pack.html`; closed, 2026-09-16 operator | Every unblocked row in this backlog was again done or Phil-gated (issue #32 already opened for section 1b), so this pass cold-read a low-mention `ops/*.py` file (`build_standards.py`) per step 5d. `gate_downloads_noindex`'s own docstring already named this exact copy step as unprotected ("a human or operator has to rerun `ops/build_standards.py` and copy `build/6S-Standards-Pack.html` over it by hand, which this gate cannot do itself, only catch if skipped") but only checked the noindex/canonical tags, not the content itself; `gate_generator_ownership` covers `build/6S-Standards-Pack.html` being correctly derived from `content.json`, but nothing compared that file to the copy a real visitor's browser fetches. Checked directly: the two are currently byte-identical, so this closes a latent gap, not a live one, same posture as `gate_kdp_cover_current`/`gate_etsy_pdfs_current`. New `gate_standards_pack_current` in `ops/preflight.py` (direct content comparison, not a regenerate-and-diff, since the generator side is already covered elsewhere), `ops/tests/test_gate_standards_pack_current.py` (5 cases), fail-then-pass proved directly against the real committed file (planted a stale title in the live copy, watched the gate fail by name citing the exact remedy, restored, reran clean). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product/page touched, IndexNow not applicable. |
| The live executive dashboard's own traffic and revenue rows had silently gone to "not measured" for 9 commits, 6 hours, despite the carry-forward mechanism built specifically to prevent that; root-caused to a merge regenerating over unresolved conflict markers, recovered and fixed, 2026-09-15 operator | `ops/dashboard.py`'s `_prev = json.load(...)` sat in a bare `try/except: pass`, so when merge `d9893032` regenerated `ops/state.json` while it still held its own unresolved conflict markers, the parse failure silently produced `_prev = {}`, erasing `traffic_line_last_measured`, `affiliate_trigger_last_measured`, `revenue_last_measured` and `customers_last_measured`; `gate_dashboard_traffic_carry_forward` never caught it because it proves the pure carry function's logic, not whether the real committed file still holds anything to carry. Extracted the load into `_load_prev_state()`, now falling back to `git show HEAD:ops/state.json` instead of `{}` on a parse failure, with a loud stderr warning. Reseeded the four lost fields from the last known-good commit (`c8e3bc76`, 2026-09-14 18:32) and regenerated; the dashboard now correctly shows the real 75-visitor/196-visit traffic figure and $0/30-day revenue, honestly marked carried forward, instead of a false "not measured." New `ops/tests/test_dashboard_prev_state_fallback.py` (3 cases), fail-then-pass proved via `git stash`. Full account in `ops/NIGHTLY-LOG.md`. |
| Sustain rewritten for all 114 micro zones | median 28 to 94 words, zero slots under 25 anywhere in the corpus, 4-gram overlap measured across all 6,441 zone pairs, mean 0.0004 |
| The Quest's only button now moves the page | tap scrolled 0px before, 458px after, card lands 79px from the top |
| Cards made legible in print | smallest glyph 2.04pt to 7.00pt, body 3.12pt to 9.5pt |
| Print packs stopped wasting every second sheet | 10.5in of cards on a 10.2in page, now 10.2 on 10.4 |
| A second live checkout charging $18 for the $9.99 book | archived, 7 of 20 lifetime checkouts had been quoted it |
| An invented $8.01 discount on the bundle's checkout page | "$66" corrected to $57.99 |
| 20 fabricated statistics removed from the card deck | including one citing a field of study that does not exist |
| 1,717 retailer links published and honestly disclosed | 120 of 123 products, verified by rendering the retailer's grid |
| The free deck's shop tile was a broken image | our main lead magnet, 404 |
| Three real content fixes above (Sustain, Quest scroll, generator regen) sat published in the repository but never once shipped in the image the host pulls, for about 40 minutes | publish-image.yml had failed on two unrelated bugs already fixed in later commits that never touched site/, so the path-filtered workflow never re-ran; triggered it directly, confirmed success, new `gate_publish_image_current` catches this shape going forward |
| IndexNow submission (`ops/indexnow.py`) automated in `hourly-brief.yml`, 2026-09-08 operator | Every prior cycle in `ops/NIGHTLY-LOG.md` reported this step blocked, "no network egress from this session"; that is true of every operator sandbox but not of the GitHub Actions runner already used for the hourly brief, which holds real Stripe/SMTP credentials and reaches the internet. Added a `--changed` step there so a new or substantially rewritten page gets announced to Bing/Yandex/Seznam/Naver within the hour, without needing an operator session with egress. While testing offline, found and fixed a real bug in `--changed` itself: it recorded a URL's content hash as "announced" even when the run never reached the network (rc=2, UNCHECKED) or was rejected, because it checked the cumulative `log["submitted"]` set rather than what THIS run actually accepted. A future failed run would have silently marked real content changes as already covered and never retried them. `run()` now returns the per-call accepted set; new `ops/tests/test_indexnow_changed.py` proves an unreachable run and a rejected run both leave hashes untouched, and a genuine 200 still records them, fail-then-pass verified in an isolated sandbox. |
| The live MCP distribution channel (`6s-mcp`, deployed 2026-08-31) was serving all 114 zones stale, 2026-09-09 operator | `mcp/content.json` is a committed copy of `content/manual/source/content.json`, kept only so the Docker image is self contained. `.github/workflows/publish-mcp.yml` only triggers on a push touching `mcp/**`, and its own drift check runs only inside that same job, so this week's real content work (the Sustain rewrite, median 28 to 94 words; the new `diagnosis` root-cause blocks on the 12 pilot zones) never touched `mcp/` and never re-triggered it. Confirmed by a structural diff, not assumed from the stale trigger alone: all 114 of 114 zones differed, every one missing `diagnosis` entirely and several still carrying the old shallow Sustain text the site itself superseded days ago. Watchtower is enabled on the deployed container (`mcp/docker-compose.yml`), so every stale query this channel has answered since 2026-08-31 was answered from the pre-rewrite corpus. Re-copied the manual into `mcp/content.json` (verified byte-identical after; `mcp/server.py` never reads the new `diagnosis` key, so it is inert to the server, and every key the server does read is still present for all 114 zones, checked directly). Fixed the trigger to also fire on `content/manual/source/content.json`'s own path. Because a workflow trigger is invisible to a local run, added `gate_mcp_corpus_current` to `preflight.py` so this specific drift is re-asserted on every cycle regardless of what triggered CI; `ops/tests/test_gate_mcp_corpus_current.py` (4 cases) proves it fails naming the exact drift and passes clean on the real files, fail-then-pass verified. |
| Pinterest and Instagram caption/board/tag text written for all 114 zone cards, ahead of the account, 2026-09-09 operator | OWNER-ACTIONS.md item 16 (2026-09-08) got Phil a single step for account creation and posting, but named the caption/board/tag list itself as the next thing to build once the accounts existed. Nothing about writing that text needs the account to exist first, and GOALS.md's own decision rule 1 is "distribution beats production": posting 228 images by hand is still a research-and-writing job per image without it. New `ops/build_social_captions.py` writes one JSON per zone under `build/social/captions/` (114 files plus a `boards.json` grouping into one Pinterest board per room), reusing `build_youtube_metadata.py`'s own `title_for()` and `zone_page_slug()` rather than re-deriving them, so this channel's title and link inherit the fix that already corrected a 13-of-114 wrong-URL bug there instead of repeating it. Everything else is pulled straight from `content.json`'s `purpose`, `done_looks_like` and `the_call` fields, nothing typed in fresh. Verified before shipping: all 114 zone-page links resolve to a real file under `site/zones/`, no Pinterest title/description or Instagram caption exceeds its platform's limit, no double space, truncation artefact or em/en dash anywhere in the corpus. Added as the fourteenth data point in `gate_generator_ownership`'s regenerate-and-diff chain, fail-then-pass proved by planting a hand edit in one committed caption file, watching the gate name it, then reverting. `OWNER-ACTIONS.md` item 16 updated to point Phil at the files. |
| A5 (funnel instrumentation) closed, 2026-09-09 operator | `PLAN-MICROZONES-DECKS-APP.md`'s A5 named five events, three of which were still missing: `quest-cause-shown`, `quest-card-abandoned` (pass, elapsed seconds), `quest-return` (days since last visit). Without them the app could not tell whether a stranger bounces at the symptom question, at the cause screen, or mid-card, nor whether anybody ever comes back, which is Epic 1 (measurement) work and outranks everything else unblocked this cycle per `GOALS.md`'s ordering. Added all three to `site/assets/js/quest.js`, reusing the existing `m()`/`window.Measure.track` path every other quest event already uses. `quest-victory-confirmed` was not added separately: `quest-first-victory` (shipped 2026-09-08) already marks that exact moment. Verified with a new static gate (`gate_quest_funnel_events`, `ops/tests/test_gate_quest_funnel_events.py`, 8 cases, fail-then-pass) and a real headless-Chromium run (`ops/tests/test_quest_flow.py`, extended to stub `window.Measure`, drive the symptom flow, and simulate a hidden tab mid-card): `quest-symptom-picked`, `quest-cause-shown` and `quest-card-abandoned` all confirmed firing with correct data. Not verified: the events landing in the live Umami database, no credential in this sandbox. `ops/fingerprint_assets.py` rerun (`quest.js` hash `7b7eabf56c` to `d10470e839`); full `preflight.py`, all 51 `ops/tests/test_*.py`, `check_urls.py` (188/188), `audit_pages.py` (0/191), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. |
| `PLAN-MICROZONES-DECKS-APP.md`'s M7/A1 (per-card victory conditions) found stale, closed against reality rather than re-authored, 2026-09-09 operator | Both rows still read as open, 1.5 days of unstarted work: "570 of 684 cards print a stop condition that card cannot reach." Checked the live code before either starting that authoring or trusting the row: Phil's own commit `fa491b1a` (2026-09-07) had already fixed the actual lie in `site/assets/js/quest.js`'s `renderCard()`, relabelling the heading from "You can stop when" to "The whole zone is done when" and adding "This card is pass N of 6, so you are not aiming for all of it right now." That closes the honesty defect the row exists to fix; it does not meet M7's literal acceptance criteria (a distinct authored `victory` line per card), which stays real, unstarted product-tier work correctly held behind the traffic constraint (`GOALS.md` rule 1) rather than started under this cycle's own time pressure. Updated both rows in `PLAN-MICROZONES-DECKS-APP.md` (section 1.4, M7, A1) to say so precisely, so a future cycle does not re-find the same "open" row and either re-investigate it cold again or spend 1.5 days re-authoring something whose urgent half is already fixed. New `gate_quest_card_victory_honesty` in `preflight.py` (`ops/tests/test_gate_quest_card_victory_honesty.py`, 6 cases) protects Phil's fix itself from regressing, since nothing previously did; proved fail-then-pass by reintroducing the literal old heading into the live, committed file and watching preflight fail by name before reverting. `preflight.py` fast clean after (0 gates failed), full test suite, `check_urls.py`, `audit_pages.py`, `affiliate.py --check`, mobile `npm test` all clean. |
| Scheduled GitHub Actions workflows found not running on their configured schedule, sustained 14+ days; two live claims corrected, a standing check added, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass checked a claim nobody had verified against the Actions API: `fulfil-orders.yml` says "every 30 minutes" and `hourly-brief.yml` says "hourly," both in their own cron comments. Pulled real run history directly: `fulfil-orders.yml`'s last 49 gaps averaged 213 minutes (worst 367) against a configured 30, zero of 49 within 35 minutes of the previous run; `hourly-brief.yml`'s last 49 averaged roughly 4 hours against a configured 60. Both sustained across the full 8 to 14+ day history the API returned, not one bad day; a same-day `ops/NIGHTLY-LOG.md` entry had already treated one instance of this as a "GitHub-side incident," which the fuller history shows is this account's sustained normal, not an outage. Found one live consequence: `ops/roadmap_report.py`'s email to Phil claimed a reply "reaches the operator within the hour," which the data shows is false; reworded to state the operator's next automated cycle picks it up, typically within a few hours, and named the cause (GitHub's own scheduler) rather than promising a clock this repository does not control. `thanks.html`'s own delivery copy already said "within a few hours... not instant" and needed no change. `fulfil-orders.yml`'s header comment, which still reasoned from the literal 30-minute promise, corrected to cite the measured reality. New `ops/check_cron_cadence.py` (parses the real cron line, pulls live run history via the Actions API, flags a workflow whose mean gap exceeds 2.5x its configured interval) and `gate_scheduled_workflow_cadence` in `preflight.py` make this a standing, machine-checked warning instead of a fact that has to be rediscovered by hand; `ops/tests/test_check_cron_cadence.py` (6 cases, fail-then-pass proved) covers on-schedule, degraded, unreachable-API and too-few-samples. Warning, not failure: the delay is GitHub's scheduler, not something a commit here causes, and no live page is currently wrong. Full `preflight.py`, all test files, `check_urls.py` (188/188), `audit_pages.py` (0 findings), `affiliate.py --check` (162 documents) all clean after. |
| The cron-cadence gate above widened from 2 of 5 scheduled workflows to all 5, a real parser bug fixed in the process, 2026-09-09 operator, a later cycle the same day | `check_cron_cadence.py`'s `WORKFLOWS` list and its parser only ever handled one cron shape ("N times an hour, every hour"), so `linkedin-drafts.yml`, `roadmap-report.yml` and `status-email.yml` were silently uncovered. Tested the old parser directly against the real files: it mis-parsed a once-daily cron as 60 minutes (24x wrong) and a six-times-daily cron as 60 minutes (4x wrong), and gave up on the four-separate-cron-lines shape entirely. Rewrote it to sum fires-per-day across every cron line, refusing to guess (`None`) when a weekday/month/day-of-month field is not `*`. Measured all 5 against the real Actions API: `linkedin-drafts.yml` and `roadmap-report.yml` both run almost exactly on schedule (ratio 1.00, 0.98); `status-email.yml` drifts 1.57x, real but under the 2.5x degraded line. This narrows the finding above rather than widening it: the sustained slowdown is specific to the two workflows GitHub is asked to fire more than once an hour. `ops/tests/test_check_cron_cadence.py` extended 5 to 9 cases, fail-then-pass proved in an isolated worktree (old parser mis-parsed all 3 new shapes and wrongly guessed a number for the weekday-restriction case). Also fixed a small honesty bug found reading that same test file: its own final line hardcoded "N of 6 cases pass" when only 5 cases existed. Full `preflight.py` (0 gates failed, 16 warnings), all 54 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. |
| Six page generators found silently stripping the cache-busting fingerprint off every page on the site when run standalone, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a low-mention `ops/*.py` file cold (`build_articles.py`) and ran it to verify, per this file's own step 5d, rather than trust it on sight. Running it standalone diffed 20 committed pages, all of them losing their `?v=` fingerprint on `site.css`/`measure.js`/`site.js`. `build_corporate.py`'s own comments explained why and how it had already been fixed there: `wire_measure.main()` rewrites the measurement tag as a bare path it cannot restore a hash onto, and `canonical_links`/`wire_pwa` do the same to other asset paths, across all 190 site pages, not just the ones a given generator writes. Checked all nine generators that chain `build_avif.wire()` the same way: only three (`build_corporate.py`, `build_kit_page.py`, `build_kitchen_deck_page.py`) also chained `fingerprint_assets.main()` after it. The other six did not, `build_zone_pages.py` (114 pages, the single biggest surface on the site) among them. Confirmed live on all six, not assumed from the pattern: ran each standalone on a clean tree and watched its own output lose every fingerprint. Fixed by chaining `fingerprint_assets.main(False)` at the end of each of the six, matching `build_corporate.py`'s exact pattern; reran all six standalone afterward, zero diff on every one. New `gate_generator_chains_fingerprint` in `preflight.py` statically scans every `ops/build_*.py` file and fails by name if it chains `build_avif.wire()` without also chaining the fingerprinter, so a future generator cannot ship the same gap unnoticed; `ops/tests/test_gate_generator_chains_fingerprint.py` (6 cases) proves it, fail-then-pass verified directly against the pre-fix generator source via `git stash` (all six real files failed by name before the fix, zero after). Full `preflight.py` (0 gates failed, 16 warnings, `hooks-enabled` also fixed this cycle by setting `core.hooksPath`), all 55 test files, `preflight.py --own` (generator-ownership regenerate-and-diff, every checkable generator byte-identical), `check_urls.py`, `audit_pages.py` all clean after. No site page content changed; this is generator robustness, not a content or pricing fix. |
| An owner action sitting unsurfaced for six days, found reading `ops/experiments.py` cold, added to `OWNER-ACTIONS.md` and gated, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a low-mention `ops/*.py` file cold and ran it, per step 5d. `ops/experiments.py --offline` prints EXP-001's `owner_action`: visit `?6s-internal=1` once on each of Phil's own devices, so a future buy-click can finally be told apart from a stranger's, the separation the nine historical clicks could never have (`BACKLOG-2026-H2.md` 1.3, permanently AMBIGUOUS, correctly not reopened). That instruction has existed in `ops/experiments.json` since 2026-09-03; `measure.js`'s own comment confirms it was still unused as of 2026-09-08 (not one event in the whole database carries a `who` key); and it was never once added to `OWNER-ACTIONS.md`, the one file CLAUDE.md 0.5 designates for exactly this, so it only ever printed inside this tool's own output. Added item 1c to `OWNER-ACTIONS.md` (header date updated) and new `gate_experiment_owner_actions_surfaced` in `preflight.py`, which fails if any experiment carrying an `owner_action` in `experiments.json` is not named by id in `OWNER-ACTIONS.md`. `ops/tests/test_gate_experiment_owner_actions_surfaced.py` (5 cases), fail-then-pass proved directly against the real repository. Also checked, and set aside as already correctly diagnosed: whether the standing cron-cadence warning (`fulfil-orders.yml`/`hourly-brief.yml`) might be self-inflicted queue contention from this repo's own push-CI volume rather than genuinely GitHub-side; both workflows already run staggered off the platform's busiest minutes and every checks.yml run inspected started the instant it was created, so nothing supports a fixable local cause. Full `preflight.py` (0 gates failed, 17 warnings), 55 of 56 test files individually (the 56th, `test_generator_ownership.py`, is a slow pre-existing worktree test unrelated to this change; the gate it protects verified directly via `preflight.py --own`, clean before and after), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. |
| A2/M5 (differentiate the 19-link related-reading block on the 102 non-diagnosed zone pages) closed, 2026-09-09 operator | Every unblocked row in this backlog was again done or Phil-gated (M6 correctly still gated on M4's 21-day read, C5/C6 correctly Phil-gated), so this pass worked the highest-ranked genuinely open, unblocked row: A2 here / M5 in `PLAN-MICROZONES-DECKS-APP.md`, explicitly aimed at the constraint (Epic 3, traffic) rather than another audit pass. New `general_reading()` in `ops/build_zone_pages.py` scores each of the 19 `ZONE_READING` articles against a non-diagnosed zone's own already-published text (its judgement call, all six passes, its hazards) using grounded keywords (each article's own title/description, plus the mapped root cause's own `meaning` where one exists) weighted by rarity across the 19, real overlap only, nothing invented for the match. Found and fixed mid-build: an early version summed keyword-weight over an unsorted set intersection and produced a different pick on roughly 1 run in 5 depending on `PYTHONHASHSEED` alone, caught by running it under multiple seeds before it ever reached the real corpus, fixed by summing over `sorted()` lists; a second pass found the initial per-slug cap of 30 was checked only against the 102 non-diagnosed zones' own usage, letting the 12 diagnosed zones' independent `cause_reading()` picks push three articles to 38 site-wide links against the stated ceiling, fixed by seeding the cap counter from real diagnosed-zone usage (`_diagnosed_article_usage()`); a third pass found 11 of 102 zones (thin, generic real text, e.g. two patio zones) collided on an identical set once the tightened cap excluded their next-best candidates, fixed with a deterministic post-pass that swaps a colliding zone's lowest-scoring pick for the next distinct alternative, preferring one still under cap and accepting a small, disclosed overage (31 to 33 against a stated 30) for 3 of the 19 articles only when no under-cap alternative exists, since uniqueness has no stated tolerance and the cap's purpose is not meaningfully harmed by a handful of articles a few links over. Verified against the real, generated pages: all 114 zone pages carry 3 to 5 related-reading links, 0 duplicate sets site-wide, every one of the 19 articles keeps 16 to 33 site-wide inbound zone links, `ops/link_graph_report.py` confirms 0 orphans introduced and article inbound-link concentration dropped from avg 76.3 (min 1, max 124) to avg 26.6 (min 1, max 47). New `gate_general_reading_differentiated` in `preflight.py` re-derives the picks fresh from `content.json` on every run and checks both the corpus-level result and that the shipped pages carry it, mirroring `gate_diagnosis_rendered`'s two-layer M4 pattern; `ops/tests/test_gate_general_reading.py` (12 cases) and `ops/tests/test_general_reading.py` (hash-seed determinism across 4 seeds, idempotency, size and uniqueness) both fail-then-pass proved (a planted render mismatch, a planted duplicate set). `ops/build_zone_pages.py` re-run twice back to back is byte-identical (`gate_generator_ownership`'s own requirement). Full `preflight.py` (every gate passed, 18 warnings, all pre-existing sandbox limitations), all 58 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0, 0 duplicate titles/descriptions), `affiliate.py --check` (163 documents), mobile `npm test` (4 suites, 36 assertions) all clean after. Full account in `PLAN-MICROZONES-DECKS-APP.md`'s M5 row. |
| `site/llms.txt`, the file AI answer engines read to learn what the site offers, found missing both free card decks and gated against future drift, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a low-mention, hand-maintained file cold rather than another audit sweep, per step 5d. `site/llms.txt` named `/zones/`, `/rooms/`, `/articles/`, `/method.html`, `/quest.html` and `/shop.html`, but not `/deck.html` (the Entryway deck, 88 cards, free to print, live since before this file was written) or `/kitchen-deck.html` (the Kitchen deck, 72 cards, shipped 2026-09-08). Both are free, ungated lead magnets, exactly the kind of asset an AI answer engine could cite or recommend, and `GOALS.md` O1 already confirms ClaudeBot and GPTBot fetch this site directly (20 and 10 times respectively in one 72-hour window). Nothing generates this file and nothing checked it, so the gap was invisible by default rather than by any decision; this is the same "source shipped, artifact never re-derived" defect class named at the top of this backlog, just on a manifest file instead of a page. Checked before writing anything: the mudroom deck (`site/deck-gallery-mudroom.html`, 90 written cards) is deliberately held back from promotion by Phil's own decision (`BACKLOG-2026-H2.md` 2.7, pending evidence from the Entryway deck), so it stays out of `llms.txt` on purpose, not by another oversight. Added one bullet naming both promoted decks, honestly noting the Kitchen deck's art is not drawn yet (a coloured glyph panel, not a photograph). New `gate_llms_txt_current` in `preflight.py` checks `llms.txt` still names every promoted free asset (`/zones/`, `/rooms/`, `/articles/`, `/quest.html`, `/deck.html`, `/kitchen-deck.html`, `/shop.html`), so the next promotable page that ships without a matching edit here fails the cycle instead of sitting invisible again; `ops/tests/test_gate_llms_txt_current.py` (5 cases) fail-then-pass proved, including directly against the real committed file with the two-deck bullet planted out and back in. Full `preflight.py` (every gate passed, 17 warnings, all pre-existing sandbox limitations), all 59 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched. |
| The one automated, credentialed, hourly mail Phil reads could have missed a repeat of the 2026-08-30 payment-link outage; fixed, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass asked which scheduled job actually holds both a Stripe credential and real egress to the live site, since `ops/check_live_links.py` (built specifically to catch a deactivated Stripe link that still answers HTTP 200) needs both, and every operator sandbox this week has had neither. `.github/workflows/hourly-brief.yml` has both: `STRIPE_SECRET_KEY` is already in its "Measure, read the inbox, and send" step, and that same job already proves real egress to `6s-success.com` by running `ops/indexnow.py`. It never called `check_live_links.py`. `ops/hourly_brief.py`'s own SITE section checked HTTP status only, the exact blind spot that tool's own docstring names, and COMMERCE's "live payment links" line was a raw count of active Stripe links, not proof the live buttons point at them. Fixed with a new pure `payment_link_summary()` wired into `build()`: it calls `check_live_links.check()` and, on a confirmed-dead or account-unknown live link, prepends `OUTAGE - PAYMENT LINK DEAD -` to the mail SUBJECT (not only the body, since a scanning reader may never open it), while a genuinely unchecked state (no credential, site unreachable) reads as UNCHECKED, never OK and never an outage, per `CLAUDE.md` 0.4. No workflow YAML edit needed; the credential was already there. New `gate_hourly_brief_payment_links` in `preflight.py`, fail-then-pass proved directly (an `AttributeError` without the fix, surfaced by name through `run_gate`'s own exception handling), exercising all four real branches plus the real `build()` subject line with a stubbed `check_live_links.check()` and no network touched. Full `preflight.py` (every gate passed, 17 warnings), all 59 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| `ops/service_orders.py`, which forwards real paid service bookings to Phil with a real calendar invite, had zero test coverage and a live date-parsing bug; fixed and tested, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a low-mention `ops/*.py` file cold, per step 5d, and this time checked one that touches real money and a real customer-facing artifact rather than a page generator. `find_time()`'s two `DATE_PATTERNS` captured an unanchored `(\d{1,2})` for the hour, so a message naming an explicit year sliced a piece of that year out as the hour instead: proved directly, `"Oct 14, 2027 at 2pm works for me"` parsed to `2026-10-14 20:00` (year silently dropped, hour wrong, both from matching "20" inside "2027"), never reaching the real "2pm" later in the same sentence. That is exactly the failure mode the file's own docstring says to avoid ("a wrong appointment time is worse than no invite, so anything ambiguous returns None"): a stated year the parser cannot use is ambiguity, not something to guess through, and the wrong guess would have shipped as a real `.ics` invite attached to a real forwarded booking. Fixed by wrapping every bare day/hour capture in `(?<!\d)...(?!\d)` so it can never match a slice of a longer digit run; a year-qualified message now correctly returns `None`. New `ops/tests/test_service_orders.py` (the file's first test), fail-then-pass proved directly (three year-qualified cases wrongly returned a time before the fix, correctly `None` after), also covering the ordinary two-pattern date parsing, the bare-hour-below-8 afternoon heuristic, `which_service()`'s earliest-position subject-over-body rule, and `ics()`'s VEVENT shape and per-service duration, none of which had any prior coverage. No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file. Also set `core.hooksPath` (per-clone, unset again this checkout as usual). Full `preflight.py` (every gate passed, 16 warnings, one fewer: hooks-enabled cleared), all 61 test files, `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| `ops/youtube_upload.py`, the tool that will publish real videos to the live public channel the moment Phil pastes OAuth (owner gate 1), had zero test coverage protecting its own double-post refusal; fixed, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a genuinely unread, low-mention `ops/*.py` file cold, per step 5d, choosing one directly in the O1/traffic distribution chain rather than another generator. Traced the logic for a real bug and found none (the slug this file reads from `build/video/youtube/*.json` and the one `build_thumbnails.py` writes under both derive from the same `video_zone.zone_slug()`, the single-source-of-truth fix already made for five sibling files; the ledger/ok/failed exit-code arithmetic checked out), but the file's own docstring states a hard rule with real consequence ("Upload the same zone twice" is refused on principle, because "YouTube cannot replace a video file after upload, so a double post means a duplicate that has to be deleted by hand") and nothing proved that refusal held. New `ops/tests/test_youtube_upload.py`, exercising `ledger()`'s corrupt-file refusal (must raise, never silently read as "nothing published"), `jobs()`'s dedupe against the ledger, its mp4-required/srt-optional filtering, and `--room` filtering, all against a real isolated tmpdir, no Google credential touched. Fail-then-pass proved directly: commented out the `if m["slug"] in done: continue` dedupe check, watched the new test fail by naming the exact double-post risk, reverted. No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file. Full `preflight.py` (every gate passed, 17 warnings), all 63 test files, `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| The five Etsy listing PDFs were stale against the live site's own Sustain rewrite; regenerated and gated, 2026-09-13 operator | Handoff from the prior cycle: `build/listings/build_etsy_assets.py`'s PDFs, unprotected against source drift the same way `build_kdp_cover.py` was until this week. Checked directly with pymupdf rather than assumed from file dates: the Whole House Pack's own source HTML was substantively regenerated 2026-09-07 (the Sustain rewrite), the Kitchen pack's zones carry the same rewrite, and all five delivered Etsy PDFs were last rendered 2026-09-03; `check_etsy.py` only compares page/card COUNTS against `etsy-listings.json`, and both stayed identical (76 pages/684 cards; 6 pages/42 cards) because no card was added or removed, only its text improved, so a listing this stale would pass every existing check and a real buyer on Etsy would have received the old, thinner text. `build_etsy_assets.py` hardcoded a Windows-only Edge path, so nothing but Phil's own laptop could ever regenerate it; widened `find_browser()` to also find a Linux Chromium-family binary (this operator sandbox ships one at `/opt/pw-browsers/chromium`), regenerated all five listings plus the shared instructions sheet, and verified with `check_etsy.py` (clean) and by extracting text with pymupdf (confirmed the rewrite text is now present; page/card counts unchanged, matching the geometry claims in `etsy-listings.json`). New `gate_etsy_pdfs_current` in `preflight.py`, same regenerate-and-diff posture as `gate_kdp_cover_current` but comparing NORMALIZED TEXT rather than raw bytes: proved directly that two back-to-back real renders of identical source are byte-different (own PDF metadata/ids) even though the text is identical, so a byte diff would flap on every run regardless of real content. `ops/tests/test_gate_etsy_pdfs_current.py` (5 cases, isolated git fixtures rendered through the real headless-Chromium path) fail-then-pass proved, including the real regression shape (source text changes, PDF does not) and the dirty-tree refusal. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price touched; these listings are not yet live (owner gate: Etsy account creation, section 6 item 4). |
| `ops/send_questions.py`'s real owner-facing email hardcoded "10 of 10 checks passing, TLS valid" and "deploys are automatic," the second one false; fixed and gated, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass read a low-mention `ops/*.py` file cold, per step 5d, this time one already caught once before for stale claims (`gate_send_questions_current`, 2026-09-05). The SITE STATUS block it drafts for a real email to Phil still hardcoded a claim of "10 of 10 checks passing, TLS valid," never measured at send time, and "Deploys are automatic: push to main and the host pulls within five minutes," which is false against `DEPLOYMENT.md`'s own canonical description ("The one step no autonomous session can perform is the Redeploy click") and against this run's own confirmation that no workflow here calls `ops/deploy.py` and this sandbox holds no deploy key. Both are the same "artifact never re-derived from its source" defect class this backlog's own closing line names as dominant, this time in a real email instead of a page. Fixed with a new `site_status_lines()` that derives the reachability/freshness line from `ops/deploy_freshness.py`'s own live-checked verdict (current, stale, or honestly unknown when unreachable, never asserting health it could not measure) and states the real deploy mechanism. `gate_send_questions_current` extended with three checks (the "automatic" claim, the hardcoded checks-passing phrase, and that `deploy_freshness` is still actually imported), fail-then-pass proved in an isolated worktree against the real pre-fix file. New `ops/tests/test_send_questions.py` (6 cases: unreachable reads as unknown never healthy, current, stale reads as out-of-date not fine, the regression itself, a broken live-check degrades to unknown rather than raising, `build()` includes the block), fail-then-pass proved (the untracked test file against a stashed pre-fix `send_questions.py` fails with `AttributeError` naming the missing function). Full `preflight.py` (every gate passed, 17 warnings), all 64 test files (via `gate_tests()`), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| `GOALS.md`'s own "Sessions from organic search" row said "0 from Google" for four days after the file's own 2026-09-05 correction said otherwise, and the same retired claim was repeated live in three more places; fixed and gated, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so this pass re-read `GOALS.md` itself, the file this backlog's own step 0 says decides what to work on, rather than another `ops/*.py` file. Found a self-contradiction on one page: the O1 table's "Sessions from organic search" row still read "1 in 30 days... one visit from Bing, none from Google," three lines above a "Corrected 2026-09-05" paragraph stating the opposite as fact (a Google referral landed 4 September). The row had never been edited since it was first written; only the paragraph below it had been corrected. Checking the sibling most likely to repeat it (`STATUS.md`, since `gate_no_stale_session_label` and `gate_no_stale_affiliate_blocker` both exist because a GOALS.md correction there had gone untold to a sibling before) found the identical stale claim live in three separate places, one of them also carrying the retired 52/144 traffic figure four days after GOALS.md moved to 60/161. Fixed all four spots to state the real count (2 search referrals ever, one Bing 21 August, one Google 4 September) and the current traffic baseline. New `gate_goals_organic_search_row_current` in `preflight.py`, checking both files, quote-aware so a correction narrating the old wording in quotes cannot trip it; fail-then-pass proved in an isolated worktree against the real pre-fix text of both files (2 distinct failures, both by name), then clean on the fixed files. Full `preflight.py` (every gate passed, 18 warnings), all 66 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| A zone's YouTube title, its own page SEO title and its own H1 named it three different ways, and a second live schema bug (a doubled article in 113 of 114 pages' own structured data) surfaced fixing it, 2026-09-12 operator | Every unblocked backlog row was again done or Phil-gated, so per step 5d this pass cold-read `ops/build_youtube_metadata.py` (6 mentions, the lowest in the tree). `title_for()`/`description_for()` built the video's title and description from the raw internal zone key, contradicting the file's own docstring, which already quoted the exact reason not to ("Nobody searches 'Landing Zone'"). Checked one zone end to end: video said "the landing zone", the linked page's own `<title>` said "the entryway drop zone", its H1 said "The Landing Spot". Fixed by extracting `zone_seo_title()` into `build_zone_pages.py` as the single source (the `zone_page_slug` precedent) and pointing the metadata generator at it and at `display()`. Regenerating to verify surfaced a second, independent, already-live defect: 113 of 114 zone pages' own HowTo JSON-LD read "How to reset the The Landing Spot", because 113 of 114 `NAME_MAP` display names already start with "The". Fixed with a one-line conditional article. New `gate_zone_name_consistency` in `preflight.py`, `ops/tests/test_gate_zone_name_consistency.py` (6 cases, fail-then-pass, fixtures built from the real pre-fix output). `build_social_captions.py` reuses `title_for()` by design; regenerated its 114 files too, caught by `gate_generator_ownership` before it could ship stale. `OWNER-ACTIONS.md` item 1 got an optional note: the 12 already-public videos carry the same stale titles, fixable by Phil directly in YouTube Studio, no OAuth needed. Full `preflight.py` and `--own` clean, all 107 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) after. No price/product touched. IndexNow attempted (113 changed URLs), correctly UNCHECKED: no egress from this sandbox. |
| `PLAN-VISUAL-STRATEGY.md` (ux-frontend, 2026-09-07) claimed in present tense that all 114 films still fail the truncation/six-pass tests, when the same-day commit that introduced the document had already fixed exactly that in `ops/video_zone.py`; corrected and gated, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated; `GOALS.md`, `STATUS.md` and both backlogs were all clean of new drift this pass, so this pass read the newest strategy document cold rather than an `ops/*.py` file, since a plan feeding future prioritization is exactly the kind of document a stale claim in it can waste a future cycle's day on. Verified directly against the code, not assumed from the plan's own footnotes: `ops/video_zone.py`'s `beats()` already splits every pass at sentence boundaries via `_sentence_chunks()` (never mid-clause, `_re.split` on `.!?`) and renders all six passes (`order = [sort, straighten, shine, safety, standardize, sustain]`), both landed in commit `2d99fecb`, the same commit `git log` shows introduced `PLAN-VISUAL-STRATEGY.md` itself. `ops/video_narrated.py`'s `build()` calls `vz.beats()` directly, so the narrated pipeline was never a separate gap. Yet the plan's §0 summary, §5.1 lead-in and its own "Today all 114 films fail V1 and V2" line all still read as if the defect were live, and its §5.2 "next version, in order" list still named the fix as step 1 and 2 to do. Corrected all four spots with a note naming the fixing commit, and struck through the two already-done steps in §5.2, while keeping the original finding text in §1/§5.1 intact and dated as the pre-fix record it is. Honestly noted what stays unverified: whether locally rendered video files (not committed to git) have actually been regenerated from the fixed code, and whether the 12 videos already live on YouTube (uploaded by hand before this pipeline existed) carry the fix, since YouTube cannot replace an uploaded file once posted. No ffmpeg in this sandbox to check either way. New `gate_visual_strategy_truncation_current` in `preflight.py`: fails if the plan's V1/V2 claim ever again drops its "fixed" correction, and independently re-checks `ops/video_zone.py` still contains both the sentence-split function and all six passes, so a future revert of the code is caught here too, not only by the existing `gate_films_teach_all_six_passes` (which needs a real rendered batch present to run). Fail-then-pass proved directly against the real files (reverted the doc text, watched the gate fail by name, restored, confirmed clean); found and fixed one em dash introduced while drafting the fix itself, caught by `ops/dashboard.py`'s own `ctrl_em` count going 0 to 1, before it reached the commit. Full `preflight.py` (every gate passed, 19 warnings), `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| A finished Etsy listing, L3-entryway, was one owner action from selling a customer the exact content the site's own catalogue already gives away free; withdrawn and gated, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so per step 5d this pass read the five zero-mention files in `build/listings/` cold rather than a saturated `ops/*.py` tier. `build/listings/etsy_economics.py` compares each Etsy listing's Etsy net against its "direct_price_usd", the same product's price through the site's own Stripe checkout, for a reason stated plainly in its own docstring: "the money left after Etsy's cut is not less than the money left after Stripe's cut." For L3, Entryway Pack, `direct_price_usd` is $9.00, but no such product exists to buy directly: `ops/generated_products.py` already excludes RP-ENTRYWAY, the identical file `build_etsy_assets.py` renders into L3's PDF, from the site's own paid catalogue, with its own stated reason, "the free Entryway deck already covers... a four dollar pack of the same cards is not gating free content, but it is selling somebody a strictly worse version of something they could have for nothing, and a customer who found out afterwards would be right to be angry." Verified directly, not assumed from either docstring: read `build/entryway-cardtext.json`, the free deck's actual corpus (Event/Habit/Micro Zone/Problem/Skill/Tool/Upgrade/Win cards, a diagnostic game), and `build/products/RP-ENTRYWAY.html`, L3's actual source (30 cards, the plain six-pass Sort-through-Sustain instructional set for the same five zones) to confirm the overlap is real, not a false positive from a name match. `MARKETPLACE-LISTINGS.md` section 3.1 already states this exact principle for the Standards Pack ("Deliberately not listed... Selling it on Etsy for money would be a trust problem the first time a buyer noticed") without anyone connecting it to L3, which was fully written, priced, rendered (`build/listings/etsy/L3-entryway/`) and named ready in `OWNER-ACTIONS.md` item 15, one 40-minute owner action from a real Etsy shop where a customer's bad review is public and permanent. Withdrawn: removed from `build/listings/etsy-listings.json` (5 listings to 4), its rendered files, `build_etsy_assets.py`'s render table and `etsy_economics.py`'s tables; `MARKETPLACE-LISTINGS.md` and `OWNER-ACTIONS.md` item 15 corrected with the full finding, the old L3 section kept as a dated, marked-withdrawn record rather than deleted. New `free_duplicate_skus()` in `check_etsy.py`, wired into its existing `main()` (and so into `preflight.py`'s existing `gate_etsy_listing_valid`, no new gate needed): fails any listing whose `source_sku` (now a field on every entry in `etsy-listings.json`) is a SKU `ops/generated_products.py` has dropped as free, so this cannot recur silently for L3 or any future listing. Fail-then-pass proved directly against the real, pre-fix `etsy-listings.json` (failed by name, naming L3-entryway and RP-ENTRYWAY), then clean after the fix; `ops/tests/test_check_etsy_free_duplicate.py` (6 cases) covers the function and its `main()` integration independently. Full `preflight.py` (every gate passed, 18 warnings), all 67 test files, `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No live price or Stripe object touched: L3 never reached Stripe or Etsy. |
| `ops/ship.py`, the tool this repository is told to ship through instead of a plain commit, could push a live unresolved merge conflict marker straight to `origin/main` while reporting success; reproduced live and fixed, 2026-09-09 operator | Every unblocked backlog row was again done or Phil-gated, so per step 5d this pass read `ops/sync_push.py` cold, a 4-mention file, and followed it to `ops/ship.py`, the newer tool that superseded it and that recent cycles' own log entries recommend using ("shipped through `ops/ship.py`... rather than a plain commit"). `sync_push.py`'s own docstring names the exact incident it exists to prevent: a naive conflict-resolution loop put unresolved conflict markers into two generated files on 2026-09-01 and broke CI. `ship.py`'s `GENERATED` list (files it treats as safe to blindly regenerate and stage on any rebase conflict) added `CHECKIN-LOG.md` and `ops/state-checkin.json` beyond `sync_push.py`'s own three. Neither is a true regenerable output: `ops/checkin.py` only ever appends to the log (never rewrites the body, so a fresh run cannot clear a marker already sitting in it) and carries forward prior state into the JSON (so a blind rerun during a conflict can silently erase a still-standing measurement), and `ship.py`'s own conflict handler calls only `dashboard.py`, which touches neither file at all. Reproduced directly rather than reasoned about: built an isolated sandbox with a real bare "origin" and two clones forcing a genuine same-line conflict in both files, ran the real, unmodified `ops/ship.py` against it. It reported "push ok, attempt 1" while `origin/main` received literal `<<<<<<<`/`=======`/`>>>>>>>` markers in both files, because `git add` marks a conflicted path resolved regardless of its content. Fixed by narrowing `GENERATED` to `sync_push.py`'s own three true dashboard outputs (a conflict in the other two now stops the push and names the files, exactly like any other real-source conflict) and adding a `sync_push.markered()` scan immediately after conflict resolution as defense in depth, aborting the rebase if any marker survives regardless of which list let it through. New `ops/tests/test_ship_conflict_safety.py` (2 cases: the real bug scenario, driving the actual shipped `ship.py`/`sync_push.py` end to end against a real git sandbox and reading the content actually pushed to origin, not just the exit code; and the legitimate dashboard-only auto-resolve path, to prove the fix did not also break the tool's own purpose). Fail-then-pass proved directly: `git stash` on the pre-fix `ops/ship.py` reproduced the exact live markers on origin/main, restored and clean after. No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file. Full `preflight.py` (every gate passed, 19 warnings, all pre-existing sandbox limitations), 68 test files, `check_urls.py` (188/188), `audit_pages.py` (0 duplicate titles/descriptions), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| This session's own first `preflight.py` run failed 2 gates a second, unmodified run passed clean; root-caused to an unverified pip install and fixed, 2026-09-10 operator | Every unblocked backlog row was again done or Phil-gated, so per step 5d this pass checked its own cold-checkout preflight run rather than trust a passing rerun as the answer. `bootstrap_fresh_sandbox()` fired one `pip install pymupdf` and moved on regardless of exit code; this run's first attempt hit a real `ReadTimeoutError` against `files.pythonhosted.org` (a cold proxy tunnel, not a policy denial), leaving pymupdf missing when `affiliate.check()` tried to read two delivered PDFs, which correctly fails closed when it cannot look, taking `test_affiliate.py` down with it. New `ensure_pymupdf()` retries the install up to 3 times with a longer timeout and verifies a real `import` after each attempt instead of trusting pip's exit code. `ops/tests/test_ensure_pymupdf.py` (4 cases), fail-then-pass proved via `git stash`; reproduced the real fix end to end by uninstalling pymupdf and running the genuine `preflight.py` cold. Full `preflight.py` (every gate passed, 18 warnings), all 74 test files, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable. |
| `ops/reflow.py`, which turns the real corpus of 311 LinkedIn drafts Phil reads and sends himself every morning into solid paragraphs, was burying the call-to-action line mid-paragraph on most of them; fixed and tested, 2026-09-10 operator | Every unblocked backlog row was again done or Phil-gated, so per step 5d this pass read a low-mention `ops/*.py` file cold and ran it against the real corpus rather than trust the read. `reflow()`'s own docstring promises a trailing block naming the book "stays on its own line, because a link buried mid paragraph is a link nobody follows," but `TAIL_RE` only matched a last block containing "6S Success", "6s-success.com" or "Chapter <digit>". Checked directly against `ops/corpus_posts.py`'s real 311-post pool, not assumed: 108 of 311 posts end on a call-to-action line ("Read the free chapter.", "Grab the free Use Test card in the online book", "Curious about the whole method? The chapter is free online.") that none of those three phrasings catch, so the line got fused into the paragraph above it, exactly the burial the docstring says this exists to prevent. Fixed by widening `TAIL_RE` to the same free-chapter vocabulary `corpus_posts.py`'s own `FREE_CLAIM` pattern already uses for a different check in the same pipeline, plus "online book"; re-ran against the real corpus, 0 of 311 posts now match. New `ops/tests/test_reflow.py` (8 cases, including the exact real 6-block post from the corpus, since a smaller 3-block reduction of the same post passed even on the old, buggy code by coincidence of the paragraph-count arithmetic and would have been a false proof), fail-then-pass proved directly via `git stash` against the real pre-fix file (1 case failed naming the exact defect, 0 after). Full `preflight.py` (every gate passed, 17 warnings, one fewer than last cycle: `hooks-enabled` cleared by setting `core.hooksPath`), all 75 test files, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price/product touched, no site page changed, IndexNow not applicable: this is draft-generation logic, not a published page. |
| The site had no RSS/Atom feed at all; a new zero-cost distribution surface shipped rather than another cold-read finding, 2026-09-10 operator | Every unblocked row in sections 2-4 was again done or Phil-gated, and roughly 20 cycles today had each cold-read one more low-mention `ops/*.py` file and mostly found nothing, the cold-read lane itself near exhausted. Re-read `GOALS.md`'s own decision rule 1, "distribution beats production," against what actually exists: no page discovery surface exists besides the sitemap and search crawling, both slow and both not fully in operator control (Search Console is Phil-gated; the site had 2 organic search referrals ever as of `GOALS.md`). A feed needs no account, nothing to enable, nothing to pay for, unlike every other channel on the owner-gates table. New `ops/build_feed.py` writes `site/feed.xml`, an Atom feed of 27 of the 29 articles under `site/articles/`, every field read back off each article page's own title, description, canonical link and JSON-LD `datePublished`/`dateModified` so it cannot drift the way a hand-typed manifest can. Discovery wired to `site/articles/index.html` (`<link rel="alternate">`) and `site/llms.txt` (a named line, same pattern the two free decks already use there). New `gate_feed_current` in `preflight.py`, the same regenerate-and-diff pattern as `gate_sitemap_complete`, and added to `gate_generator_ownership`'s own regenerate-and-diff chain as the fifteenth data point. Fail-then-pass proved directly against the real files and in an isolated worktree planting the actual regression shape (a new article shipped without a feed rerun). The first push still failed CI: the initial version fell back to `git log`'s commit date for the 2 `ops/build_articles.py` pages with no JSON-LD date, which looked checkable and was not, because `git log -1 -- <path>` depends on how much history the checkout holds, and CI checks out depth=1. Proved directly against a real `git clone --depth 1`: a file the tip commit never touched reports the tip commit's own date, not its real history, so the fallback silently disagreed with itself between a full local clone and CI's shallow one. Fixed by removing the fallback: those 2 pages are skipped from the feed (27 of 29) rather than dated by guesswork, re-verified inside a genuine depth=1 clone before pushing again, not just reasoned about. `ops/tests/test_build_feed.py` (6 cases) and `ops/tests/test_gate_feed_current.py` (5 cases) both pass; one collateral fixture in `ops/tests/test_gate_llms_txt_current.py` updated for the new required line. Full `preflight.py` (every gate passed, 19 warnings, all pre-existing sandbox limitations), all 77 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after, plus the full suite rerun inside a real depth=1 clone to match CI. No price or product touched. |
| The site linked to its own live YouTube channel nowhere, and `method.html` told visitors none of the videos existed; both fixed, 2026-09-10 operator | Every unblocked row was again done or Phil-gated. Per `GOALS.md` decision rule 1 (distribution beats production), checked whether the site references its own YouTube channel anywhere: it did not, on any of 191 pages, despite 12 real narrated, captioned zone videos being live there today (`ops/youtube-published.json`, `ops/state-checkin.json`, measured today). Worse, `site/method.html`'s video section said "None of it has been filmed yet," a live false claim on a customer-facing page (`CLAUDE.md` section 8). Corrected the claim and added an honest, visible link to the channel; only then added it to Organization JSON-LD's `sameAs` in `ops/build_seo.py`, which had been deliberately left empty with a comment explaining why, now stale. Disclosed the new outbound host in `privacy.html`'s "Links to other websites" section, which `gate_third_party` correctly required and caught on the first preflight run after the change. New `gate_sameas_backed_by_onsite_link` in `preflight.py`: any `sameAs` entry with no matching on-site `href` now fails the build, in either direction (a fabricated claim, or a link quietly removed while `sameAs` still claims it), fail-then-pass proved directly against the real file. Full `preflight.py` (every gate passed, 19 warnings), all 76 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `link_graph_report.py` (0 orphans), mobile `npm test` (4 suites) all clean after. No price or product touched. IndexNow `--changed` run, correctly reported UNCHECKED (no egress from this sandbox), picked up by the hourly workflow's own credentialed run. |
| `ops/build_avif.py`'s `wire()` silently stopped adding AVIF sources to a page's second picture block once its first was already wired; fixed and tested, 2026-09-10 operator | Handed off by the prior PM check-in as the next low-mention file to cold-read, per step 5d. `wire()` skipped an entire page the instant it contained any `type="image/avif"` string anywhere, on the assumption one avif source meant the whole page was done. Reproduced directly: a two-picture fixture with the first block already avif-wired and the second still webp-only (its own `.avif` file present on disk) got nothing added to the second block. No live page hits this today (887 of 887 webp files carry an avif sibling, confirmed by direct scan), because every generator regenerates its pages from scratch before wiring, but a hand-maintained page gaining a second image after its first wire pass, or a changed generator run order, would ship it silently. Fixed to check per source tag whether an avif source immediately precedes it, not whether the file has one anywhere. New `ops/tests/test_build_avif.py` (3 cases), fail-then-pass proved (`git stash` on the fix, 2 of 3 assertions failed by name, clean after). Reran `--wire` against the real site: 0 pages changed, confirming today's coverage was already complete and the fix is purely protective. Full `preflight.py` (every gate passed, 18 warnings), 84 test files via `gate_tests`, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page content changed, IndexNow not applicable. |
| `ops/root_causes.py` still said EXCESS had no matching article a full two days after a real, on-topic one shipped, and fixing that mapping surfaced a second, real bug in `general_reading()`'s cap logic that could push a zone under M5's 3-link floor; both fixed and gated, 2026-09-10 operator | Every unblocked row was again done or Phil-gated, so per step 5d cold-read `ops/root_causes.py`, a file used by both M1 (frozen cause vocabulary) and M4/M5 (related reading). Its own comment said EXCESS and CONFLICTING USERS both had no article; checking `site/articles/` directly found `more-storage-wont-fix-clutter` (shipped 2026-09-08, "the container trap": excess, wrong location, no assigned home, unclear ownership), a genuine match for EXCESS that the mapping was never told about, the same "source shipped, artifact never re-derived" class this backlog's own closing line names as dominant. Confirmed live impact before fixing: 10 real friction branches across the 12 diagnosed pilot zones reference EXCESS. Mapped it. Regenerating `site/zones/` to pick up the fix (`ops/build_zone_pages.py`) surfaced a second, independent bug rather than a clean rebuild: `general_reading()`'s initial per-zone pick loop respects its own `article_cap` but never checks the 3-link `floor` its exclusions could push a zone below, and the extra diagnosed-zone usage from the EXCESS fix (shared `counts` state, dict-iteration order deciding who gets first pick) starved two low-signal patio zones to 2 links each, silently under M5's stated floor. Fixed `general_reading()` to guarantee the floor over the cap, the same standing the uniqueness swap pass already gives the floor; verified the fix holds even under an artificially harsh `article_cap=1` against the real corpus, not just today's numbers. New `gate_root_cause_articles_current` in `preflight.py` (two-way docstring-vs-mapping consistency, so this cannot silently drift again in either direction; `ops/tests/test_gate_root_cause_articles_current.py`, 5 cases) and a 5th check added to `ops/tests/test_general_reading.py`, both fail-then-pass proved directly against the real files. `ops/build_zone_pages.py` re-run twice back to back, byte-identical after (`gate_generator_ownership`'s own requirement). Full `preflight.py`, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), `link_graph_report.py` (0 orphans, min 3 inbound on the two previously-starved zones), mobile `npm test` (4 suites) all clean after. Full account in `PLAN-MICROZONES-DECKS-APP.md`'s M5/M1 follow-on. No price or product touched, no new page, IndexNow not applicable. |
| `ops/build_sample_html.py`, the generator for the site's primary lead magnet (the free 30-chapter sample), found stripping its own cache-busting fingerprint on a standalone run; fixed and the gate widened to catch a seventh generator shape, 2026-09-10 operator | Every unblocked row was again done or Phil-gated, so per step 5d cold-read `ops/build_social_captions.py` (clean, ran it and checked every field length directly) then `ops/build_sample_html.py`. Its own docstring instructs `python ops/build_sample_html.py --apply` as a standalone command, but `main()` never chained `fingerprint_assets.main()`. Reproduced directly, not assumed: ran its own transform logic against the real source and diffed the result against the committed, shipped file. The only difference was the `?v=` cache-busting hash missing off both stylesheet links (fonts.css, book.css) on `site/downloads/...Sample (Chapters 1-30).html`. `gate_generator_chains_fingerprint` (2026-09-09, protects six sibling generators) could not see this one: it never calls `build_avif.wire()` at all, since it wires no pictures, only degrades them to text. Fixed by chaining `fingerprint_assets.main(False)`; verified a standalone run now reproduces the committed file byte-for-byte. Widened the gate with a second, direct trigger: any `ops/build_*.py` whose source contains a literal unversioned href to a `.css`/`.js` under `assets/`. Checked against the real tier before trusting it: hits exactly the 9 real page generators that write such a literal, all 9 now correctly chaining the fingerprinter, 0 false positives on the rest, including a case proving an already-versioned literal href (the `build_kitchen_deck_page.py` shape) does not trip it. `ops/tests/test_gate_generator_chains_fingerprint.py` extended 6 to 9 cases, fail-then-pass proved directly against the real file (planted the pre-fix regression, gate failed by name, reverted, gate clean). Full `preflight.py` (every gate passed, 18 warnings, unchanged), all test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no new page, IndexNow not applicable, no site page content changed. |
| `ops/merge_cardtext.py`, which builds the free Entryway deck's own text corpus, had never once been run from `preflight.py`; running it cold surfaced 47 dead card cross-references and two fabricated statistics, one already baked into a live download; fixed and gated, 2026-09-10 operator | Every unblocked row was again done or Phil-gated, so per step 5d cold-read `ops/merge_cardtext.py`, a file whose own checks (dangling links, unsourced claims, banned brand terms) had never been wired into `preflight.py` at all. Running it against the real corpus printed "34 dangling links" and "CLAIMS TO VERIFY", output nobody had acted on because nothing ever called it. Widened the check myself (the field also appears as lists of "CODE Title" strings across different transcription batches, not only bare codes) and found 47, not 34: mostly a whole "Experts" card family (`EX-001` to `EX-012`) that was cross-referenced but never authored. Verified against the actual shipped product, not the JSON alone: opened `site/assets/cards/entryway/EE-002-Entryway-Rainstorm-back-lg.webp`, already live on `deck-gallery.html`, and its printed "RELATED CARD PATH" box names "EXPERTS to EX-002 Weather Prep," a card that does not exist. **20 of the 72 already-drawn cards carry this exact dead reference**, confirmed by cross-referencing every reference against the real rendered-image filenames, not assumed from corpus counts. One card, `EM-012` (the deck's last card), additionally carries an unsourced "People make up to 35,000 decisions a day" statistic in its printed "DID YOU KNOW" box (CLAUDE.md section 8) and a "NEXT CARD" promise pointing at "ER-002 Living Room," a second room deck this backlog's own section 5 holds on evidence, not yet built. `gate_unsourced_stats` had a second, independent gap that let the statistic through even where it does render as a plain string: `did_you_know`'s "35,000 decisions a day" was never caught because the number sits before a noun, not directly before a time unit, the exact shape `STAT`'s regex requires; a third gap let the corpus's own "claims" field (a list, not a string) skip the scan entirely regardless of content. Fixed what code can fix, for free: all 47 dangling references corrected or dropped across the six source batches (`ops/cardtext/batch-*.json`; one, "EU-000 Vertical Storage", was a single-digit transcription typo for the real `EU-008`, corrected rather than dropped; the rest point at ids that never existed, dropped per this file's own no-plausible-wrong-value discipline), both fabricated statistics rewritten to true, non-statistical copy, and the `claims` field cleared on the 3 cards whose flagged statement had already been resolved via a rewritten `did_you_know` (so the gate stops re-flagging a problem already fixed). New `STAT_BIG` in `gate_unsourced_stats` catches a thousands-separated number regardless of adjacent unit word; the field-type gap fixed so list-valued fields are scanned the same as string ones; new `gate_card_related_links` re-derives the full valid-id set from the source batches on every run and fails by name on any dangling `next_card`/`related_path`, checking both the batches and the merged `build/entryway-cardtext.json` so a hand-edit to only one side cannot slip through (`ops/tests/test_gate_card_related_links.py`, 10 cases, fail-then-pass proved against both a synthetic regression and the real committed files, including a case proving a legitimate spec number like "684 cards" is not wrongly flagged). **Not fixed, and cannot be by text alone:** the pixels on the 20 already-drawn card backs still show the old, wrong content, because the corpus is a transcription of finished artwork, not its source; regenerating just those 20 backs needs the same Gemini billing gate as `OWNER-ACTIONS.md` item 1b, added there as a new, cheaper line under it rather than a duplicate ask. Full `preflight.py` (every gate passed, 17 warnings), all test files individually, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
| `ops/check_affiliate_trigger.py`, the tool watching the one trigger that authorises an Amazon application, was counting every outbound click, not only retailer ones; fixed and tested, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, and the standing 5-mention cold-read tier's own top candidates (`build_icons.py`, `check_affiliate_trigger.py`, `room_image_variants.py`, `video_narrated.py`, `wire_aria_current.py`) had not been read; `wire_aria_current.py` checked out clean (verified against the real header nav on `resources.html`, `book.html`, `zones/index.html` and a nested zone page, all correctly marking exactly one destination), so per step 5d moved to `check_affiliate_trigger.py`, which gates a real, named business decision (`PLAN-AFFILIATE-MONETISATION.md` T2: apply to Amazon only when retailer clicks reach 60 in 90 days). Its SQL counted every `outbound-click` event regardless of host, on the assumption (true when the query was written, T1's own precondition) that the only outbound clicks were retailer ones. That stopped being true 2026-09-10: `site/method.html` now links the live YouTube channel, which fires the identical event with a `youtube.com` host, verified directly by scanning every external href on the live site (`www.target.com` 135 pages, `www.homedepot.com` 73, `www.youtube.com` 1) and by reading `measure.js`'s click handler, which branches only on Stripe vs. everything-else, no retailer check. A reader clicking the channel link would have counted toward the Amazon-application threshold, the wrong evidence for that decision. Fixed by requiring the event's own `host` field to match a real retailer host, read from `ops/product_links.py`'s own `MERCHANTS` config rather than a second hardcoded list, so a retailer added or retired there is reflected here automatically and a future non-retailer outbound link (Pinterest, Instagram, once those accounts exist) cannot silently inflate T2 either. New `ops/tests/test_check_affiliate_trigger.py` (3 cases: `retailer_hosts()` excludes `youtube.com` and includes the real live retailers; the built SQL names the retailer hosts and filters on `data_key = 'host'`; an empty retailer config reads as unreadable, never as a silent full count), fail-then-pass proved directly (`git stash` on the fix, case 1 failed with `AttributeError` naming the missing function, clean after). No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file, and the fix is structural (read from `MERCHANTS`), not a list that itself needs protecting. Full `preflight.py` (every gate passed, 18 warnings, unchanged), all 92 test files individually, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page changed, IndexNow not applicable: this is analytics-reader logic, not a published page. |
| `STATUS.md` going stale went ungated despite recurring at least six times in one week, and CLAUDE.md's own step 10b says a defect recurring three times with no gate earns one; a new WARN-level gate closes it, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, `preflight.py` was clean on attach, no mail credential, no open PRs. Rather than fish for one more low-mention `ops/*.py` file on a lane the log itself called exhausted, re-read `ops/NIGHTLY-LOG.md`'s last several entries per this prompt's own step 1 and found the actual live pattern: two separate PM check-ins that same day, plus at least four earlier this week, each independently found and corrected `STATUS.md` describing work several commits old ("four commits stale", "ten commits stale", "one commit stale", "three substantive commits stale"). Two of those entries explicitly reasoned "no new gate proposed... STATUS.md's own prose is not mechanically diffable the way a generator's output is", which is true and is not what this builds: it does not diff content, it only tracks whether a material commit's own hash was ever cited anywhere in the file's prose, the same citation convention this repository's log and STATUS.md already use everywhere. New `gate_status_currency` in `ops/preflight.py`, WARN not FAIL (an ordinary short lag between check-ins is normal operation, not a defect; it only fires once at least 8 real, unmentioned commits touching `site/`, a page generator, `ops/preflight.py` itself, or one of the five startup-procedure planning documents have piled up, deliberately excluding command-deck-only and log-only churn). Fail-then-pass proved twice: `ops/tests/test_gate_status_currency.py` (17 cases against the pure `status_currency_gap()`/`_status_material_path()` logic, including that citing every hash suppresses the warning and that a 3-commit ordinary lag does not trigger it), and directly against the real repository in an isolated `git worktree add --detach`, planting 9 real unmentioned commits after `STATUS.md`'s actual last edit and watching the warning fire naming real hashes, then removing the worktree; the real, unmodified repository today reports clean (0 warnings from this gate), confirming today's actual lag sits below the threshold. Full `preflight.py` (every gate passed, 20 warnings, one new gate registered and silent), all 93 test files individually, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
| Two real defects found cold-reading `ops/build_pwa.py` and `ops/build_epub.py`, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, so per step 5d cold-read `ops/build_pwa.py`. Its precache-asset scan read only `quest.html`, but `SHELL_PAGES` also precaches `/`, which loads `assets/js/data.js` (`window.CATALOG`) that quest.html never references. An install never cached it, so the homepage worked offline with a silently empty catalog (site.js already guards `window.CATALOG` with `\|\| []`, so nothing errors, it just quietly shows nothing). Fixed by extracting assets from every `SHELL_PAGES` entry, not only the first; new `shell_assets()` function and `ops/tests/test_build_pwa.py` (4 cases), fail-then-pass proved directly (old committed file has no `shell_assets` attribute at all, `AttributeError`; fixed file 4/4). Regenerated `site/sw.js` (13 to 14 precached files, `data.js` now included). Reading further per step 5d found a second, larger issue in `ops/build_epub.py`: regenerating the real Amazon KDP deliverable (`build/6S-Success-Home-Edition.epub`, OWNER-ACTIONS.md item 14, not yet published) against the current tree produced a real diff against the committed file, not just noise. Chapters and front matter were byte-identical both ways, but the shared `book.css` was not: the committed EPUB predates Phil's own 2026-09-05 accessibility fixes (`0162eedf` 36 heading-level jumps, `819d514d` 401 real WCAG contrast failures), so the file waiting on Phil's own KDP upload still carried the pre-fix styling. `build_epub.py` was never in `gate_generator_ownership`'s regenerate-and-diff chain; the dict claiming it was "protected elsewhere" cited `gate_no_stray_dashes`, which only scans forbidden characters and cannot see staleness. Adding it to the chain surfaced a second, independent bug the fix would otherwise have made permanent: `dc:modified` was derived from source file mtimes, and every checkout in this repository's own sandboxes stamps every source file with the identical checkout-time mtime (proven directly: every chapter file and the front matter file identical to the second), so a fresh regenerate-and-diff would have reported drift on every single run regardless of real content, the same depth=1-clone-shaped bug `build_feed.py` hit a day earlier. Fixed by replacing the mtime read with an explicit `EDITION_DATE` constant, bumped by hand alongside a real content change. Regenerated the EPUB (32/32 of its own internal structural checks pass), verified two consecutive builds byte-identical, added `build_epub.py` to `GENERATOR_OWNERSHIP_CHAIN`, removed the now-redundant `GENERATOR_PROTECTED_ELSEWHERE` entry. Regenerating `site/sw.js` also required a `site/build-id.txt` refresh (`ops/build_id.py`), caught by preflight's own `build-id` gate on the first rerun rather than missed. Full `preflight.py` (every gate passed, 21 warnings, all pre-existing sandbox limitations), new test plus `test_duplex.py`, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched; the book itself is not yet live (Phil has not run OWNER-ACTIONS item 14), so this closes a gap before it ships wrong rather than fixing a live customer-facing defect. IndexNow not applicable, no site page content changed. |
| The report Phil reads four times a day was telling him 30 items were operator-actionable when they were really blocked on him or on another blocked item; fixed at the parser and at the source, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, GitHub held 8 open issues (all decision or blocked-on-art, unchanged), no mail credential, no egress to the live site, so per step 5d cold-read `ops/roadmap_report.py`, the generator behind the live, credentialed, four-times-daily `.github/workflows/roadmap-report.yml` email. `backlog_next()`'s "waiting on Phil" check was a bare `"Phil" in cells[4]` (the Owner column), so a row reading "blocked on 2.1" (2.1 is itself a Phil row), "operator, needs 1.1", "conditional on 3.4" or "needs traffic" never matched and printed in "NEXT IN THE QUEUE, ordered by dependency not appeal" as something the operator could just go do. Re-derived directly against the live `BACKLOG-2026-H2.md` rather than assumed: 14 of the 37 then-open rows were misclassified this way. Fixed with a new `row_is_waiting()` matching dependency language (`blocked`, `needs \d`, `conditional`, `owner-actions`, `spending decision`) alongside the literal name. Also fixed at the source, not only patched around in the parser: row 1.5 (Search Console) said Owner "operator" while `OWNER-ACTIONS.md` item 1a shows Search Console has never been verified and needs Phil's own paste, corrected to say so; and rows 1.2, 1.4 and 1.6 each carried six pipe-delimited cells against the section's five-column header (an old authoring slip that had shifted Est into the Owner slot for 1.4 and would misrender the table on GitHub too), merged back to five. New `ops/tests/test_roadmap_report.py` (10 `row_is_waiting` cases plus a live-file column-count check and the real row-1.5 regression), fail-then-pass proved directly (`git stash` on the fix, `AttributeError: no attribute 'row_is_waiting'`, clean after). No new `preflight.py` gate: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file, and the fix is structural. Verified the real report end to end with `--allow-partial`: "DECISIONS WAITING ON YOU" now correctly lists 1.5/2.2/2.7 among others, and "NEXT IN THE QUEUE" shrank from 30 falsely-actionable rows to the 7 that genuinely are. Full `preflight.py` (every gate passed, 20 warnings, all pre-existing sandbox limitations), full `ops/tests/test_*.py` suite, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page changed, IndexNow not applicable: this is an internal owner-facing report, not a published page. |
| `ROADMAP-2026-2029.md` corrected its own "nine days old" claim in one paragraph and repeated the original wrong number in the next; fixed and gated, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, and the standing "6-mention" `ops/*.py` cold-read handoff traced out as genuinely exhausted (all 15 named files already read, run and closed by prior cycles today or earlier this week, verified by reading each file's real investigation history rather than trusting list membership), so read a strategy document cold instead of repeating that sweep. Section 2 of `ROADMAP-2026-2029.md` carries "Corrected: the site is not nine days old... eighteen days as of this review," but section 3's Horizon 1 paragraph, one screen down, still read "the site is nine days old" (now 22, and counting), the exact claim the correction three lines above exists to retire, the same copy-vs-control shape `gate_roadmap_prices_current` already polices one section over in the same file. Fixed by rewording Horizon 1 to cite dated arithmetic (22 days as of 2026-09-11 against the stated first analytics day) instead of an undated, unverifiable number. New `roadmap_site_age_drift()`/`gate_roadmap_site_age_current` in `ops/preflight.py`: re-derives the day count from the doc's own "First analytics day" line on every run, fails on a mismatch, warns once the as-of date goes more than 35 days stale, and fails closed if no dated claim exists at all to check against. `ops/tests/test_gate_roadmap_site_age.py` (8 cases), fail-then-pass proved directly: `git stash` on the real pre-fix file reproduced the "no dated claim" failure, restored clean after. Full `preflight.py` (every gate passed, 21 warnings, all pre-existing sandbox limitations), all test files via `gate_tests()`, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents) all clean after. No price or product touched, no site page changed, IndexNow not applicable. |
| A diagnosed pilot zone's own root cause was silently unlinkable, on two separate causes, because the article lookup `cause_reading()` reads was built from the wrong list; found by reading `ops/link_graph_report.py`'s own output rather than another cold-read, fixed and gated, 2026-09-11 operator | Every unblocked row in sections 2-6 was again done or Phil-gated, and the standing 5-mention `ops/*.py` cold-read tier has come back clean on repeat passes for days (every file in it re-verified clean at least twice this week, per the last several log entries), so this pass tried a different instrument: `ops/link_graph_report.py --detail articles`, unrun this way before. One article, `why-you-have-to-dig-for-what-you-need`, sat at exactly 1 inbound link (the articles index) against a next-lowest of 2, the same "orphaned real article" shape this file's own comments already document fixing twice before (2026-09-01). Traced rather than assumed: `root_causes.py` names this article as KC-004's (EXCESS MOTION) own explainer, and two diagnosed pilot zones (Kitchen Upper Cabinet Zone, Kitchen Lower Cabinet and Cookware Zone; a third, Kitchen Sink and Dishwashing Zone, surfaced once the fix ran) carry KC-004 in a real friction branch, so `cause_reading()` should have linked it there. It never did: `_ARTICLE_BY_SLUG`, the lookup `cause_reading()` reads, is built strictly from the general `ZONE_READING` list, and this article had been deliberately kept out of that list (2026-09-01, to avoid duplicating "too many steps" on all 114 zone pages, a decision about the general block that a later comment mischaracterized as covering KC-004 too). Reading `_ARTICLE_BY_SLUG` also surfaced a second instance of the identical shape: `root_causes.py` maps RC-015 (UNRESOLVED DECISION) to `why-mail-piles-up-by-the-door`, a `ZONE_SPECIFIC_READING` article wired only to `entryway-the-landing-spot`, but a second, different Entryway zone (`entryway-the-bench-or-console`) also carries RC-015 in a real friction branch and had no path to that link at all. Fixed with a new `_CAUSE_ONLY_READING` list and a `_CAUSE_ARTICLE_BY_SLUG` lookup built from `ZONE_READING + _CAUSE_ONLY_READING`, used by `cause_reading()` only; `_ARTICLE_BY_SLUG` itself is untouched on purpose, because `ops/preflight.py`'s M5 gate reads it as "the set of article slugs `general_reading()` actually chooses among" and widening it would have added an article to that gate's floor/ceiling pool that `general_reading()` can never satisfy, a new false failure rather than a fix. Regenerated `site/zones/`: the three Kitchen zones and `entryway-the-bench-or-console` gained the correct link (the pre-existing zone-specific dedupe already prevents a duplicate on `entryway-the-landing-spot`, verified: that page did not change), plus the expected knock-on reshuffle on a handful of non-diagnosed zones whose `general_reading()` picks depend on diagnosed-zone usage counts, which shifted once the three Kitchen zones' own picks changed. `link_graph_report.py` confirms the fix: articles' "thin (exactly 1 inbound)" count went 1 to 0. New reachability check added to `gate_root_cause_articles_current` (`ops/preflight.py`): for every cause `root_causes.py` maps to a real article, the slug must actually resolve through `cause_reading()`'s own lookup, not only be non-`None`, the mechanical half the existing docstring-comparison check could not do. `ops/tests/test_gate_root_cause_articles_current.py` extended 5 to 6 cases (the new case cuts the real `_CAUSE_ONLY_READING` entry out of the file, same technique as the existing docstring-drift cases, since the gate reloads `build_zone_pages` fresh from disk on every call and an in-memory monkeypatch would not survive that), fail-then-pass proved directly, and separately reproduced the real pre-fix defect in an isolated `git worktree add --detach` (the gate named both EXCESS MOTION and UNRESOLVED DECISION as unreachable against the original committed files). Full `preflight.py` (every gate passed, 22 warnings, one more than before: `build-id` warned once on this cycle's own uncommitted `site/` changes, cleared once staged, not a new defect), all 100 `ops/tests/test_*.py` files individually by exit code, `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. `ops/build_mobile_corpus.py --check` confirmed no drift: related reading is a site-only concern, not part of the mobile corpus. No price or product touched, no new page. IndexNow `--changed` run, correctly reported UNCHECKED (no egress from this sandbox); the 13 changed zone pages are recorded and will announce on the next credentialed hourly run. |
| A live approved zone hero contradicted its own zone's standard, and withdrawing it exposed two more real bugs, 2026-09-11 operator | Looked at the actual rendered picture rather than trusting a recorded verdict: `kitchen--primary-prep-counter`'s "ok" hero shows a counter covered in bowls, produce and flowers, directly contradicting its own done_looks_like ("holding only the board, the knife block or strip, and the salt... no fruit bowl... anywhere on the surface"), captioned "an illustration of the finished state," which was false for this image. Withdrew the verdict; that alone did nothing, because `ops/wire_zone_heroes.py`'s figure-removal sweep only ran with real source PNGs present, which is Phil's machine only, so a verdict withdrawn from any other environment could never reach an already-published page. Fixed `fallback_wire()` to pull an already-wired figure too. Regenerating then surfaced two more bugs the fix itself revealed: `ops/audit_pages.py`'s "first image should be eager" check flagged the page's unrelated video thumbnail once the real hero was gone (fixed: video-play thumbnails excluded); `ops/preflight.py`'s zone-art count still read the page as pictured because it checked for any `<img>`, not the hero specifically (fixed: checks the hero marker). All three fail-then-pass proved directly against the pre-fix code. `OWNER-ACTIONS.md` corrected (7 zone gaps to 8, 30 surfaces to 31, both still a free local retry, no billing). Three new test files (`test_wire_zone_heroes.py`, `test_audit_pages_hero_lazy.py`, `test_gate_pages_missing_art.py`). Full `preflight.py` (0 gates failed, 22 warnings), all 100+ test files individually (0 fail), `check_urls.py` (188/188), `audit_pages.py` (0 findings), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched. IndexNow not applicable, no new page. |

**Follow-on, found and fixed 2026-09-11, a later cycle the same day.** The withdrawal above fixed `site/zones/` but missed a second, separate consumer of the same verdict: `ops/build_quest.py` owns `site/assets/js/quest-data.js`, the payload behind the Home Quest's own symptom-entry screen (the second most visited page on the site), and nothing chains it to run after `ops/hero-verdicts.json` changes. The committed file kept naming `kitchen--primary-prep-counter` as the picture for its "the counter is never clear" symptom, the identical withdrawn image, on the identical honesty grounds, that the zone-page fix above exists to remove. Found chasing why `preflight.py --own` (not run by default) disagreed with the fast default: the generator-ownership regenerate-and-diff named `site/assets/js/quest-data.js`, `site/quest.html` and `site/sw.js` as drifted. Fixed by running the real generator chain (`build_quest.py`, `fingerprint_assets.py`, `build_pwa.py`, `build_mobile_corpus.py`); the hash change cascaded correctly into `quest.html`'s script tag and `sw.js`'s precache list and cache name, exactly as designed. New `gate_quest_data_heroes_current` in `preflight.py`, deliberately fast and always-on rather than folded into the slow, opt-in `--own` path, since this is the second time in one day a hero withdrawal shipped incompletely; checked directly against the real pre-fix file (failed by name, naming the stem and both its zone and symptom usage) before the fix and clean after. `ops/tests/test_gate_quest_data_heroes.py` (4 cases). Full `preflight.py` (0 gates failed, 22 warnings), full test suite (102 of 103 individually; the 103rd, `test_generator_ownership.py`, independently confirmed via `preflight.py --own` on the clean committed tree), `check_urls.py` (188/188), `audit_pages.py` (0 dup), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no new page.

**A2, distribution follow-on: two more written, unused channels connected to a draft mailer, and a real corpus bug found on the way, 2026-09-12 operator.** Every unblocked row in sections 2 to 6 was again done or Phil-gated, so this pass read `ops/corpus_index.py` cold, per step 5d, one of the lowest-mention files in `ops/NIGHTLY-LOG.md`. `python ops/corpus_posts.py --stats` showed 155 finished Facebook posts and 723 finished X posts, real writing, zero served for either kind, the exact "distribution beats production" gap `GOALS.md` decision rule 1 names, on two channels nothing had ever pointed a draft mailer at (`OWNER-ACTIONS.md` item 16 already closed this gap for Pinterest/Instagram; nothing equivalent existed for these two). Found and fixed on the way: `corpus_posts.py`'s `split_numbered()` stripped the trailing "(NNN chars)" sizing note in only one of the four shapes the corpus actually uses; 261 of 741 real X posts still carried it as the last line of the body, which `clean()`'s own docstring calls exactly the bar a served post must clear ("reject anything that would embarrass somebody who posted it as written"). Fixed the pattern to match all four shapes; `ops/tests/test_corpus_posts.py` extended from 17 to 18 cases, fail-then-pass proved directly against the real corpus (261 to 0). New `ops/social_drafts.py`, a thin sibling of `ops/linkedin_drafts.py`: reads the same corpus, filters X's picks to 280 characters or fewer before rotation (`corpus_posts.take()` extended with an optional `where` predicate, backward compatible, LinkedIn's own call unaffected), and combines both platforms into one daily email (`build_all()`) rather than two, so this adds one new message to Phil's inbox, not two. New `.github/workflows/social-drafts.yml`, mirroring `linkedin-drafts.yml`'s own credential-check-before-build pattern, reusing the same already-configured SMTP secrets. `ops/tests/test_social_drafts.py` (6 cases: preview never spends rotation, send does, an unknown platform raises, and every real X draft the live corpus can produce is checked directly against the 280-character limit and against the annotation regression this file exists to prevent). `OWNER-ACTIONS.md` item 18 added: create the two accounts, the workflow is already live and sending. No new `preflight.py` gate needed: `gate_tests()` already globs and runs every `ops/tests/test_*.py` file, and both new test files fail-then-pass proved directly (`git stash` on the fix: `test_corpus_posts.py` fails naming the three uncaught annotation shapes; `test_social_drafts.py` fails with `TypeError` naming the missing `where` parameter its own char-cap guarantee depends on). Full `preflight.py`, `check_urls.py`, `audit_pages.py`, `affiliate.py --check`, mobile `npm test` all clean after (exact counts in this cycle's `ops/NIGHTLY-LOG.md` entry). No price or product touched, no site page changed, IndexNow not applicable: this is an internal drafting tool and a scheduled workflow, not a published page.

**Follow-on, found and fixed 2026-09-11, a later cycle the same day.** An unresolved merge conflict reached `main` in `ops/NIGHTLY-LOG.md` (a plain `git add -A` after a manual merge, bypassing `ops/ship.py`'s own conflict-marker refusal, item 61 above's exact defect class recurring through a different door). A concurrent session fixed the file and added `.githooks/pre-push`, refusing any future push carrying a marker, before this cycle could push its own fix; discarded the redundant edit and fast-forwarded onto theirs rather than duplicate. Verified the new hook actually works rather than trusting its own commit message: `.githooks/pre-push` was committed at mode `100644` while its sibling `.githooks/pre-commit` is `100755`, and git silently skips a non-executable hooksPath hook, so the control was inert on this and every fresh clone the same day it was written. `gate_hooks_enabled()` already checked exactly this for `pre-commit` but was never widened to cover a second hook. Fixed the file mode and widened the gate to check every present hook by name, one shared warning. New `ops/tests/test_gate_hooks_enabled.py` (7 cases), fail-then-pass proved via `git stash`: the old gate returned no warning at all for the pre-push-not-executable case, and mis-named only `pre-commit` in the both-missing case. Full `preflight.py` (0 gates failed, warnings 21 to 20 as the real `hooks-enabled` warning this sandbox carried cleared), `preflight.py --deep`, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. One unrelated, pre-existing failure noted honestly rather than hidden: `test_audit_visual_reduced_motion.py`'s browser-capability check depends on this sandbox's own Chromium build and was not touched by this change. No price or product touched, no new page.

**PLAN-MICROZONES-DECKS-APP.md K6 closed: the Kitchen deck's only take action fired zero analytics, 2026-09-12 operator.** A same-day PM check-in (see `ops/NIGHTLY-LOG.md`) had already read `ops/build_kitchen_deck_page.py` cold and flagged K1 to K6 as unreconciled against `BACKLOG-2026-09-07.md` B1, naming K6 (the `deck_full_download`/`deck_page_view` events) as genuinely not done and handing it to the next operator by name. Verified directly: `site/kitchen-deck.html`'s "Print the 72 fronts" button (the deck's only take action; B1 shipped no downloadable PDF) called plain `window.print()` with no tracking, so every reader who took the free Kitchen deck was invisible to analytics. Fixed in the generator by wiring the button to `window.Measure.track('free-download', {what:'kitchen-deck-print', from:'kitchen-deck'})` before printing, reusing the site's existing `free-download` event (the same one the Entryway deck's PDF link already fires via `measure.js`'s `/downloads/` pattern) rather than a second, unread event name. `deck_page_view` needed no new code: Umami's own script already records a pageview for every load of `/kitchen-deck.html`, the same way every other page on the site is counted, with no per-page custom event precedent anywhere else in the codebase. New `gate_kitchen_deck_print_tracked` in `ops/preflight.py`, `ops/tests/test_gate_kitchen_deck_print_tracked.py` (4 cases), fail-then-pass proved directly against the real pre-fix page (failed by name, clean after). `PLAN-MICROZONES-DECKS-APP.md`'s K6 row and its own reconciliation note updated. Full `preflight.py` (every gate passed, 24 warnings, all previously diagnosed sandbox limitations), all 110 test files individually, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` all clean after. No price or product touched, no new page, IndexNow not applicable (existing page edited, not added). K2 (the `--deck` flag) and K4's paper-print verification remain genuinely open, correctly left for a cycle with the right tooling.

**`CURRENT_STATE_AUDIT.md` called itself a living operating artifact and had not been re-measured in 12 days, describing production as unable to take money after that was fixed; corrected, 2026-09-12 operator.** Every unblocked row in this backlog was again done or Phil-gated, and the `ops/*.py` cold-read and required-doc lanes were both confirmed exhausted, so this pass ranked a wider set of documents by nightly-log mention count. `CURRENT_STATE_AUDIT.md`, a dated audit rather than a policy document, had only one row (of 7) touched since its 2026-08-31 measurement, and its "What blocks revenue right now" section still read present tense that all six payment links were deactivated, the live catalog held 10 of 159 SKUs, and Listmonk's root URL was the blocker. All three are stale: `STATUS.md` records the links reactivated 2026-08-31, the catalog widened to 158 of 159 buyable 2026-08-27, and `GOALS.md` records the Listmonk diagnosis moved to a shared sending identity 2026-09-09. Added a dated correction banner at the top and inline on that section, pointing to `GOALS.md`/`STATUS.md` as the current source; kept the original text as the historical outage record rather than rewriting it. No new gate: a one-off Phase Zero audit tied to a past exercise, not a generator output, low recurrence risk. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. No price or product touched, no site page changed, IndexNow not applicable.

**The owner-facing mail tools all pointed "Full deck"/"Full detail" at a claude.ai artifact that turned out to be eleven days stale; fixed, and a real crash in `send_questions.py --preview` found alongside it, 2026-09-12 operator.** `ops/send_brief.py` (never cold-read before this pass, per step 5d) hardcoded a `DECK_URL` to a claude.ai artifact. Fetched the real page rather than trusting the link: "Generated 2026-09-01 07:46", still claiming $0 revenue, "production is serving an older build" (fixed weeks ago), and issues since closed or reclassified as open. `ops/send_questions.py` and `ops/status_report.py` carried the identical URL. Nothing in this environment can republish that artifact (this run's own instructions say the Artifact tool needs an interactive approval no autonomous session here can give), so the link could only ever drift further from the truth. Fixed all three to link the GitHub blob view of `EXECUTIVE-DASHBOARD-LIVE.md`, which `ops/dashboard.py` regenerates and this repository commits on every run, so the link is never older than the last push. Verifying the fix by running `send_questions.py --preview` surfaced a second, independent bug: it unconditionally built a real calendar invite via `mailer.owner()` before checking the mode, so a documented "send nothing" preview crashed with no output at all whenever `OWNER_EMAIL` was unset. Fixed by moving invite construction after the `--preview` early exit. New `gate_no_frozen_deck_link` in `ops/preflight.py`, fail-then-pass proved directly (planted the old URL, gate failed by name; planted a missing deck link, gate failed by name; clean on the real fixed files). `ops/tests/test_send_questions.py` extended 6 to 8 cases (the DECK link, and a real subprocess run of `--preview` with `OWNER_EMAIL` stripped from the environment), fail-then-pass proved against the pre-fix file (`AttributeError: no attribute 'DECK'`). Full `preflight.py` (every gate passed, 21 warnings, one fewer: `hooks-enabled` cleared), all 117 test files individually, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page changed, IndexNow not applicable: these are internal owner-facing mail tools, not published pages.

**"Page speed," named in `GOALS.md` as unblocked O1 work needing no owner gate, had never actually been measured; measured and gated, 2026-09-14 operator.** Every cold-read lane (`ops/*.py`, hand-maintained pages, `.github/workflows/*.yml`) was independently reconfirmed exhausted this pass (every file has 8+ `ops/NIGHTLY-LOG.md` mentions, every workflow file 9+), so per step 5d this pass checked a claim rather than a file: `ops/NIGHTLY-LOG.md` has zero mentions of "lighthouse" and exactly one of "page speed" (an incidental `@font-face` round-trip fix), so the "not blocked, prepared" status GOALS.md gives this lever had never been backed by a real audit. Checked directly rather than assumed: scanned all 193 real pages' `<head>` for a `<script src>` with neither `defer` nor `async`, the single biggest first-paint blocker a page can carry, since it forces the parser to fetch and run before building anything else. Found 0: every real script on the site is already deferred (`measure.js`, the Umami snippet) or placed at the very end of `<body>` (`quest-data.js`, `quest.js`, `site.js`, `photos.js`). Also checked `site.css` (14.2KB gzipped, well inside any reasonable first-round-trip budget) and image `loading="lazy"` coverage (355 of 481 `<img>` tags, the remaining 126 either explicit `eager` above-the-fold heroes or already correct by browser default). No live defect: the absence of a blocking script was real but was an accident nothing protected, not a checked fact. New `gate_head_scripts_non_blocking` in `ops/preflight.py` (pure `check_head_scripts_non_blocking()`) makes it one; `ops/tests/test_gate_head_scripts_non_blocking.py` (4 cases) fail-then-pass proved, including planting a real blocking `<script src>` directly into the committed `site/index.html`, watching the gate fail by name, and reverting. One real, scoped-out opportunity found and not attempted here: `quest-data.js` is 420KB (135KB gzipped), the whole 114-zone/12-room manual, loaded in full even though the app's own first screen (the five-symptom picker) only needs a handful of fields; splitting it to lazy-load per zone would help Time-to-Interactive on the site's second-most-visited page, but is a real change to the core conversion funnel's data path, out of scope for a single cold-read pass without dedicated performance tooling to validate it against. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed), all 137 test files individually, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no page content changed, IndexNow not applicable.

**`site/sitemap.xml` never declared a single one of the site's 896 self-hosted, already-optimised images to an image crawler; fixed, 2026-09-12 operator.** Every unblocked row in sections 2-6 was again done or Phil-gated, and every open GitHub issue was decision-labelled or blocked-on-art, so this pass worked GOALS.md's own decision rule 1 ("distribution beats production... prefer the thing that puts an existing asset in front of a person") directly rather than another cold-read. `sitemap.xml` used only the plain `sitemapindex/0.9` schema; Google's image sitemap extension (`xmlns:image`, `<image:image><image:loc>`) tells Google Image Search which photo belongs to which page without needing any account only Phil can create, the same category of zero-cost lever as the RSS feed and llms.txt fixes earlier this week. `ops/build_seo.py`'s `build_sitemap()` now emits one `<image:image>` block per URL, reading the same `og:image`/`og:description` meta tags already written for social sharing (`page_image()`), so the sitemap can never assert a different picture than the page shows when shared, and a page with no og:image (404.html, thanks.html, the retired print-and-play redirect notice) correctly gets no image block rather than a guessed one. Verified against the real regenerated file, not assumed: 187 of 188 URLs now carry an image entry, all 187 declared files confirmed to exist on disk, XML re-parses cleanly with `xml.etree.ElementTree`, two back-to-back runs byte-identical (idempotent), and `check_urls.py`/`gate_sitemap_complete`'s own `<loc>`-only regexes confirmed unaffected (the `<image:loc>` tag name never matches a bare `<loc>` search). New `gate_sitemap_images_current` in `ops/preflight.py` catches three real drift classes: the namespace declaration disappearing, a declared image pointing at a file that no longer exists, and a page's own og:image changing without the sitemap being regenerated to match; `ops/tests/test_gate_sitemap_images.py` (7 cases) fail-then-pass proved directly against the real committed file for all three. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), all 120 test files individually, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no page content changed (sitemap.xml only), IndexNow not applicable.

**The executive dashboard's own headline sentence went stale the moment a real fix landed, because nothing recorded a confirmed live deploy anywhere durable; fixed structurally, 2026-09-14 operator.** A same-day local session with real VPS/Stripe/mail access (`5bc41464`) deployed and confirmed production current by build id, then, in the same pass, edited `site/standards.html`, which moved the repo's own build id on again before ever rerunning `ops/dashboard.py`. `ops/state.json` still held `deploy_last_verdict: "stale"` from before that deploy, and this sandbox has no egress either, so simply regenerating the dashboard here would have carried that same now-superseded "stale" forward, with no way to tell "never redeployed in weeks" apart from "one file behind since an hour ago" (the exact ambiguity CLAUDE.md 0.3/0.4 warns against). `ops/deploy.py` is the only file that ever runs with real access to production, and it proved a real deploy live, then said nothing durable about it. Fixed by having it write `ops/deploy-verdict.json` (verdict, build id, timestamp) the instant it confirms a build live, and by teaching `ops/dashboard.py`'s `resolve_deploy_verdict()` to read that marker when this run has no live measurement of its own: current if the repo's build id still matches the marker, honestly "stale, N-vs-M build ids, not yet redeployed since a confirmed deploy at <time>" if it has since moved on. Backfilled `ops/deploy-verdict.json` from `5bc41464`'s own real, credentialed claim (build `497533af2aea0b51`, confirmed live) so this run's regeneration reflects it rather than the stale carry. Found and fixed two follow-on bugs while proving this: the marker branch first reused this run's own unmeasured 0/0 asset counts instead of the real carried "1 of 9," recreating the exact 2026-08-31 bug this file's own docstring warns against, fixed by routing every branch through one shared `_carried_counts()` helper; and a strict `>` timestamp comparison caused the honest note to appear once and then silently vanish on the very next regeneration (the marker's timestamp, once carried into `prev`, was no longer "newer"), fixed to `>=`. New `gate_dashboard_deploy_marker_carry_forward` in `ops/preflight.py` (7 synthetic cases: fresh matching marker resolves current; a drifted marker names both build ids and reads stale; an older marker cannot regress a fresher carry; a real live measurement always wins; the marker re-applies idempotently on repeat runs), fail-then-pass proved directly against the pre-fix `resolve_deploy_verdict()` signature (`TypeError`, no `marker` parameter existed). `ops/tests/test_deploy.py` extended 14 to 20 cases covering `write_verdict_marker()` (a confirmed deploy persists build id and timestamp; a failed or no-op check never writes one). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `test_deploy.py` (20/20), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no site page changed, IndexNow not applicable.

**LinkedIn drafts had been repeating the identical three posts to Phil every morning for 9 days, and the sibling Facebook/X mailer had never once advanced either, both silently violating `ops/corpus_posts.py`'s own "nothing repeats until the pool is exhausted" guarantee; fixed and gated, 2026-09-15 operator.** Every unblocked row in sections 2-6 was again done or Phil-gated, the 7 open GitHub issues had no new reply, and the low-mention `ops/*.py` cold-read lane had itself been independently confirmed exhausted (minimum 8 mentions across all 139 files) by several of today's own prior cycles, so this pass tried a different instrument: reading a same-day PM check-in's own handoff ("open the 3 generated drafts and read them for real... nobody has checked this specific day's actual draft content since the reflow.py fix") rather than the content itself, and asking why nobody ever had. Traced rather than assumed: `ops/linkedin_drafts.py --send` calls `corpus_posts.take(..., record=True)`, which writes `ops/corpus-rotation.json` to disk, but `git log --all` on that file shows exactly two commits, both by Phil directly, the last 2026-09-06; no automated commit has ever touched it. Neither `.github/workflows/linkedin-drafts.yml` nor `.github/workflows/social-drafts.yml` contains a `git commit`/`git push` step at all (confirmed by grep, zero hits in either file), and both ran with `permissions: contents: read`, which could not push even if a step tried. Selection in `take()` is purely deterministic on the served-set (`sorted(fresh, key=lambda x: (chapter, title))[:n]`, no date or randomness), so every GitHub Actions run starts from the same committed baseline and produces the same three posts. Confirmed this was not a theoretical gap: pulled real workflow-job step logs directly from the GitHub API for `linkedin-drafts.yml`'s scheduled (non-push) runs on 2026-09-09/10/12/13/14, every one's "Write three drafts and send them" step completed `success`, not skipped, meaning each was a genuine send of the identical batch ("Your room is not a messy person", "Borrow the logic, not the look", "It came from a car factory"); `social-drafts.yml` showed the identical shape across its 3 real scheduled sends (2026-09-12/13/14), and its rotation file carried no `facebook-post` or `x-post` key at all, meaning those two channels had never advanced even once. Fixed both workflows with a `permissions.contents: write` bump and a commit-and-push step mirroring `hourly-brief.yml`'s own already-proven pattern (`git add`, `git diff --cached --quiet || git commit`, `git pull --rebase --autostash origin main || true`, `git push origin HEAD:main || true`), gated behind the same `steps.gate.outputs.send == 'yes'` condition the send step already uses. Corrected `ops/corpus-rotation.json` itself to mark the batch Phil has already received (repeatedly) as served, via the real `corpus_posts.take(..., record=True)` call, so the fix advances rotation forward rather than repeating that exact batch one more time before finally moving. New `gate_draft_rotation_persisted` in `ops/preflight.py` (pure logic in `check_draft_rotation_persisted`), checking both workflows for `permissions.contents: write` and a step that references `ops/corpus-rotation.json` together with a `git push`, so a future edit that drops either half regresses loudly instead of silently; `ops/tests/test_gate_draft_rotation_persisted.py` (6 cases: both fixed, permission missing, persist step entirely absent, a push step that pushes the wrong file, a missing workflow file, and the real committed files) fail-then-pass proved directly (`git stash` on the two workflow files reproduced both real pre-fix failures by name). Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits) after. No price or product touched, no site page changed, IndexNow not applicable: this is a scheduled-mailer persistence bug, not a published page. Not verifiable from this sandbox at the time: an actual GitHub Actions run of the new commit-and-push step (no workflow-trigger access here); correctness verified by direct code and YAML review plus the fail-then-pass gate, not by watching a real run succeed, and flagged honestly rather than claimed. **Verified live, 2026-09-15 PM check-in:** the next real scheduled run of each workflow did push. `linkedin-drafts.yml`'s 15:16:28 UTC schedule run committed `09555683` ("LinkedIn drafts: advance rotation", author `github-actions`) three new served ids into the `linkedin-post` list. `social-drafts.yml`'s 17:43:17 UTC schedule run committed `14a9498c` the same way into both `facebook-post` and `x-post`, the two channels that had never advanced before this fix. Confirmed by reading the real commits and their diffs, not inferred from a green run: the fix is holding in production.

**The three P0-trust Stripe checks (price claims on the live checkout page, duplicate products per SKU, the account's own public business identity) had read "UNCHECKED, not clean" in every sandbox this project has ever run in, despite the credential sitting one workflow away; wired in, 2026-09-15 operator.** Handed off unstarted across three prior same-day PM check-ins (`ops/NIGHTLY-LOG.md`): `gate_stripe_price_claims`, `gate_stripe_one_product_per_sku` and `gate_stripe_brand` in `preflight.py` each need a real Stripe credential this operator sandbox has never held; `.github/workflows/hourly-brief.yml` already carries `STRIPE_SECRET_KEY` and real egress, proven by `check_live_links.py`'s own wiring into it on 2026-09-09, but nothing had extended that pattern to these three checks. Confirmed the gap directly before touching anything (grep both directions: none of the three imported into `ops/hourly_brief.py`, and `hourly_brief` referenced nowhere in the three Stripe files), so this was genuinely unstarted, not merely unfinished. Extracted `stripe_catalog.price_claim_gaps()` out of the preflight gate's own inline logic so both the gate and the new hourly-brief wiring call one implementation rather than two that could drift; `stripe_dedupe.duplicates()` and `stripe_brand.check()` already existed as reusable functions and needed no change. Added three summary functions to `ops/hourly_brief.py` (`price_claims_summary`, `duplicate_sku_summary`, `brand_summary`), mirroring `payment_link_summary()`'s own OK/UNCHECKED/problem shape, wired into `build()`: a real fabricated price, a real duplicate SKU, or a real Stripe account with more than one active product now reaches the mail SUBJECT line, not only the body, the same reasoning `payment_link_summary()` used for a dead payment link. New `gate_hourly_brief_stripe_checks` in `preflight.py`, mirroring `gate_hourly_brief_payment_links`'s own shape: proves all three summaries read OK or UNCHECKED with no credential here, proves each correctly reads a real problem when its underlying library call is stubbed with one, and proves a fabricated price reaches the real `build()` SUBJECT line. Fail-then-pass proved directly (stubbed `hb.price_claims_summary` to always return OK, watched the gate fail by name, reverted). No workflow YAML edit needed: the credential was already there. Full `preflight.py`, run twice, clean and isolated the second time after the first run showed two failures (`fingerprints`, `test_affiliate.py`/`test_gate_page_ownership_registry.py`) that did not reproduce when run standalone or in a second isolated run, both gate names have no prior history of flaking and the same two files pass individually every time; diagnosed as a race from running other test invocations concurrently with that background run, not a real defect, and not reported as one. Not verifiable from this sandbox: an actual GitHub Actions run of the new checks against the live Stripe account (no credential here); correctness rests on direct code review, the fail-then-pass gate, and the already-proven `check_live_links.py` precedent. No price or product touched, no site page changed, IndexNow not applicable.

**Phil's own `0ab37390` (the Quest watch-video/finish-picture commit) made the executive dashboard's "dead links" count read 1 instead of 0; false alarm, fixed at the checker, 2026-09-16 PM check-in.** Step 2's own `preflight.py` run failed on arrival (`mobile-corpus-current`, `tests`): a concurrent session (`b2bdf921`) closed that half independently before this cycle's own push landed, confirmed by rerunning preflight clean after rebasing onto it. Separately, `ops/dashboard.py`'s `_count_dead_links()` only recognised one JavaScript shape for "this href="#" gets a real target before a visitor can click it": the literal one-liner `$("#id").href = ...`. `quest.js`'s new watch-video row uses a two-step shape instead (`var watchLink = $("#c-watch-video"); ... watchLink.href = ...`, needed because the same reference also toggles `.hidden`), which the heuristic could not see, so a live, correctly-wired link (Phil's own commit verified it in a real browser) read as "1 dead links" on the dashboard's product-readiness table, a checker false positive rather than a real defect. Widened `_count_dead_links()` to also recognise the var-then-`.href` shape; verified directly, not assumed: an isolated-root proof shows a genuine dead link (no script reference at all) still counts as 1, and the real `quest.js` pattern now counts as 0. New `ops/tests/test_dashboard_dead_links.py` (5 cases: a real dead link, the original direct-assignment shape, the new indirect shape, a same-named variable that must not mask an unrelated dead link, and an id-less `href="#"`), fail-then-pass proved by lifting the pre-fix function signature out of source the same way `test_dashboard_prev_state_fallback.py` already does. Full `preflight.py` clean after (every gate passed, 24 warnings, all previously diagnosed sandbox limits). No price or product touched, no site page changed, IndexNow not applicable.

**A live, silent dead button on the Home Quest's install prompt, found continuing the previous cycle's own named handoff (install-prompt flow, symptom-skip escape hatch) by driving both in a real headless browser rather than reading the source; fixed and gated, 2026-09-19 operator.** Drove the symptom-skip escape hatch (`#sym-other`, "or show me the house instead") first: correct in every branch tested, no defect. The install prompt was not: `quest.js`'s own comment already says the common case out loud, "the browser fires this whenever it likes, which on a cold landing is immediately," and the handler correctly holds `#go-install` hidden until a first win exists by returning early when `isFirstRun()` is true. But the click handler that calls `ev.prompt()` was wired only inside that same early-return branch, so a cold-landing prompt (the case the comment names as the common one) hit the `return` before the click listener was ever attached. `applyFirstRunGate()` later sets `.hidden = false` once real progress exists, exactly as designed, but nothing then wires a click handler: a visitor who reaches the returning-visitor start screen and taps "Install the app" gets no prompt and no error, the same "renders but does nothing" shape as the cart tile A6 already found and fixed elsewhere on this page. Confirmed live before fixing: dispatched a synthetic `beforeinstallprompt` before any progress existed, finished a full zone, returned to the start screen, and the now-visible button called the mocked `ev.prompt()` zero times. Fixed by factoring the click-wiring into one `wireInstallButton()` function called from both the event handler and `applyFirstRunGate()`, guarded by a single `installWired` flag so it attaches exactly once regardless of which call site gets there first. New `ops/tests/test_quest_install_button_wired.py`, fail-then-pass proved directly against the real committed file (`git stash` on `quest.js` alone reproduces the exact pre-fix symptom, `promptCalled: 0`, restored and reran clean, `promptCalled: 1`). `gate_tests()` already globs every `ops/tests/test_*.py` file, so no separate `preflight.py` gate registration was needed. No generator owns `quest.js` (hand-authored, confirmed by grep). No price or product touched, existing page's script corrected not added; IndexNow not applicable.

**`ops/corpus_index.py` cold-read closed, and a fabricated-testimonial near-miss caught on the way; fixed and gated, 2026-09-22 PM check-in.** Handed to the operator by name across three prior same-day PM check-ins (02:4x, 03:1x, 03:4x); nobody had picked it up, so this cycle did it directly rather than hand off a fourth time. `ops/generated_products.py` (the paired file) read clean: 149 generated products, 6 correctly excluded as already free, every deliverable file present and a plausible size, no defect. `ops/corpus_index.py` was not clean: five real per-chapter content types (landing-page-intro, back-cover-copy, ebook-sales-copy, reader-discussion-questions, newsletter-short-teaser, 306 files, one already-mixed pair under the shared name "sales-copy") were finished prose by the module's own stated test but fell through its `KINDS` list into "other", uncounted, the identical defect class `gate_dashboard_social_units_live`'s own history names for x-post/newsletter/linkedin-article (2026-09-01) and quote/summary/takeaways (2026-09-01) and Facebook/X (2026-09-12): a kind not yet taught to the classifier is invisible to the dashboard's own "unused corpus" figure. Classified all five (`corpus_index.py`), extending `units_in()` with a `discussion-questions` rule (a plain numbered list, no bold lead) verified against the real corpus, e.g. Chapter 33's 20 real questions counted as 20, not 1. **Caught before shipping, not after:** the same defect a third time, this time worse than a zero yield. `corpus_posts.py`'s `EXTRACTORS` dict had no entry for any of the four new kinds, so they would have fallen back to `split_posts`'s numbered-`## `-heading shape, which none of them use, and 42 of 51 `back-cover-copy.md` files (over three-quarters of the merged "sales-copy" kind) carry a trailing pull-quote section under one of at least six different headings ("Testimonials", "Pull Testimonials", "Pull Testimonials (PLACEHOLDERS)", some `##`, one `###`) holding unfilled scaffolding: `[PLACEHOLDER TESTIMONIAL 1: ...]` blockquotes or `PLACEHOLDER: reader testimonial about...` bullets, explicitly marked "do not publish as real quotes." Marking the kind ready without an extractor that understood this shape would have been a live fabricated-testimonial risk the moment anything served it, not just an empty pool, so this was checked before the classifier change was shipped, not discovered after. New `split_sales_copy` cuts at the first heading whose text contains "testimonial", any level, any wording, then refuses the whole file if the word "placeholder" survives anywhere in what is left rather than trust the cut caught every shape (3 of 51 files still fail that net, on a pre-heading disclaimer line, and are correctly dropped rather than served with the disclaimer attached); `split_short` and `split_questions` cover the other three kinds. Verified directly against the live corpus, not assumed: `cp.pool()` on all four new kinds finds 0 files anywhere containing the word "placeholder", 5,243 total ready units (was 4,408), 4,823 usable posts across all fourteen kinds (was 3,999 across ten). `ops/tests/test_corpus_posts.py` extended 18 to 27 cases, fail-then-pass proved directly (the new cases raise `AttributeError` against the pre-fix module, clean after). `gate_risks_evidence_current` correctly failed on the first `preflight.py` run after the corpus count moved, naming the exact stale citation (`RISK-0012`'s own `social_units=4408` evidence line); fixed to `5243`, the gate's own job working as designed rather than a defect of this change. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (189/189), `affiliate.py --check` (164 documents) all clean after. No price or product touched, no site page changed: this is corpus classification and an internal drafting-pipeline extractor, not a published page. IndexNow not applicable.

---

## 1b. Measured this week, awaiting a decision

**23 catalogue entries were explained nowhere but their own shop tile, measured 2026-09-15 evening, local session, never previously added to this file (only to `ops/NIGHTLY-LOG.md`, the "corrected source, unrederived artifact" gap section 7 names as the dominant defect class, now closed for this specific finding).** 31 of the 159 catalogue entries carried no page link; 128 of those (zone and room packs) were fixed the same evening by linking the free page that already explains each. The remaining 23 had no such page anywhere on the site: all 15 situation kits ($14), all 6 area bundles ($16), the $49 Complete Digital Bundle, and Corporate Lean 6S (which has its own quote page, the one honest exception). The few of the 23 that were named elsewhere were named only in passing (a sentence on `deck.html`, a list line on `resources.html`), so linking to those pages would have sent a buyer to a page about something else, which is not a fix. This was a content decision, not a linking one: either each kit/bundle earns a page of its own (what is in it, which rooms/zones it covers, who it is for), or that part of the catalogue line should shrink. GitHub issue #32.

**21 of the 23 resolved 2026-09-22, operator, by shrinking rather than authoring: `DECISIONS.md` D-023 retires the 15 Situation Kits and 6 Area Bundles.** `REVIEW-COMMERCE-2026-09-07.md` 1.3/1.4 found the real reason nobody had written a page for these was not neglect: both tiers charge 74-84% of the $19 whole-house pack's price for 7-20% of its content, with $0 realised revenue on either, so a page would have been marketing a weak deal rather than fixing an omission. Repository-side removal done (`data.js` 159 → 138, `ops/generated_products.py`'s new `RETIRED` exclusion, full definitions preserved in `ops/retired-skus.json`); deploy/live-verify/Stripe-archival stages left open per `REVIEW-COMMERCE-2026-09-07.md`'s own C6/C7 rows, since no sandbox here has ever held a Stripe credential. **Still open: the $49 Complete Digital Bundle (`BK-BUNDLE`)**, a genuinely coherent 3-product bundle with real internal logic (book + manual + print pack, $8.99 cheaper than buying separately) that still has no page of its own; unlike the retired tiers this one is worth writing up, not shrinking. Issue #32 not yet closed, since one of its two findings remains.

---

## 2. Now: micro zones and the web app

Ordered by evidence strength, then by whether a stranger ever sees it.

| # | Item | Why now | Effort | Accept when |
|---|---|---|---|---|
| **A1** | ~~Build the diagnostic half of the micro zone model~~ | We prescribe for 114 zones. Root cause is mapped for **12 of 114** as of 2026-09-07 (M3, both halves: Kitchen 7 + Entryway 5, `PLAN-MICROZONES-DECKS-APP.md`; gated by `gate_diagnosis_authoring`). `CLAUDE.md` section 6 says diagnose before prescribing, and the product mostly still does the opposite | 3d, 2.5d spent | **M4 done 2026-09-07, operator.** The 12 pilot zone pages now render the diagnosis block above the six passes, one FAQPage entry per friction, and cause-chosen related reading, gated by the new `gate_diagnosis_rendered`. Not yet checked against the live domain (`CLAUDE.md` 0.3, no egress from this sandbox). M6 (the remaining 102 zones) is explicitly gated on a 21-day read of this, not to start early. **Regression found and fixed 2026-09-08, this operator:** M4's cause-chosen swap had silently dropped three hand-authored `ZONE_SPECIFIC_READING` links (the key article and mail article off `entryway-the-landing-spot`, the junk-drawer article off `kitchen-the-utensil-and-utility-drawers`), found by running the new `ops/link_graph_report.py` and seeing all three fall to a single articles-index inbound link. `cause_reading()` now supplements the zone-specific entry instead of replacing it, still capped at 5 for `gate_diagnosis_rendered`'s own 3-to-5 range. Gate extended to check this specifically, proved fail-then-pass. |
| **A2** | ~~Differentiate the 19-link "Related reading" block~~ | **12 of 114 zone pages no longer carry it, as of M4** (2026-09-07: replaced with 3 to 5 cause-chosen links, no two of the 12 identical). **102 of 114 still do.** The generator already supports the fix for a diagnosed zone; the remaining 102 have no `diagnosis` yet (M6, gated) | 0.5d | **Done 2026-09-09, operator.** Same fix as `PLAN-MICROZONES-DECKS-APP.md`'s M5, this row's full duplicate: `general_reading()` in `ops/build_zone_pages.py` scores the 19 general articles against each of the 102 non-diagnosed zones' own real published text (no diagnosis authored, none needed), no two zones share a set, gated by `gate_general_reading_differentiated`. Full account in the plan doc's M5 row and in `ops/NIGHTLY-LOG.md`. |
| **A3** | ~~Surface the FAQ answers to readers~~ | Nine questions per zone exist **only inside JSON-LD**. Invisible to humans, and structured data is meant to reflect visible content | 0.5d | **Done 2026-09-07, Phil (`9b7f5abb`).** `faq_html()` in `ops/build_zone_pages.py` renders a plain `<dl>` of every question and answer visibly, ahead of `related_reading()`. Verified this cycle against the served output while working M4: all 114 zone pages carry the visible block, and the 12 diagnosed pilot zones now also carry a FAQPage entry per friction on top of it. |
| **A4** | ~~Rebalance the zone page against its own query~~ | ~100 words answer "how to organize" in a 2,400-word page; 481 are supply list and 572 cleaning procedure | 1d | **Found already done 2026-09-08, operator.** `short_answer()` (commit `ccb8fdbc`, Phil, the same day this row was written, several hours later) already renders the 6-step "how to organize" summary as the second content block on every zone page, ahead of both the supply list and the full cleaning procedure. Checked live rather than assumed: all 114 `site/zones/*.html` pages carry it there, none behind the supply list. This row had gone stale the moment the fix landed; nobody had told it. New `gate_zone_short_answer_above_fold` in `preflight.py` (plus `ops/tests/test_gate_zone_short_answer.py`, 5 cases) so a future edit to `zone_page()` cannot silently move the answer back behind the supply list, proved fail-then-pass on two planted regressions (answer stripped, answer reordered behind supply). The supply list and cleaning procedure stay exactly where they are, after the six passes: `zone_page()`'s own comment explains why (a reader who meets the kit list after starting has already made the trip they were trying to avoid), and moving them earlier again would undo that, not honour this row. |
| **A5** | ~~The app's first thirty seconds~~ | A symptom question in household words before any room name, then zone, then cause, then a two-minute action with a victory condition | 2d | **Done 2026-09-08, operator.** `ops/build_quest.py` now emits five real symptoms (`SYMPTOM_PICKS`, one per zone spanning Entryway and Kitchen) straight from content.json's own `diagnosis` frictions and `root_causes.py`, nothing paraphrased. `quest.html`/`quest.js` add a new default first screen (`#symptom-step`): "What is annoying you right now?", five household-worded options plus "show me the house instead" (the old single-button screen, kept as the no-JavaScript fallback and the bail-out). Picking one shows `#cause-step`: the zone, the real branch answer as the cause, the zone's reviewed illustration, and the `first_15` two-minute action with its victory line. Starting it opens card one of that zone's run with the purpose/instruction/victory swapped for that two-minute action (badge, zone name and Done stay; count, session length and teach line are hidden, per the spec's "nothing else on the screen"); every card after the first renders exactly as before. New events `quest-symptom-picked`, `quest-symptom-start`, `quest-first-victory` (no free text, no zone name, per `CLAUDE.md` section 47). Verified: `ops/tests/test_quest_flow.py` rewritten to drive the real flow end to end in headless Chromium (symptom pick to cause screen to simplified card to done to finish to reload) and passes; three screenshots at 390px confirm the screens render correctly. New `gate_quest_symptom_entry` in `preflight.py`, proved to fail on a planted markup regression and a planted empty-data regression, passes clean on the real files. `preflight.py` fast and `--deep` both clean, all 44 `ops/tests/test_*.py` files, `ops/build_mobile_corpus.py` regenerated and mobile `npm test` (4 suites) all pass. |
| **A6** | ~~Fix the four app defects QA found~~ | "never sent anywhere" while eight events POST; cart unreachable (`data-add-sku` on zero pages); 7 contrast failures at 2.28:1 our auditor misses because it ignores CSS `opacity` | 1d | **Done 2026-09-08, operator.** Cart removed entirely (it was unreachable, not fixed to be reachable: no page could ever add an item, and `renderProduct()`'s own comment already treated the branch as dead). `quest-first-start`'s zone field dropped, the one event that still named one despite the page's "no zone is named" line. `ops/audit_visual.py` now multiplies through CSS `opacity` (`effOpacity()`); the 7 real failures (`.filters button .n` / `.chips .n` at 2.28:1) fixed in `site.css` and `ops/build_deck_gallery.py`; proved by replanting the exact regression and watching contrast failures jump from 0. Also found and fixed while verifying: `privacy.html` claimed the contact form discards input same as the newsletter form; false since 2026-08-17, split into what each form actually does. `audit_visual.py --all`: 0 findings across 190 pages. |
| **A7, 2026-09-15, operator: a live violation of CLAUDE.md's own affiliate rule, "never recommend a storage product before the reader has done Sort."** Every unblocked row above was again done or Phil-gated, GitHub's 7 issues unchanged, no fresh mail, so this pass cold-read a low-mention `ops/*.py` file (`ops/zone_supplies.py`, 10 mentions, the same tier the last several cycles' cold-reads have come from) rather than repeat an exhausted sweep. Checked live rather than assumed: `site/zones/*.html`'s "What to have on hand before you start" kit block rendered every needed/conditional product regardless of family, above all six passes including Sort. The catalogue's own "Storage & Organization" family (bins, shelf risers, drawer dividers, hangers, shoe racks) had 570 product rows across 112 of 114 zones in that block, most already live `rel="nofollow"` retailer links from `ops/product_links.py`. Home Office File Storage's "Fireproof Document Box" (Home Depot, tagged for the Straighten pass) was the clearest instance: a genuine storage container, linked, recommended before Sort on a real, currently-served page. Fixed at the source, not the page: `ops/zone_supplies.py`'s `kit()` now buckets `STORAGE_FAMILY` items into `storage_needed`/`storage_maybe` separately from `needed`/`maybe`; `render()` only emits the non-storage buckets (unchanged position, before Sort); a new `render_storage()` emits the storage buckets in their own "What to store it in, now that you know what you are keeping" section, and `ops/build_zone_pages.py` wires it in right after the Sort pass, before Straighten. Each block keeps its own disclosure, scoped to its own links (the pre-Sort disclosure no longer fires on a storage-only zone, which would have been a promise that block did not keep); the storage block's copy of `ops/affiliate.py`'s disclosure `id` is renamed (`affiliate-disclosure-storage`) so two blocks on one page never collide (checked: nothing anchors to the id). `schema()`'s HowTo JSON-LD (no reading order) still lists storage Core items; `room_kit()`/`render_room()` (room pages have no Sort heading to be "before") merge both buckets back into one list, unchanged in content from before this fix. Regenerated all 114 zone pages with `ops/build_zone_pages.py`; confirmed live: the Fireproof Document Box now renders after `id="sort"` and before `id="straighten"`, no page carries a duplicate `id="affiliate-disclosure"`, 112 of 114 zones now carry the new section. New `gate_no_storage_before_sort` in `preflight.py`, re-deriving storage-family names from the real catalogue and checking the real shipped HTML (not the module's own bucketing logic) for any of them before `id="sort"`; fail-then-pass proved directly against the real file (planted the exact old defect shape, watched the gate fail by name citing the file and product, reverted via regeneration, reran clean: "69 storage-family product name(s) checked against 115 zone page(s): none recommended before Sort"). Full `preflight.py` (every gate passed, 24 warnings, one more only because this session's own uncommitted diff triggers the pre-existing `build-id` staged-change warning), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents, "every page with links discloses above them") all clean after. No price or product touched; IndexNow not applicable (114 existing pages edited, none added). |
| **A8, D5: shrink the retailer-link supply block and move it below the method, 2026-09-20, operator.** Handed to the operator by name in the 21:1x and 21:4x PM check-ins once D3/D4 both closed; `REVIEW-DISCOVERY-2026-09-07.md` section 2 D5, "Blocked on. Nothing," for the same 12-zone pilot cohort (Entryway 5, Kitchen 7). The review's own measurement: the pre-Sort "What to have on hand before you start" kit block was 481 words, the single largest block on the page, and "sending a reader to a retailer's search results is a weak destination." Verified live before changing anything: the 12 pilot pages actually ran 325 to 481 words for that block, confirming the review's own number. **Done.** New `zone_supplies.render_compact()`/`_row_compact()` in `ops/zone_supplies.py`: same data, same links, same disclosure honesty as `render()`, but each item's full "why" sentence ("Clean glass, mirrors, and polished surfaces, in the Shine and Sustain passes.") is cut to the pass names alone. `ops/build_zone_pages.py` now branches on `zone.get("diagnosis")` (the same pilot-cohort signal M4/D3/D4 already use): the 12 pilot zones get `render_compact()` placed after the six passes instead of `render()` before them; the other 102 are unchanged. A one-line pointer ("This zone's full kit list is below, after the method...") replaces the full list at the old position, answering the "discover a missing product mid-task" concern `zone_page()`'s own comment raised for the original placement, without keeping the whole 481-word block there. Regenerated: the 12 pilot pages now run 237 to 301 words for this block, all under a new 340-word gate ceiling. **Found and fixed while verifying, not after shipping:** a first version linked each pass name back to its `#id` (reusing `_why()`'s own pattern); `ops/audit_visual.py --all --mobile` caught multiple real tap targets under 44px across the 12 pilot pages (e.g. "Sort" at 25x13, "Shine" at 34x13, "Safety" at 37x13) because short words at this block's compact 13px render fail the site's own touch-target floor; the tool's own output truncated before its summary count, so no total is asserted here, only that it was non-zero and real, confirmed by name and pixel size on every instance read. Since the block now sits after the passes rather than before them, the audience has already read the pass explanations, so the pass names render as plain text instead: rerun of `audit_visual.py --all --mobile` after showed "targets under 44px: 0". New `gate_kit_compact_rendered` in `preflight.py` (pure logic in `check_kit_compact_rendered`), re-deriving the pilot cohort fresh from `content.json`'s `diagnosis` field on every run: fails if a pilot zone's kit list still renders before Sustain, if a non-pilot zone's kit list wrongly moves after the passes (D5 must stay scoped to the 12, not spread to all 114), if the block exceeds the word ceiling, or if it skips `render_compact()`'s own `kit-compact` class. `ops/tests/test_gate_kit_compact_rendered.py` (7 cases, including the real committed site) fail-then-pass proved directly: planted the pre-D5 shape (kit list before the passes) on a real pilot page, watched the gate fail by name citing the file and both the Sustain-position and passes-heading checks, confirmed clean after. Two small `.kit-pass`/`.kit-item.kit-compact` rules added to `site/assets/css/site.css` (the compact list otherwise had no block-level rule for the pass-name line and would have sat flush against the product name); `ops/fingerprint_assets.py` rerun for the new hash, `ops/build_seo.py` rerun (only the 12 pilot pages' content hashes moved; the site-wide fingerprint churn this CSS change causes on every page's `?v=` query string does not, confirmed by diffing a sample non-pilot page). Idempotency proved directly: two consecutive full regenerations (`build_zone_pages.py` + `build_seo.py`) after the fix produced a byte-identical tree. Full `preflight.py` (every gate passed except the expected `prerender-shop-current` "differs from HEAD" refusal that resolves on commit, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents, one transient `--check` failure mid-cycle traced to a concurrent session's own `_test_bad_delivered_doc.html` scratch file, gone and clean on immediate rerun), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans), `ops/audit_visual.py --all` (desktop and mobile, 0 findings after the tap-target fix) all clean after. No price or product touched, no new page (12 existing zone pages restructured, 0 added); the 190-page fingerprint-only diff from the CSS change touches every page's stylesheet version string but no visible content. IndexNow will pick up the 12 changed pages on its next successful run. |
| **A9, D1: put the direct answer first, on all 114 zone pages, 2026-09-20, operator.** Handed to the operator by name across the 22:1x and 22:4x PM check-ins once D5's CI confirmed green on both `checks.yml` #1221 and `publish-image.yml` #358 (`115946ca`); `REVIEW-DISCOVERY-2026-09-07.md` section 2 D1, "Blocked on. Nothing," and unlike D3/D4/D5 explicitly scoped to all 114, not a pilot cohort ("~2 operator-days for all 114, template-driven, reusing existing copy... safe to do now"). The review's own measurement: the first 60 words after the H1 should answer "what goes in this zone, what does not, and where it goes," and before this the opening paragraph was `purpose` alone, a one-sentence description of the zone's job with none of that; the actual list of what belongs, `done_looks_like`, first appeared three blocks down, inside the zone diagram figure. **Done.** New `direct_answer()` in `ops/build_zone_pages.py`, called for the opening `<p class="lede">` on every zone page: `"The {thing}, in the {room}. {purpose} {done_looks_like}"`, all three fields already on the page elsewhere, nothing newly authored. Read a sample across all 114 before shipping, not after: a first version tried to splice a lowercased `purpose` directly onto a hand-built lead sentence, and broke on the corpus's bare-imperative shape ("Holds the coats..." with an implied "it"), producing "In the Mudroom, holds the coats..." with no subject once lowercased. Rebuilt so `purpose` is always quoted verbatim, never rewritten, after a standalone naming sentence built only from `thing` (the search-term common noun) and the room, which has no grammar of its own to collide with. Verified against all 114 real corpus entries after: 0 grammar defects (no double punctuation, no lowercase sentence start), word count 51 to 107 (avg 72), consistent with the review's ~60-word target. The diagram, the "What done looks like" heading, the visible FAQ and the FAQPage JSON-LD all keep rendering `done_looks_like` exactly as before; nothing was deleted, per the review's own "without deleting anything." New `gate_zone_direct_answer_current` in `preflight.py` (pure logic in `check_zone_direct_answer`), re-deriving the expected opening paragraph from the real corpus via `direct_answer()` itself and diffing it byte for byte against the real shipped HTML, so a hand edit to the template or a stale build cannot ship silently; also fails if the opening paragraph is a supply-list/disclosure string instead of an answer, or grows past a 140-word ceiling. `ops/tests/test_gate_zone_direct_answer_current.py` (7 cases, including the real committed site) fail-then-pass proved directly: planted the pre-D1 purpose-only opening on the real committed `entryway-the-landing-spot.html`, watched the gate fail by name citing the file and the mismatched text, restored byte for byte (confirmed via `diff`), reran clean. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (163 documents), `fix_dashes.py --check` (0/0), `link_graph_report.py` (0 orphans), `ops/audit_visual.py --all --mobile` (0 findings across 194 pages) all clean after. `ops/build_seo.py` rerun: only the 114 changed zone pages' content hashes moved. A concurrent PM check-in session (`2bd65454`) pushed while this cycle was in progress, confirming D5's CI green and handing D1 to the operator by name; fetched and fast-forward merged before continuing, no collision. No price or product touched, no new page (114 existing pages restructured, 0 added); IndexNow will pick up all 114 changed pages on its next successful run. |

---

## 3. Now: decks

| # | Item | Why now | Effort | Accept when |
|---|---|---|---|---|
| **B1** | ~~Ship the Kitchen deck typeset and unillustrated, free, ungated~~ | 72 cards are written and gated. The art is the twice-failed part, and Kitchen is where 7 of our 12 published videos already are | 5.75d | **Done 2026-09-08, operator.** `ops/build_kitchen_deck_page.py` is a new generator (does not touch `ops/build_deck_gallery.py` or `ops/build_card_template.py`, both image-pipeline-specific and not a fit for a deck with no photography yet) that reads the already-gated `ops/cardtext/build_kitchen_deck.py` corpus and writes `site/kitchen-deck.html`: all 72 cards, grouped the way the Room card's own `how_to_play` explains the deck is played (Room, then each of the 7 zones with its 3 frictions/2 actions/1 standard, then the 4 whole-kitchen actions, the 12 shared Root Causes, the 6 Events). Each card shows its front always (family-tinted glyph panel standing in for the missing photograph, band, title, tagline, one core line) with a native `<details>` disclosure for the back, so nothing is hidden from a crawler or a keyboard user. A separate `@media print` sheet lays out all 72 fronts at true 2.5x3.5in trim size, 3 to a row on US Letter, in real CSS points against `ops/card_spec.py`'s own 7pt floor. Verified: `ops/audit_visual.py --all` on both desktop and mobile viewports, 0 contrast/heading/landmark/tap-target/side-scroll findings (one non-blocking "fonts NOT loaded" measurement race tied to the page's DOM size, reproduced and isolated to the hidden print sheet; every substantive check still completed and came back clean, and the print sheet uses only system fonts, so no real font fetch is involved, recorded honestly rather than hidden). New `gate_kitchen_deck_rendered` in `preflight.py` (pure logic in `check_kitchen_deck_rendered`, `ops/tests/test_gate_kitchen_deck_rendered.py`, 4 cases) checks the shipped page against the real corpus: every card id present both ways, and a sample field per card type byte-identical, not paraphrased. Proved to fail on three planted regression classes (missing cards, stale extra ids, drifted text) before shipping. `site/deck.html` cross-links it and its own stale "no second deck" framing corrected (the policy was about a second *illustrated* deck; this one is not that kind of deck yet). `sitemap.xml` regenerated (188 URLs), `ROADMAP-2026-2029.md`'s page count and `RISKS.md`'s `forms_dead` citation updated for the real count. No new SKU: free and ungated, same as the Entryway deck. `preflight.py` fast, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean. |

**B1 follow-on, found and fixed 2026-09-08, this operator, a fresh cycle re-reading `ops/build_kitchen_deck_page.py` cold.** The generator that shipped this page never got the whole-site wiring chain (`canonical_links`, `prune_catalog_js`, `wire_landmarks`, `wire_progressive`, `wire_measure`, `wire_pwa`, `wire_aria_current`, `build_avif.wire()`, `fingerprint_assets.py`) every sibling single-page generator already chains into its own `main()`, the exact issue #26 shape `build_kit_page.py`, `build_corporate.py` and `build_resources.py` each already found and fixed for themselves earlier this week. Its own template hardcodes the PWA block and the `site.css`/`measure.js` fingerprint as literal strings, so a standalone rerun after either asset changed would silently ship a stale hash with nothing to catch it: `gate_generator_ownership`'s own `gens` list did not even include this generator either, so the site-wide regenerate-and-diff check could not have caught drift here. No live defect today (the hardcoded values still matched; verified rerunning the generator produces a byte-identical `site/kitchen-deck.html`), but the next asset change would have shipped it silently. Added the same chain to `build_kitchen_deck_page.py`'s `main()` and added it to `gate_generator_ownership`'s `gens` list. Proved the new gate registration can fail: in an isolated worktree, planted a stale fingerprint in `site/kitchen-deck.html`, committed it, watched `preflight.py --own` correctly fail naming the file, removed the worktree. `check_urls.py` (188/188) and `audit_pages.py` clean after.
| **B2** | ~~Teach the card template the Kitchen taxonomy~~ | **65 of 72 Kitchen cards would print as identical dark cards**: `card_spec.FAMILY` holds only the Entryway families | 0.5d | **Found already done, 2026-09-08, operator.** Phil's own `8806970a` (2026-09-07 evening, same day this row was written) added the five Kitchen taxonomy words, Zone/Friction/Root Cause/Action/Standard, to `card_spec.FAMILY` with distinct colours and glyphs, reusing an existing hue for the four that map onto an Entryway concept (Friction=Problem, Action=Upgrade, Zone=Micro Zone) so the same idea looks the same in either deck. Verified by reading the module directly: all ten families now pass the file's own 4.5:1 contrast assertion at import time. This row had gone stale the moment the fix landed. |
| **B3** | ~~Reconcile the deck's card count~~ | The free deck is 89 on one page and 88 on another. `gate_deck_count` returns early whenever `build/cards-rendered/` is empty, which is every cloud run, so it has never fired where it runs | 0.3d | **Done 2026-09-07, operator.** Verified live: `deck.html`'s title, meta description, and OG/Twitter tags said 89 with no explanation while `data.js`, `shop.html` and the print-and-play page all said 88; fixed the six flat headline/meta instances on `deck.html` to 88, matching the catalogue. Left the already-correct "72 of the deck's 89 cards are drawn" progress sentences untouched (89, the full written total including the Room divider card, is the right denominator there; `deck-gallery.html` already explains the 89/88 split honestly). `gate_deck_count` rewritten to read `build/entryway-cardtext.json` (a committed, always-present corpus file) instead of locally rendered card-front PNGs, so it now actually runs in every cloud environment instead of silently returning before comparing anything; also now asserts `ops/build_deck_gallery.py`'s hardcoded `DECKS['entryway']['written']` against the real corpus, so that number can never quietly drift either. New `ops/tests/test_gate_deck_count.py` (7 cases) proves both real defect classes get caught and two genuine false-positive risks (a CSS comment mentioning "72 cards", and the honest "X of Y drawn" phrasing) do not trip it. |
**B3 follow-on, found and fixed 2026-09-12, this operator.** Every unblocked row in this backlog was again done or Phil-gated, so this pass read the homepage cold rather than another `ops/*.py` file, per step 5d, since a stranger's first content is the highest-leverage page nobody had re-read end to end since B3 shipped. The site's own homepage, `site/index.html` (hand-maintained, not generated by anything, confirmed by grep across every `ops/build_*.py`), still told every first-time visitor the free Entryway deck was "Forty six cards" that "take one entryway through all six passes": the exact retired 46-card mockup number B3 fixed everywhere else five days earlier, plus a structural mix-up borrowing the Whole House Print Pack's six-pass framing for a deck B4 had already established plays a different, eight-family diagnostic game. `gate_deck_count`'s own docstring already names "46 ... on the homepage" as the original 2026-08-30 defect it exists to catch, and `site/index.html` was already one of the four pages it scans, but the check only ever looked for a digit; a spelled-out count carries none, so the gate could not fail on the exact shape of defect it was written for. Fixed the homepage sentence to "Eighty eight cards that walk one entryway through twelve micro zones: what is wrong, the fix, and the habit that holds it," reusing `deck.html`'s own established framing rather than inventing new copy. Extended `check_deck_count()` in `ops/preflight.py` to also parse plain-English cardinals up to ninety-nine, guarded against two real false-positive shapes found while building it: the tail of an unrelated hundred-scale number (the same homepage's own honest "six hundred and eighty four cards" Print Pack tile, which must not read as a bare "eighty four") and an honest retired-number notice that names the real total nearby but outside the same clause (live, real text on `site/deck/entryway-print-and-play.html`). `ops/tests/test_gate_deck_count.py` extended 7 to 11 cases, fail-then-pass proved directly: the old spelled-out defect string fails by name, the fix passes clean, and both false-positive shapes pass clean. Full `preflight.py` (every gate passed, 22 warnings, all previously diagnosed sandbox limits), all 107 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price or product touched, no new page, IndexNow not applicable (existing page edited, not added). |
| **B4** | ~~Make a deck distinguishable from a printable pack~~ | A buyer cannot currently tell them apart. The pack gives steps for a zone; the deck works out *which* zone and *why* | 0.5d | **Done 2026-09-08, operator.** `deck.html`'s own upsell copy said the Print Pack "is the same cards for every room," which is the confusion this row names, not a fix for it: the pack's 684 cards are the six-pass instruction set (verified against `ops/build_printpack.py`'s own docstring, one card per pass per zone), the free deck's 88/89 are the eight-family diagnostic game (`card_spec.FAMILY`: Problem, Event, Tool, Habit, Win and friends). Rewrote that paragraph to say so, keeping the true physical claim (same trim size and card stock, so a mixed pile still prints and handles the same) and dropping the false content claim. Same fix in `site/assets/js/data.js`'s `PACK-HOUSE`/`DECK-ENTRY` blurbs, the one source `shop.html`'s product grid and JSON-LD both render from; regenerated both derived surfaces (`ops/prerender_shop.py`, `ops/build_product_schema.py`) and re-fingerprinted. `audit_pages.py` (190/0) and `check_urls.py` (187/187) clean after. |
| **B4 follow-on, found and fixed 2026-09-08, this operator, reading GitHub issue #31 (the gallery-vs-download design decision) for an unrelated reason.** The exact false claim B4 fixed on `deck.html` and `data.js` was still live in the one place B4's own read never reached: `ops/build_deck_gallery.py`'s "Getting it" copy, which builds `site/deck-gallery.html`. It shipped "the Whole House Print Pack is the same cards for all 114 micro zones" until this cycle. Verified independently before fixing: read `build_card_template.py`, `build_deck_pdf.py` and `concept_hero()`'s own docstring to confirm issue #31's claim that the gallery (photographic, scanned, numbered callouts) and the free PDF (a card-game template, colour band and glyph, smaller photo) really are two different visual designs, not a stale complaint. Fixed the generator's false equivalence and, per issue #31's own recommendation ("C now, A after the art is regenerated"), added the honest disclosure of the two designs to both `deck.html` and the regenerated `deck-gallery.html`. New `gate_pack_deck_distinct` in `preflight.py` scans every rendered page for the "same cards" false-equivalence phrase (whitelisting quest.html's genuine "same 684 cards" claim, which is true); proved fail-then-pass by planting the exact old defect string and watching it fail by name before confirming the real site clean. `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), all `ops/tests/test_*.py`, mobile `npm test` (4 suites), all clean after. Commented on issue #31 recording the partial (Option C) fix; left open, Option A still needs Phil's Gemini billing decision (`OWNER-ACTIONS.md` 1b). |
| **B5, GitHub issue #20 closed as superseded, 2026-09-15, this operator.** Every unblocked row in this backlog was again done or Phil-gated, so this pass cold-read the older, less-recently-touched decision issues (`#18`, `#20`, `#21`) rather than re-sweep the same ground today's other cycles already covered (`#2`, `#29`). Checked #20 against the live catalogue rather than trusted: it asked Phil to confirm a four-tier pricing ladder for a 46-card, line-art Entryway deck (free / $12 illustrated PDF / $29 printed / $34 bundle), unchanged since 2026-08-20. `site/assets/js/data.js` carries exactly one deck SKU, `DECK-ENTRY`, 88 cards, free, no paid tier of any kind. B3/B4 above already moved the card count everywhere it is customer-facing; nobody had told this decision issue or `PRICING.md`, whose section 2 repeated the same stale ladder in full, comparables and all. Commented and closed #20 as superseded (the real decision, hold paid tiers pending sales evidence, was already made by events and is correctly recorded in section 5 below); corrected `PRICING.md` with a new section 0.6 rather than deleting the historical reasoning, since it stays useful whenever paid tiers are revisited, just not against the 46-card premise. New `gate_pricing_deck_ladder_current` in `preflight.py` (pure logic in `pricing_deck_ladder_problems`, `ops/tests/test_gate_pricing_deck_ladder_current.py`, 5 cases including the real committed file) requires the correction marker to stay present and to keep citing the live card count, so this cannot silently drift again the way it did for weeks with no gate at all, unlike every sibling money-adjacent document (ROADMAP, GOALS, RISKS, STATUS all already had one). Fail-then-pass proved directly. Full `preflight.py` (every gate passed, 23 warnings, all previously diagnosed sandbox limits), all 150 test files (excluding the known-slow, unrelated `test_generator_ownership.py`), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), mobile `npm test` (4 suites) all clean after. No price, product or page touched; this is a decision-record and pricing-doc correction, not a live catalogue change. |
| **B6** | Author the 21 Kitchen micro quests `DECK-GAME-DESIGN.md` section 2 specified (the 1 to 3 minute rung measured missing from the whole deck) and wire them in | Handed off by name twice in `ops/NIGHTLY-LOG.md` (2026-09-16/17 PM check-in, "author the 21 Kitchen-only micro quest lines... nothing under `ops/` or `build/` defines a `micro_quest` field anywhere"); real product depth work matching Phil's own reprioritisation at the top of this file | 1d | **Done 2026-09-17, operator.** All 21 authored (3 per zone, `MICRO_QUESTS` in `ops/cardtext/build_kitchen_deck.py`), each grounded in that zone's own real Manual passes (`content/manual/source/content.json`), never invented, and each a single physical movement ending in something observable, per `DECK-SYSTEM.md` 5.4's own contract (e.g. Primary Prep Counter: "Lay the cutting board flat on bare counter and clear whatever is stopping it from lying flat"). Wired into `standard_card()` as a new `micro_quest` field and printed on the STANDARD card back, the one genuinely sparse back in the deck, per `DECK-GAME-DESIGN.md` section 4.1's own recommendation (the zone and event backs are already full). The generator's own `gate()` now asserts exactly 3 per standard card, none blank, none repeated across the deck, and none too long for a card back; fail-then-pass proved directly (planted a duplicate line, watched `build()` raise by name, reverted). Regenerated `ops/cardtext/kitchen-deck.json` and `site/kitchen-deck.html`; live-checked all 21 lines render verbatim in the standard cards' `<details>` disclosure. New `gate_kitchen_micro_quests` in `preflight.py` (pure logic in `check_kitchen_micro_quests`, `ops/tests/test_gate_kitchen_micro_quests.py`, 4 cases) checks the shipped page against the real corpus the same way `gate_kitchen_deck_rendered` already does for the fronts, so a hand edit to the template or a generator that stops reading `micro_quest` cannot ship silently; fail-then-pass proved directly against the real committed page (stashed the fix, watched the gate fail naming all 7 standard cards and their exact missing lines, restored, reran clean). `DECK-GAME-DESIGN.md` section 7 item 1 marked done rather than left as an open ask to Phil: this is reversible content on an existing free page, no price or product touched, `CLAUDE.md` 0.5's GREEN tier, not one of its actual gates. Items 2 to 4 (Momentum, the deleted Upgrade/Tool cards, the side-quest edges) are unchanged and out of scope for this pass, per `CLAUDE.md` 0.1's "finish one thing." Full `preflight.py` (every gate passed, 24 warnings, the one new one being this session's own uncommitted diff, all others previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0), `ops/audit_visual.py --all` (193 pages, 0 findings, one already-documented non-blocking font-load race on this same page's hidden print sheet) all clean after. No price or product touched, no new page; IndexNow will pick up the changed page on its next hourly pass. Mobile `npm test` not run: nothing under `mobile/` was touched. |
| **B7, 2026-09-17, operator.** `DECK-GAME-DESIGN.md` section 4.2/item 4, Kitchen half | "the single cheapest combinatorial gain available, because the content is already authored": the Kitchen deck's `related` field, on 53 of 72 cards, was never printed anywhere | 0.5d | **Done, scoped to the Kitchen deck.** Checked the Entryway half of this recommendation against reality before touching either: `ops/build_card_template.py`'s own docstring already measured the printed card back as full at the 8.5pt floor (158px free, 193px needed for one more labelled block) and cut `related_path` for exactly that reason, a real constraint the design doc's "printed on nothing" framing had not checked; this sandbox also has no `build/cards-rendered` to attempt or verify an image-based render. Left for Phil or a future session with the local image pipeline, recorded in `DECK-GAME-DESIGN.md` section 7 item 4. The Kitchen deck's own equivalent gap was fully reachable: all 18 ACTION cards were missing `related` even though the zone, standard and root-cause edges already existed as plain fields (`a["zone"]`, `a["causes"]`) on every one, just never grouped or printed. Added `related` to all 18 (the 14 zone actions get `zone`+`standard`+`root_causes`; the 4 whole-kitchen actions, KA-015 to KA-018, get only their real root causes, no zone invented for them) and to KR-001 (a range, the same table-of-contents shape ER-001 already uses in the Entryway corpus, not a graph edge), bringing the field to 72 of 72. `gate()` in `ops/cardtext/build_kitchen_deck.py` extended to require every card carry `related` and to validate every action's zone/standard resolve to a real card id. Rendered on `site/kitchen-deck.html`'s ACTION CARD back as real anchor links, reusing the exact "link to a real card by title" pattern the ROOT CAUSE card back already used for its own `related.actions` field, nothing new invented: "If this keeps happening" for root causes (all 18 cards), "Part of:" for the zone and standard (the 14 zone-tied cards only). New `gate_kitchen_action_related` in `preflight.py` (pure logic in `check_kitchen_action_related`, `ops/tests/test_gate_kitchen_action_related.py`, 5 cases) checks the shipped page against the real corpus the same way `gate_kitchen_micro_quests` already does for the standard cards' micro quests; fail-then-pass proved directly against the real committed page (stashed the page-generator fix, regenerated with the old template against the new corpus, watched the gate fail naming all 14 zone-action links by id, restored, reran clean). Full `preflight.py` (every gate passed, 24 warnings, all previously diagnosed sandbox limits), `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents), `fix_dashes.py --check` (0/0), `ops/audit_visual.py --all` (193 pages, 0 findings, the same already-documented non-blocking font-load race on this page's hidden print sheet) all clean after. No price or product touched, no new page; IndexNow will pick up the changed page on its next hourly pass. Mobile `npm test` not run: nothing under `mobile/` was touched. |

---

## 4. Now: images and video

The sequence changed this week. **Image *understanding* is not billing-gated,
only generation is.** So the free work comes first and the $30 click lands
second-to-last, after the checks that stop a paid batch producing art that needs
redoing.

| # | Item | Blocked? | Effort | Accept when |
|---|---|---|---|---|
| **C1** | ~~Audit all 346 existing images by machine, free~~ | No | 1d | every image tested against its own card's callouts; a picture cannot pass while contradicting the card it sits on. **Partially done 2026-09-08, operator: the tool exists and its logic is proven (`PLAN-MEDIA-2026-09-07.md` A4), the 346-image run does not, blocked here on `GEMINI_API_KEY`/egress neither present in this sandbox.** |
| **C2** | ~~Fix the 27 unsourced claims still in the card corpus before any render~~ | No | 0.5d | **Found already clean, 2026-09-08, operator.** Ran `gate_unsourced_stats` (checks `ops/cardtext/*.json`, `build/*-cardtext.json`, `build/cardtext/*.json` and all 190 rendered pages for a numeric claim about people/results or an uncited appeal to research) as part of this cycle's preflight: 0 hits, no warning fired. Not re-derived from a diff against the "27" figure; recorded here as a current-state check, the same way B2/B3/C3 turned out stale. If a fresh count is ever needed, `gate_unsourced_stats`'s own hit list is the source, not a re-grep. |
| **C3** | ~~Repair 412 mid-word truncated sentences across 113 of 114 descriptions~~ | No | 0.5d | **Found already done, 2026-09-08, operator.** Phil's own `b6fe7165` (2026-09-07 evening) rewrote `first_sentence()` in `ops/build_youtube_metadata.py`: it now takes the whole first sentence whatever its length, and only falls back to a word-boundary cut with an ellipsis when no sentence end exists at all, replacing the old hard `text[:limit]` slice that produced "tape it inside the coat cu". Verified live: scanned all 115 committed `build/video/youtube/*.json` description files for a first line ending on a bare letter with no terminator; 0 found. The 12 already-published videos keep their frozen YouTube descriptions (not reachable by this fix, noted in `b6fe7165` itself); the 102 unpublished all carry the corrected text. |
| **C4** (card artwork; not `REVIEW-COMMERCE-2026-09-07.md`'s own C4 row, corporate nav/footer distribution, done 2026-09-22) | ~~Stop the card pipeline shipping images marked `no`~~ **Renamed: the free download ships 12 cards with no artwork** (**updated 2026-09-18: 7, not 9**; EP-008, ET-004, EU-011 fixed 2026-09-15, EH-004 and EP-007 fixed 2026-09-18 on Phil's own workstation, disclosure now 81 of 88; the seven left show a text-only objective panel, not a glyph; LRN-0012) | No, to find it; **YES, Phil** to fix it | 0.3d to find, then C5 | **Reframed 2026-09-08, operator.** The previous investigation was right and stopped one question short. The pipeline does not ship rejected images: `approved_heroes()` excludes them, correctly. What nobody recorded is what happens instead. A card whose hero is rejected still renders, with a placeholder glyph where the photograph goes, and those cards go into the free print-and-play PDF. 12 of 88 heroes are rejected, so 12 of 89 cards in that download have no picture. EE-002 "Rainstorm" confirmed on page 1 by pixel-matching all 178 images embedded in the PDF, distance 0.02. On the same two pages that link that download, the gallery shows Rainstorm illustrated with five numbered callouts, from a separate source. So the browsing experience and the downloaded product disagree, on the top-of-funnel artifact. `gate_deck_download_has_art` now names the twelve every run; the fix is C5. |
| **C5** | **Enable Gemini billing, then generate** | **YES, Phil** | 5 min | 89 card heroes + 7 zone rejects = **$13.53**; everything including a full re-shoot = **$47.26**. **Superseded, see `OWNER-ACTIONS.md` 1b: neither the card heroes nor the zone rejects (now 8, not 7, a wrongly-approved kitchen counter hero withdrawn 2026-09-11) actually need this gate. Both are a free local retry on Phil's own machine; only the 11 room chapters are genuinely billing-gated.** |
| **C6** | Publish the 102 remaining videos | **YES, Phil** | 5 min | OAuth paste; uploader is built and dry-run clean |
| **C7** | ~~Fix the "What done looks like" screen's word-dropping split, found live on 11 of 12 published videos~~ | No, to fix the generator; **YES, Phil** to re-narrate/re-render/re-upload | 0.5d | **Root cause fixed 2026-09-15, this operator, picking up where a concurrent local session left off.** That session fixed `ops/build_social_pins.py`'s copy of the split the same day but had not checked `ops/video_zone.py`'s own independent copy, the one that actually feeds narration, captions and the batch renderer for all 114 zone videos: it cut at every comma and every " and ", kept only fragments of three words or more, and capped at four, so "one wallet and one phone per adult" lost "one wallet and" and multi-sentence standards were welded into one item. Consolidated into one real function, `video_zone.done_items()`; `build_social_pins.py` and the third, cruder copy in `video_zone_photo.py` (comma-only, first three) both now call it. New `gate_done_items_single_source` in `preflight.py`, fail-then-pass proved against both the "beats() forgets to call it" and "a sibling file forks its own copy again" regression shapes. Also regenerated the 114 committed `build/video/zones/*.srt` sidecars (the plain-typographic pipeline, pure Python, no browser or audio needed): 92 of 114 had gone stale the moment the source changed, now current. **Not reachable from this sandbox:** `build/video/zones-narrated/*.srt` and the .mp4 files themselves need real TTS audio and ffmpeg to regenerate, so the already-published 11 of 12 YouTube videos still say the wrong thing until Phil re-runs `ops/video_narrated.py` locally and re-uploads them; the 102 unpublished videos will render correctly the first time once that happens, since the generator itself is now fixed. Full `preflight.py` (every gate passed, 23 warnings, none new), all 151 test files, `check_urls.py` (188/188), `audit_pages.py` (191/0), `affiliate.py --check` (162 documents) all clean after. Mobile `npm test` not run: nothing in `mobile/` was touched. |
| **C7 follow-on, same day, this operator.** A second concurrent local session had already measured the consequence of this exact fix on the wide (16:9, YouTube) frame before it landed: once `done_items()` stops truncating, 35 of 114 zones' checklists collide with the footer, because `beats()`'s one `SHELL` template reused the portrait frame's padding (`300px`/`470px`) and footer position (`bottom:360px`) for the 1080px-tall wide frame too, leaving only 310px of content height. That session measured and proposed exact replacement values but did not land them ("not work to start at the end of a session"). Applied them here: `page()` now picks `padding:110px 120px 170px` and `.foot{bottom:80px;left:120px}` for `--wide`, the portrait frame unchanged. Verified with real Chromium screenshots (this sandbox has one, contrary to what earlier cycles assumed): all four already-published, previously-colliding Kitchen zones (Cooking, Lower Cabinet and Cookware, Refrigerator and Freezer, Sink and Dishwashing) render clear of the footer in wide mode; the corpus-wide worst case by character count, Guest Vanity Storage (424 chars across 3 items), also clears; the hook, call and a long Safety-pass instructional beat all still render correctly, portrait unaffected. No srt regeneration needed for this part: it is a layout-only change, the beat text and timing are unchanged. Full `preflight.py`, all 151 test files, `check_urls.py`, `audit_pages.py`, `affiliate.py --check` all clean after. |

---

## 5. Hold: correct, but ahead of the constraint

Not dropped. Each states what has to be true first.

| Item | Waiting on |
|---|---|
| Roll Sustain-style depth and diagnosis to all 114 | a 21-day read of a 12-zone pilot |
| Decks 3 to 20 | evidence that anybody wants deck 2 |
| The email list and any gated download | traffic. A capture form in front of 2 visitors a day captures nobody |
| Physical decks, paid decks | a stranger buying anything at all |
| A/B tests of any kind | the registry computes 1,427 days to significance |
| Re-applying to Impact | a network that declined a site with 52 visitors will decline it again, and traffic (60 as of 2026-09-07) is still nowhere near the range that would change their answer |

---

**The shop's App tile will need one line changed, but not yet.** The live
catalogue carries `APP-FREE`, "The Home Quest, web app, free", with the line
"No email, no account, nothing to install." That is true today and becomes
false the day a store build exists. Changing it now would advertise a phone
app nobody can download, so this is tied to gate 5 below, not done ahead of
it.

---

## 6. Owner gates, ranked by what they unblock

| # | Action | Time | Unblocks |
|---|---|---|---|
| 1 | **YouTube OAuth paste** | 5 min | 102 finished videos with captions and thumbnails |
| 2 | **Search Console verification** | 3 min | all query data. It does not backfill: every unverified day is gone permanently |
| 3 | **Gemini billing** | 5 min | every image on the roadmap, $13.53 to $47.26 |
| 4 | Amazon KDP and Etsy accounts | 30 min | the only channels with buyers already in them |
| 5 | Apple ($99/yr) and Play ($25) developer accounts | 15 min + money | the phone app. Everything up to submission is committed: art, listing, price decision |
| 6 | Six screenshots while running the 16 on-device checks | 0 min extra, free | both store listings. Neither store accepts a listing without them, and they cannot be honestly faked |

---

## 7. What I would tell you if you only read one line

The site is technically finished and commercially unproven. Six independent
reviews this week found no significant technical debt and eleven separate places
where **the source was corrected and the shipped artifact was never re-derived
from it**. That is now the dominant defect class, it is what the gates are being
rebuilt around, and it is more dangerous than any missing feature, because every
one of those defects passed a check that reported green.
